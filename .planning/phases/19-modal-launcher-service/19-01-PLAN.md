---
phase: 19-modal-launcher-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Valt.UI/Services/IModalLauncher.cs
  - src/Valt.UI/Services/ModalLauncher.cs
  - src/Valt.UI/Services/DesignTimeModalLauncher.cs
  - src/Valt.UI/Extensions.cs
  - tests/Valt.Tests/UI/Services/ModalLauncherTests.cs
autonomous: true
requirements:
  - VM-SVC-03
user_setup: []

must_haves:
  truths:
    - A reusable IModalLauncher service exists in Valt.UI.Services
    - ModalLauncher delegates modal creation to IModalFactory and presentation to ShowDialogSafeAsync
    - DesignTimeModalLauncher keeps the MainViewModel design-time constructor working
    - ModalLauncher has unit tests covering factory delegation, result plumbing, and VM configuration
  artifacts:
    - path: src/Valt.UI/Services/IModalLauncher.cs
      provides: Modal launcher abstraction
      exports: ["IModalLauncher"]
    - path: src/Valt.UI/Services/ModalLauncher.cs
      provides: Production launcher implementation
      exports: ["ModalLauncher"]
    - path: src/Valt.UI/Services/DesignTimeModalLauncher.cs
      provides: Design-time launcher no-op
      exports: ["DesignTimeModalLauncher"]
    - path: tests/Valt.Tests/UI/Services/ModalLauncherTests.cs
      provides: Unit tests for ModalLauncher
      contains: "class ModalLauncherTests"
  key_links:
    - from: src/Valt.UI/Services/ModalLauncher.cs
      to: src/Valt.UI/Services/IModalFactory.cs
      via: constructor injection and CreateAsync delegation
      pattern: "_modalFactory.CreateAsync"
    - from: src/Valt.UI/Services/ModalLauncher.cs
      to: src/Valt.UI/Base/WindowDialogExtensions.cs
      via: ShowDialogSafeAsync extension methods
      pattern: "ShowDialogSafeAsync"
    - from: src/Valt.UI/Extensions.cs
      to: src/Valt.UI/Services/IModalLauncher.cs
      via: AddSingleton<IModalLauncher, ModalLauncher>
      pattern: "IModalLauncher"
---

<objective>
Create the reusable modal-launcher service abstraction and production implementation, register it in the UI DI container, and cover it with unit tests.

Purpose: Remove the repeated `_modalFactory.CreateAsync` + cast + `ShowDialogSafeAsync` boilerplate from `MainViewModel` by introducing a thin, testable seam.
Output: `IModalLauncher`, `ModalLauncher`, `DesignTimeModalLauncher`, DI registration, and `ModalLauncherTests`.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-modal-launcher-service/19-CONTEXT.md
@.planning/phases/19-modal-launcher-service/19-RESEARCH.md
@src/Valt.UI/Services/IModalFactory.cs
@src/Valt.UI/Services/ModalFactory.cs
@src/Valt.UI/Services/DesignTimeModalFactory.cs
@src/Valt.UI/Base/WindowDialogExtensions.cs
@src/Valt.UI/Base/IValtModal.cs
@src/Valt.UI/Extensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IModalLauncher interface and ModalLauncher implementation</name>
  <read_first>
    src/Valt.UI/Services/IModalFactory.cs
    src/Valt.UI/Services/ModalFactory.cs
    src/Valt.UI/Base/WindowDialogExtensions.cs
    src/Valt.UI/Base/IValtModal.cs
    src/Valt.UI/Base/ValtBaseWindow.cs
  </read_first>
  <files>
    src/Valt.UI/Services/IModalLauncher.cs
    src/Valt.UI/Services/ModalLauncher.cs
  </files>
  <action>
    Create `src/Valt.UI/Services/IModalLauncher.cs` with three overloads: `Task ShowAsync(ApplicationModalNames modalName, Window owner, object? parameter = null)`, `Task<TResult?> ShowAsync<TResult>(ApplicationModalNames modalName, Window owner, object? parameter = null)`, and `Task<TResult?> ShowAsync<TViewModel, TResult>(ApplicationModalNames modalName, Window owner, Action<TViewModel>? configure = null, object? parameter = null)`.
    Create `src/Valt.UI/Services/ModalLauncher.cs` implementing `IModalLauncher`. Inject `IModalFactory` in the constructor. Each overload must call `_modalFactory.CreateAsync(modalName, owner, parameter)`, throw `InvalidOperationException` with the modal name if the factory returns null, apply the `configure` action only when `modal.DataContext` is `TViewModel`, and delegate presentation to protected virtual `ShowDialogAsync` hooks that call `dialog.ShowDialogSafeAsync(owner)`. Do not store `Window` in the service; pass owner per call.
  </action>
  <acceptance_criteria>
    Both files compile, the interface exposes all three overloads, and the implementation delegates creation to `IModalFactory` and presentation to `ShowDialogSafeAsync`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Valt.UI/Valt.UI.csproj</automated>
  </verify>
  <done>
    `IModalLauncher.cs` and `ModalLauncher.cs` exist, compile, and implement the specified contract with virtual show hooks for testability.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DesignTimeModalLauncher and register IModalLauncher in DI</name>
  <read_first>
    src/Valt.UI/Extensions.cs
    src/Valt.UI/Services/DesignTimeModalFactory.cs
    src/Valt.UI/Views/Main/MainViewModel.cs
  </read_first>
  <files>
    src/Valt.UI/Services/DesignTimeModalLauncher.cs
    src/Valt.UI/Extensions.cs
  </files>
  <action>
    Create `src/Valt.UI/Services/DesignTimeModalLauncher.cs` implementing `IModalLauncher` as a no-op: `ShowAsync` returns `Task.CompletedTask`, the two typed overloads return `Task.FromResult(default(TResult?))`. In `src/Valt.UI/Extensions.cs`, register the production launcher with `services.AddSingleton<IModalLauncher, ModalLauncher>()` immediately after `services.AddSingleton<IModalFactory, ModalFactory>()`. Do not change the design-time constructor registration path.
  </action>
  <acceptance_criteria>
    `DesignTimeModalLauncher` compiles and safely no-ops for all overloads; `Extensions.cs` registers `IModalLauncher` as a singleton.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Valt.UI/Valt.UI.csproj && grep -c "AddSingleton<IModalLauncher, ModalLauncher>" src/Valt.UI/Extensions.cs</automated>
  </verify>
  <done>
    DI registration for `IModalLauncher` exists in `AddValtUI`, and the design-time no-op launcher is available.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ModalLauncher unit tests</name>
  <read_first>
    src/Valt.UI/Services/ModalLauncher.cs
    src/Valt.UI/Services/IModalFactory.cs
    tests/Valt.Tests/UI/Services/TransactionDetailsBuilderTests.cs
  </read_first>
  <files>
    tests/Valt.Tests/UI/Services/ModalLauncherTests.cs
  </files>
  <behavior>
    - Test 1: `ShowAsync` delegates to `IModalFactory.CreateAsync` with the modal name, owner, and parameter, then shows the dialog.
    - Test 2: `ShowAsync<TResult>` returns the typed result produced by the dialog.
    - Test 3: `ShowAsync<TViewModel, TResult>` invokes the configure action on the modal's DataContext when it matches `TViewModel`.
    - Test 4: A null result from `IModalFactory.CreateAsync` throws `InvalidOperationException` containing the modal name.
  </behavior>
  <action>
    Create `tests/Valt.Tests/UI/Services/ModalLauncherTests.cs` using NUnit and NSubstitute. Use a testable subclass of `ModalLauncher` that overrides the protected virtual `ShowDialogAsync` hooks to avoid instantiating real Avalonia windows. Mock `IModalFactory` to return a fake `ValtBaseWindow` with a configurable `DataContext`. Assert the factory receives the expected modal name, owner, and parameter; assert typed results round-trip; assert the configure action mutates the ViewModel; assert null factory output throws.
  </action>
  <acceptance_criteria>
    All four ModalLauncher unit tests pass and the test project builds.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Valt.Tests/Valt.Tests.csproj --filter "FullyQualifiedName~ModalLauncherTests"</automated>
  </verify>
  <done>
    `ModalLauncherTests` exists with at least four passing tests covering delegation, result plumbing, VM configuration, and null-factory error handling.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Valt.UI/Valt.UI.csproj` passes.
2. `dotnet test tests/Valt.Tests/Valt.Tests.csproj --filter "FullyQualifiedName~ModalLauncherTests"` passes.
3. `grep` confirms `Extensions.cs` contains `AddSingleton<IModalLauncher, ModalLauncher>`.
</verification>

<success_criteria>
- `IModalLauncher` and `ModalLauncher` exist in `Valt.UI.Services`.
- `ModalLauncher` delegates to `IModalFactory` and `ShowDialogSafeAsync`.
- `DesignTimeModalLauncher` is available for designer-safe construction.
- DI registration wires `IModalLauncher` as a singleton.
- Unit tests cover all three `ShowAsync` overloads and the null-factory path.
</success_criteria>

## Artifacts this phase produces

- `src/Valt.UI/Services/IModalLauncher.cs` — service contract.
- `src/Valt.UI/Services/ModalLauncher.cs` — production implementation wrapping `IModalFactory` and `ShowDialogSafeAsync`.
- `src/Valt.UI/Services/DesignTimeModalLauncher.cs` — no-op design-time implementation.
- `src/Valt.UI/Extensions.cs` — updated with `AddSingleton<IModalLauncher, ModalLauncher>()`.
- `tests/Valt.Tests/UI/Services/ModalLauncherTests.cs` — unit tests for the launcher.

<output>
Create `.planning/phases/19-modal-launcher-service/19-01-SUMMARY.md` when done.
</output>
