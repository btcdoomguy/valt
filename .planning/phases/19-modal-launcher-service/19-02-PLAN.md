---
phase: 19-modal-launcher-service
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/Valt.UI/Views/Main/MainViewModel.cs
  - tests/Valt.Tests/UI/ViewModels/MainViewModelModalCommandTests.cs
autonomous: true
requirements:
  - VM-SVC-03
user_setup: []

must_haves:
  truths:
    - MainViewModel receives IModalLauncher instead of IModalFactory
    - All modal launch sites in MainViewModel use _modalLauncher.ShowAsync
    - Modal behavior remains unchanged
    - Repetitive modal launch boilerplate is removed
    - MainViewModel modal commands have unit tests
  artifacts:
    - path: src/Valt.UI/Views/Main/MainViewModel.cs
      provides: Main shell using modal launcher service
      contains: "_modalLauncher"
    - path: tests/Valt.Tests/UI/ViewModels/MainViewModelModalCommandTests.cs
      provides: Unit tests for MainViewModel modal commands
      contains: "class MainViewModelModalCommandTests"
  key_links:
    - from: src/Valt.UI/Views/Main/MainViewModel.cs
      to: src/Valt.UI/Services/IModalLauncher.cs
      via: _modalLauncher field and constructor injection
      pattern: "_modalLauncher"
    - from: src/Valt.UI/Views/Main/MainViewModel.cs
      to: src/Valt.UI/Services/DesignTimeModalLauncher.cs
      via: design-time constructor assignment
      pattern: "DesignTimeModalLauncher"
    - from: tests/Valt.Tests/UI/ViewModels/MainViewModelModalCommandTests.cs
      to: src/Valt.UI/Views/Main/MainViewModel.cs
      via: command execution verification
      pattern: "MainViewModel"
---

<objective>
Refactor `MainViewModel` to use the new `IModalLauncher` service for every modal launch, removing the repetitive `_modalFactory` boilerplate, and add unit tests for the modal commands.

Purpose: Realize the VM-SVC-03 requirement by consolidating modal presentation in `MainViewModel` behind the launcher abstraction.
Output: Refactored `MainViewModel.cs` and `MainViewModelModalCommandTests.cs`.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-modal-launcher-service/19-CONTEXT.md
@.planning/phases/19-modal-launcher-service/19-RESEARCH.md
@src/Valt.UI/Views/Main/MainViewModel.cs
@src/Valt.UI/Services/IModalLauncher.cs
@src/Valt.UI/Services/ModalLauncher.cs
@src/Valt.UI/Services/DesignTimeModalLauncher.cs
@src/Valt.UI/Views/ApplicationModalNames.cs
@src/Valt.UI/Views/Main/Modals/InputPassword/InputPasswordViewModel.cs
@src/Valt.UI/Views/Main/Modals/InitialSelection/InitialSelectionViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor MainViewModel to use IModalLauncher</name>
  <read_first>
    src/Valt.UI/Views/Main/MainViewModel.cs
    src/Valt.UI/Services/IModalLauncher.cs
    src/Valt.UI/Services/DesignTimeModalLauncher.cs
    src/Valt.UI/Views/ApplicationModalNames.cs
  </read_first>
  <files>
    src/Valt.UI/Views/Main/MainViewModel.cs
  </files>
  <action>
    Replace `IModalFactory _modalFactory` with `IModalLauncher _modalLauncher` in the runtime constructor and assign `new DesignTimeModalLauncher()` in the parameterless design-time constructor. Convert every modal launch site to a one-line `_modalLauncher.ShowAsync(...)` call:
    - `OpenPriceHistory`, `ManageCategories`, `ManageSettings`, `OpenImportWizard`, `About`, `OpenConversionCalculator`, `OpenLeverageSimulator`, `OpenSpendingEvolution`, `OpenStatusDisplay`, and `ShowTipsAsync` use the non-result overload.
    - `ToggleSecureMode` uses the generic `ShowAsync<InputPasswordViewModel, InputPasswordViewModel.Response?>` overload and passes a configure action that sets `HideSecureModeCheckbox = true`.
    - `OpenInitialSelectionModal` uses the typed-result `ShowAsync<InitialSelectionViewModel, InitialSelectionViewModel.Response?>` overload.
    Remove all casts to concrete modal view types and delete the now-unused modal view `using` directives. Keep the exact order of operations, null checks, and post-dialog logic unchanged.
  </action>
  <acceptance_criteria>
    `MainViewModel` has `_modalLauncher`, has no `_modalFactory` field, all modal launch sites call `_modalLauncher.ShowAsync`, unused modal view usings are removed, and the file builds.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Valt.UI/Valt.UI.csproj && grep -c "_modalFactory" src/Valt.UI/Views/Main/MainViewModel.cs</automated>
  </verify>
  <done>
    Build passes and `_modalFactory` references in `MainViewModel.cs` are zero.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MainViewModel modal command tests</name>
  <read_first>
    src/Valt.UI/Views/Main/MainViewModel.cs
    src/Valt.UI/Services/IModalLauncher.cs
    tests/Valt.Tests/UI/Services/TransactionDetailsBuilderTests.cs
  </read_first>
  <files>
    tests/Valt.Tests/UI/ViewModels/MainViewModelModalCommandTests.cs
  </files>
  <behavior>
    - Test 1: `OpenPriceHistoryCommand.ExecuteAsync` calls `_modalLauncher.ShowAsync(ApplicationModalNames.PriceHistory, owner)`.
    - Test 2: `ManageCategoriesCommand.ExecuteAsync` calls `_modalLauncher.ShowAsync(ApplicationModalNames.ManageCategories, owner)`.
    - Test 3: `AboutCommand.ExecuteAsync` calls `_modalLauncher.ShowAsync(ApplicationModalNames.About, owner)`.
    - Test 4: When secure mode is enabled, `ToggleSecureModeCommand.ExecuteAsync` calls the generic `ShowAsync<InputPasswordViewModel, InputPasswordViewModel.Response?>` overload and the configure action sets `HideSecureModeCheckbox` to true.
  </behavior>
  <action>
    Create `tests/Valt.Tests/UI/ViewModels/MainViewModelModalCommandTests.cs` using NUnit and NSubstitute. Build a `MainViewModel` with `Substitute.For` for all interface dependencies, real parameterless state objects (`SecureModeState`, `McpServerState`, `TabRefreshState`, `FilterState`), `new BackgroundJobManager([])`, and `Substitute.For<Window>()` for the owner. Set `vm.Window = owner` before executing commands. For the `ToggleSecureMode` test, pre-enable secure mode and supply a valid password response. Assert the launcher receives the correct modal name, owner, and configure behavior.
  </action>
  <acceptance_criteria>
    All MainViewModel modal command tests pass and the test project builds.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Valt.Tests/Valt.Tests.csproj --filter "FullyQualifiedName~MainViewModelModalCommandTests"</automated>
  </verify>
  <done>
    `MainViewModelModalCommandTests` exists with at least four passing tests verifying the no-result and generic configure launcher patterns.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build solution and run full test suite</name>
  <read_first>
    Valt.sln
    .planning/STATE.md
  </read_first>
  <files>
    Valt.sln
  </files>
  <action>
    Build the full solution with `dotnet build Valt.sln` and run `dotnet test`. Fix any compilation errors introduced by the constructor signature change or DI registration. Treat only the pre-existing live-API failures (`BitcoinDominanceProviderTests` and `CoinGeckoProviderTests` from Phase 25 debt in STATE.md) as acceptable failures; all other tests must pass.
  </action>
  <acceptance_criteria>
    Solution builds and the full test suite is green except for the known Phase 25 live-API network failures.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Valt.sln && dotnet test</automated>
  </verify>
  <done>
    `dotnet build Valt.sln` succeeds; `dotnet test` passes except for the documented live-API tests.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Valt.UI/Valt.UI.csproj` passes with zero `_modalFactory` references in `MainViewModel.cs`.
2. `dotnet test tests/Valt.Tests/Valt.Tests.csproj --filter "FullyQualifiedName~MainViewModelModalCommandTests"` passes.
3. `dotnet build Valt.sln` passes.
4. `dotnet test` passes except for the pre-existing `BitcoinDominanceProviderTests` and `CoinGeckoProviderTests` network failures (Phase 25 debt).
</verification>

<success_criteria>
- `MainViewModel` constructor injects `IModalLauncher` and no longer holds `IModalFactory`.
- Every modal-launch command in `MainViewModel` delegates to `_modalLauncher.ShowAsync`.
- Unused modal view `using` directives and cast boilerplate are removed.
- `MainViewModelModalCommandTests` verify at least the no-result and generic configure patterns.
- Full solution builds and existing tests remain green.
</success_criteria>

## Artifacts this phase produces

- `src/Valt.UI/Views/Main/MainViewModel.cs` — refactored to use `IModalLauncher` for all modal launches.
- `tests/Valt.Tests/UI/ViewModels/MainViewModelModalCommandTests.cs` — unit tests for modal command delegation.

<output>
Create `.planning/phases/19-modal-launcher-service/19-02-SUMMARY.md` when done.
</output>
