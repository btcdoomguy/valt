---
phase: 07-commands-queries
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetHandler.cs
  - src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
  - tests/Valt.Tests/Application/Assets/Commands/EditAssetHandlerSnapshotPreservationTests.cs
  - tests/Valt.Tests/Application/Assets/Queries/GetBtcLoansDashboardHandlerSnapshotTests.cs
autonomous: true
requirements:
  - QUERY-03
  - TEST-02
user_setup: []
must_haves:
  truths:
    - Editing a BTC loan preserves its existing snapshot timeline
    - AssetQueries.MapBtcLoan exposes latest-snapshot values for current-state fields
    - GetBtcLoansDashboardQuery reflects the latest snapshot's debt, collateral, APR, LTV, and health status
    - Deleting the latest snapshot causes dashboard calculations to fall back to the previous snapshot or setup values
  artifacts:
    - path: src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetHandler.cs
      provides: BTC loan edit rebuild with snapshots preserved
    - path: src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
      provides: Snapshot-aware MapBtcLoan projection
    - path: tests/Valt.Tests/Application/Assets/Commands/EditAssetHandlerSnapshotPreservationTests.cs
      provides: Edit snapshot preservation regression test
    - path: tests/Valt.Tests/Application/Assets/Queries/GetBtcLoansDashboardHandlerSnapshotTests.cs
      provides: Dashboard latest-snapshot integration test
  key_links:
    - from: EditAssetHandler.BuildBtcLoanDetails
      to: BtcLoanDetails.Snapshots
      via: snapshots: existingSnapshots
    - from: AssetQueries.MapBtcLoan
      to: BtcLoanDetails.Snapshots
      via: MaxBy(s => s.EffectiveDate)
    - from: GetBtcLoansDashboardHandler
      to: AssetQueries.MapBtcLoan
      via: IAssetQueries.GetAllAsync
---

<objective>
Close the gaps that would cause existing queries and edits to ignore or destroy the snapshot timeline.

Purpose: Ensure the loan dashboard, summary, and edit flows treat the latest snapshot as the single source of truth for current-state values, and prevent editing a BTC loan from wiping its history.

Output: Snapshot-aware MapBtcLoan, EditAssetHandler fix, and regression/integration tests.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-commands-queries/07-CONTEXT.md
@.planning/phases/07-commands-queries/07-RESEARCH.md
@.planning/phases/07-commands-queries/07-PATTERNS.md
@src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetHandler.cs
@src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetCommand.cs
@src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
@src/Valt.App/Modules/Assets/Queries/GetBtcLoansDashboard/GetBtcLoansDashboardHandler.cs
@src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
@src/Valt.Core/Modules/Assets/Details/BtcLoanDetails.cs
@src/Valt.Core/Modules/Assets/Details/LoanStateSnapshot.cs
@src/Valt.Core/Modules/Assets/Asset.cs
@tests/Valt.Tests/Application/Assets/Queries/GetBtcLoansDashboardHandlerTests.cs
@tests/Valt.Tests/Builders/AssetBuilder.cs
@tests/Valt.Tests/DatabaseTest.cs
</context>

## Artifacts this phase produces

- Modified `EditAssetHandler.BuildBtcLoanDetails` method (passes existing snapshots forward)
- Modified `AssetQueries.MapBtcLoan` method (uses latest snapshot for current-state fields)
- `EditAssetHandlerSnapshotPreservationTests` (NUnit test fixture)
- `GetBtcLoansDashboardHandlerSnapshotTests` (NUnit test fixture)

<tasks>

<task type="auto">
  <name>Task 1: Update AssetQueries.MapBtcLoan to use the latest snapshot for current-state fields</name>
  <files>
    src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
  </files>
  <read_first>
    - src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
    - src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
    - src/Valt.Core/Modules/Assets/Details/BtcLoanDetails.cs
    - src/Valt.Core/Modules/Assets/Details/LoanStateSnapshot.cs
    - .planning/phases/07-commands-queries/07-RESEARCH.md
  </read_first>
  <action>
    In AssetQueries.MapBtcLoan, resolve the latest snapshot once with `var snapshot = btcLoan.Snapshots.MaxBy(s => s.EffectiveDate);`. Use snapshot values when present for the following AssetDTO fields: CollateralSats, LoanAmount, Apr, LiquidationLtv, MarginCallLtv, Fees, RepaymentDate, LoanStatusId, FixedTotalDebt, and the BTC price used for LTV/health/distance calculations. Keep InitialLtv, LoanStartDate, and PlatformName from the immutable setup values. Continue to set AccruedInterest = btcLoan.CalculateAccruedInterest() and TotalDebt = btcLoan.CalculateTotalDebt(). Compute currentLtv, healthStatus, and distanceToLiquidation using `snapshot?.CurrentBtcPriceInLoanCurrency ?? btcLoan.CurrentBtcPriceInLoanCurrency` so the latest snapshot's recorded price is used when available. Set HasFixedTotalDebt from `snapshot?.FixedTotalDebt.HasValue ?? btcLoan.HasFixedTotalDebt`.
  </action>
  <acceptance_criteria>
    - MapBtcLoan contains a local `snapshot` variable assigned from `btcLoan.Snapshots.MaxBy(s => s.EffectiveDate)`.
    - AssetDTO.CollateralSats, LoanAmount, Apr, LiquidationLtv, MarginCallLtv, Fees, RepaymentDate, LoanStatusId, and FixedTotalDebt are mapped from `snapshot` when it is not null.
    - LTV/health/distance calculations use the snapshot's CurrentBtcPriceInLoanCurrency when present.
    - Existing GetBtcLoansDashboardHandlerTests continue to pass (loans without snapshots behave identically).
    - `dotnet test --filter "FullyQualifiedName~GetBtcLoansDashboardHandlerTests" -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~GetBtcLoansDashboardHandlerTests" -x</automated>
  </verify>
  <done>MapBtcLoan projects latest-snapshot values for current-state fields while keeping setup-only fields immutable.</done>
</task>

<task type="auto">
  <name>Task 2: Fix EditAssetHandler to preserve BTC loan snapshots</name>
  <files>
    src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetHandler.cs
    tests/Valt.Tests/Application/Assets/Commands/EditAssetHandlerSnapshotPreservationTests.cs
  </files>
  <read_first>
    - src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetHandler.cs
    - src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetCommand.cs
    - src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetValidator.cs
    - src/Valt.Core/Modules/Assets/Details/BtcLoanDetails.cs
    - src/Valt.Core/Modules/Assets/Asset.cs
    - tests/Valt.Tests/Application/Assets/Commands/CreateBtcLoanHandlerTests.cs
    - tests/Valt.Tests/Builders/AssetBuilder.cs
    - tests/Valt.Tests/DatabaseTest.cs
    - .planning/phases/07-commands-queries/07-RESEARCH.md
  </read_first>
  <action>
    1. In EditAssetHandler.BuildBtcLoanDetails, capture the existing snapshot list before rebuilding: `var existingSnapshots = existingAsset.Details is BtcLoanDetails existingLoan ? existingLoan.Snapshots : null;`. Pass `snapshots: existingSnapshots` as the final argument to the `new BtcLoanDetails(...)` constructor call.

    2. Create EditAssetHandlerSnapshotPreservationTests inheriting DatabaseTest. Save a BTC loan with at least one snapshot using AssetBuilder.ABtcLoan(...).WithSnapshot(...).Build(). Dispatch an EditAssetCommand that changes the name (or another non-snapshot field) with a valid BtcLoanDetailsInputDTO. Reload the asset and assert that `((BtcLoanDetails)asset.Details).Snapshots` still contains the original snapshot(s) with the original EffectiveDate and CurrentTotalDebt.
  </action>
  <acceptance_criteria>
    - The BtcLoanDetails constructor call in BuildBtcLoanDetails includes `snapshots: existingSnapshots`.
    - EditAssetHandler.cs compiles and existing EditAsset tests (if any) still pass.
    - EditAssetHandlerSnapshotPreservationTests exists and asserts snapshot survival after edit.
    - `dotnet test --filter "FullyQualifiedName~EditAssetHandlerSnapshotPreservationTests" -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~EditAssetHandlerSnapshotPreservationTests" -x</automated>
  </verify>
  <done>Editing a BTC loan no longer drops its snapshot history; regression test protects the behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard latest-snapshot integration tests</name>
  <files>
    tests/Valt.Tests/Application/Assets/Queries/GetBtcLoansDashboardHandlerSnapshotTests.cs
  </files>
  <read_first>
    - tests/Valt.Tests/Application/Assets/Queries/GetBtcLoansDashboardHandlerTests.cs
    - src/Valt.App/Modules/Assets/Queries/GetBtcLoansDashboard/GetBtcLoansDashboardHandler.cs
    - src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
    - tests/Valt.Tests/Builders/AssetBuilder.cs
    - tests/Valt.Tests/DatabaseTest.cs
  </read_first>
  <action>
    Create GetBtcLoansDashboardHandlerSnapshotTests inheriting DatabaseTest. Add two tests:

    1. Latest snapshot drives dashboard: Save a BTC loan with an initial setup (e.g., loanAmount 25_000, collateral 1 BTC, apr 0.12) and a later snapshot with currentTotalDebt 30_000 and lower collateral sats (e.g., 80_000_000) using AssetBuilder.ABtcLoan(...).WithSnapshot(effectiveDate: new DateOnly(2025, 6, 1), currentTotalDebt: 30_000m, collateralSats: 80_000_000L).Build(). Run GetBtcLoansDashboardQuery with USD rates and BTC price 50_000. Assert TotalDebtInMainCurrency equals 30_000, DebtWeightedAvgLtv reflects the snapshot collateral/debt, and DebtWeightedAvgApr reflects the snapshot Apr (if changed).

    2. Fallback after deleting latest snapshot: Add a second snapshot, delete it via DeleteLoanStateUpdateCommand, then run the dashboard query and assert TotalDebtInMainCurrency returns to the first snapshot's currentTotalDebt (or setup value if only the seeded snapshot remains).
  </action>
  <acceptance_criteria>
    - Test file exists at the exact path and inherits DatabaseTest.
    - Test asserts dashboard TotalDebtInMainCurrency uses the latest snapshot's CurrentTotalDebt.
    - Test asserts dashboard LTV/APR/health metrics reflect snapshot values.
    - Test asserts fallback after deleting the latest snapshot.
    - `dotnet test --filter "FullyQualifiedName~GetBtcLoansDashboardHandlerSnapshotTests" -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~GetBtcLoansDashboardHandlerSnapshotTests" -x</automated>
  </verify>
  <done>Dashboard integration tests prove QUERY-03 behavior and snapshot fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit input -> EditAssetHandler | User-provided DTO could rebuild details without snapshots. |
| AssetQueries -> Dashboard | Read path must not use stale setup fields for current-state calculations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | EditAssetHandler.BuildBtcLoanDetails | mitigate | Capture and forward existingSnapshots; regression test fails if the argument is removed. |
| T-07-09 | Tampering | AssetQueries.MapBtcLoan | mitigate | Read CollateralSats, LoanAmount, Apr, LiquidationLtv, MarginCallLtv, Fees, FixedTotalDebt, RepaymentDate, Status, and current BTC price from the latest snapshot when present; fallback to setup values. |
| T-07-10 | Information Disclosure | Dashboard summary | accept | Dashboard already operates on asset data the user owns; no additional exposure. |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~EditAssetHandlerSnapshotPreservationTests|GetBtcLoansDashboardHandlerSnapshotTests" -x` passes.
- Existing `GetBtcLoansDashboardHandlerTests` still pass.
- `dotnet build Valt.sln` succeeds with 0 errors.
</verification>

<success_criteria>
- Editing a BTC loan preserves its snapshot timeline.
- MapBtcLoan exposes latest-snapshot values for current-state fields.
- Dashboard/summary queries reflect the latest snapshot and correctly fall back after deletion.
- All regression and integration tests pass.
</success_criteria>

<output>
Create `.planning/phases/07-commands-queries/07-03-SUMMARY.md` when done.
</output>
