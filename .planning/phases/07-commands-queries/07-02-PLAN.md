---
phase: 07-commands-queries
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/Valt.App/Modules/Assets/DTOs/LoanStateDTO.cs
  - src/Valt.App/Modules/Assets/DTOs/LoanStateSnapshotDTO.cs
  - src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineQuery.cs
  - src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineHandler.cs
  - src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateQuery.cs
  - src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateHandler.cs
  - tests/Valt.Tests/Application/Assets/Queries/GetLoanStateTimelineHandlerTests.cs
  - tests/Valt.Tests/Application/Assets/Queries/GetLatestLoanStateHandlerTests.cs
autonomous: true
requirements:
  - QUERY-01
  - QUERY-02
user_setup: []
must_haves:
  truths:
    - GetLoanStateTimelineQuery returns the full chronological list of snapshots for a BTC loan
    - GetLatestLoanStateQuery returns the latest snapshot fields plus the asset ID and name
    - Both queries return empty/null for non-existent or non-BTC assets
    - Snapshot data is projected into dedicated DTOs, not an enriched AssetDTO
  artifacts:
    - path: src/Valt.App/Modules/Assets/DTOs/LoanStateDTO.cs
      provides: Latest-state DTO with asset metadata
    - path: src/Valt.App/Modules/Assets/DTOs/LoanStateSnapshotDTO.cs
      provides: Timeline row DTO
    - path: src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineQuery.cs
      provides: Timeline query contract
    - path: src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineHandler.cs
      provides: Timeline query handler
    - path: src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateQuery.cs
      provides: Latest-state query contract
    - path: src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateHandler.cs
      provides: Latest-state query handler
    - path: tests/Valt.Tests/Application/Assets/Queries/GetLoanStateTimelineHandlerTests.cs
      provides: Timeline query tests
    - path: tests/Valt.Tests/Application/Assets/Queries/GetLatestLoanStateHandlerTests.cs
      provides: Latest-state query tests
  key_links:
    - from: GetLoanStateTimelineHandler
      to: BtcLoanDetails.Snapshots
      via: OrderBy(s => s.EffectiveDate)
    - from: GetLatestLoanStateHandler
      to: BtcLoanDetails.Snapshots
      via: MaxBy(s => s.EffectiveDate)
    - from: both handlers
      to: IAssetRepository
      via: _assetRepository.GetByIdAsync(new AssetId(query.AssetId))
---

<objective>
Implement read-only queries that expose the BTC loan snapshot timeline and the latest recorded state.

Purpose: Give the UI (Phase 8) and MCP clients the data they need to display history and prefill the update modal, without returning a bloated AssetDTO.

Output: LoanStateDTO, LoanStateSnapshotDTO, GetLoanStateTimeline query/handler/tests, and GetLatestLoanState query/handler/tests.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-commands-queries/07-CONTEXT.md
@.planning/phases/07-commands-queries/07-RESEARCH.md
@.planning/phases/07-commands-queries/07-PATTERNS.md
@src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
@src/Valt.App/Modules/Assets/Queries/GetAsset/GetAssetQuery.cs
@src/Valt.App/Modules/Assets/Queries/GetAsset/GetAssetHandler.cs
@src/Valt.Core/Modules/Assets/Asset.cs
@src/Valt.Core/Modules/Assets/Details/BtcLoanDetails.cs
@src/Valt.Core/Modules/Assets/Details/LoanStateSnapshot.cs
@src/Valt.Core/Modules/Assets/Contracts/IAssetRepository.cs
@tests/Valt.Tests/Builders/AssetBuilder.cs
@tests/Valt.Tests/DatabaseTest.cs
</context>

## Artifacts this phase produces

- `LoanStateSnapshotDTO` (record)
- `LoanStateDTO` (record)
- `GetLoanStateTimelineQuery` (record, `IQuery<IReadOnlyList<LoanStateSnapshotDTO>>`)
- `GetLoanStateTimelineHandler` (class, `IQueryHandler<GetLoanStateTimelineQuery, IReadOnlyList<LoanStateSnapshotDTO>>`)
- `GetLatestLoanStateQuery` (record, `IQuery<LoanStateDTO?>`)
- `GetLatestLoanStateHandler` (class, `IQueryHandler<GetLatestLoanStateQuery, LoanStateDTO?>`)
- `GetLoanStateTimelineHandlerTests` (NUnit test fixture)
- `GetLatestLoanStateHandlerTests` (NUnit test fixture)

<tasks>

<task type="auto">
  <name>Task 1: Create LoanStateDTO and LoanStateSnapshotDTO</name>
  <files>
    src/Valt.App/Modules/Assets/DTOs/LoanStateDTO.cs
    src/Valt.App/Modules/Assets/DTOs/LoanStateSnapshotDTO.cs
  </files>
  <read_first>
    - src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
    - src/Valt.Core/Modules/Assets/Details/LoanStateSnapshot.cs
    - .planning/phases/07-commands-queries/07-CONTEXT.md
    - .planning/phases/07-commands-queries/07-RESEARCH.md
  </read_first>
  <action>
    Create two DTO records in Valt.App.Modules.Assets.DTOs:

    1. LoanStateSnapshotDTO: required init-only properties mirroring LoanStateSnapshot — PlatformName (string), CollateralSats (long), LoanAmount (decimal), CurrencyCode (string), Apr (decimal), InitialLtv (decimal), LiquidationLtv (decimal), MarginCallLtv (decimal), Fees (decimal), LoanStartDate (DateOnly), RepaymentDate (DateOnly?), StatusId (int), CurrentBtcPriceInLoanCurrency (decimal), FixedTotalDebt (decimal?), CurrentTotalDebt (decimal), EffectiveDate (DateOnly), Note (string?).

    2. LoanStateDTO: include all LoanStateSnapshotDTO fields plus AssetId (string) and AssetName (string). Do not reuse or inherit from AssetDTO per D-05.
  </action>
  <acceptance_criteria>
    - LoanStateSnapshotDTO.cs defines a public record with all listed snapshot fields as required init properties.
    - LoanStateDTO.cs defines a public record with AssetId, AssetName, and the same snapshot fields as required init properties.
    - `dotnet build Valt.sln` succeeds after the DTOs are added.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>Both DTOs exist and compile, providing stable read models for timeline and latest-state queries.</done>
</task>

<task type="auto">
  <name>Task 2: Create timeline and latest-state query handlers</name>
  <files>
    src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineQuery.cs
    src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineHandler.cs
    src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateQuery.cs
    src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateHandler.cs
  </files>
  <read_first>
    - src/Valt.App/Modules/Assets/Queries/GetAsset/GetAssetQuery.cs
    - src/Valt.App/Modules/Assets/Queries/GetAsset/GetAssetHandler.cs
    - src/Valt.App/Modules/Assets/DTOs/LoanStateDTO.cs
    - src/Valt.App/Modules/Assets/DTOs/LoanStateSnapshotDTO.cs
    - src/Valt.Core/Modules/Assets/Asset.cs
    - src/Valt.Core/Modules/Assets/Details/BtcLoanDetails.cs
    - src/Valt.Core/Modules/Assets/Contracts/IAssetRepository.cs
    - .planning/phases/07-commands-queries/07-PATTERNS.md
  </read_first>
  <action>
    Create the query folders and files:

    1. GetLoanStateTimelineQuery.cs: public record GetLoanStateTimelineQuery : IQuery<IReadOnlyList<LoanStateSnapshotDTO>> with required string AssetId.

    2. GetLoanStateTimelineHandler.cs: internal sealed class implementing IQueryHandler<GetLoanStateTimelineQuery, IReadOnlyList<LoanStateSnapshotDTO>>. Inject IAssetRepository. Load the asset by ID; return an empty list if the asset is null or asset.Details is not BtcLoanDetails. Project btcLoan.Snapshots.OrderBy(s => s.EffectiveDate) into LoanStateSnapshotDTOs.

    3. GetLatestLoanStateQuery.cs: public record GetLatestLoanStateQuery : IQuery<LoanStateDTO?> with required string AssetId.

    4. GetLatestLoanStateHandler.cs: internal sealed class implementing IQueryHandler<GetLatestLoanStateQuery, LoanStateDTO?>. Inject IAssetRepository. Load the asset; return null if missing or not a BTC loan. Take the latest snapshot with btcLoan.Snapshots.MaxBy(s => s.EffectiveDate). Return null if no snapshot exists (auto-seeding should prevent this, but guard anyway). Map the snapshot fields into a LoanStateDTO with AssetId = entity id and AssetName = asset.Name.
  </action>
  <acceptance_criteria>
    - GetLoanStateTimelineQuery and GetLatestLoanStateQuery records exist with the correct IQuery signatures and an AssetId property.
    - Both handlers implement the matching IQueryHandler interfaces and load assets via IAssetRepository.GetByIdAsync(new AssetId(query.AssetId)).
    - Timeline handler orders snapshots by EffectiveDate ascending and maps to LoanStateSnapshotDTO.
    - Latest-state handler maps the MaxBy snapshot to LoanStateDTO including AssetId and AssetName.
    - `dotnet build Valt.sln` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>Both queries and handlers compile and only expose data for the requested BTC loan asset.</done>
</task>

<task type="auto">
  <name>Task 3: Add query handler tests</name>
  <files>
    tests/Valt.Tests/Application/Assets/Queries/GetLoanStateTimelineHandlerTests.cs
    tests/Valt.Tests/Application/Assets/Queries/GetLatestLoanStateHandlerTests.cs
  </files>
  <read_first>
    - tests/Valt.Tests/Application/Assets/Queries/GetBtcLoansDashboardHandlerTests.cs
    - tests/Valt.Tests/Builders/AssetBuilder.cs
    - tests/Valt.Tests/DatabaseTest.cs
    - src/Valt.App/Modules/Assets/Queries/GetLoanStateTimeline/GetLoanStateTimelineHandler.cs
    - src/Valt.App/Modules/Assets/Queries/GetLatestLoanState/GetLatestLoanStateHandler.cs
  </read_first>
  <action>
    Create the two test fixtures inheriting DatabaseTest:

    1. GetLoanStateTimelineHandlerTests: instantiate handler with _assetRepository. Save a BTC loan with snapshots added out of chronological order (e.g., 2025-06-01 and 2025-03-01). Assert the result count equals the number of snapshots and the EffectiveDate values are in ascending order. Assert a non-existent asset returns an empty list and a stock asset returns an empty list.

    2. GetLatestLoanStateHandlerTests: instantiate handler with _assetRepository. Save a BTC loan with multiple snapshots. Assert the returned LoanStateDTO.AssetId/AssetName match the saved asset and the fields (CurrentTotalDebt, CollateralSats, EffectiveDate, Note) come from the snapshot with the latest EffectiveDate. Assert a non-existent asset returns null and a non-BTC asset returns null.
  </action>
  <acceptance_criteria>
    - Both test files exist at the exact paths and inherit DatabaseTest.
    - Timeline test asserts chronological order and empty results for invalid assets.
    - Latest-state test asserts correct latest-snapshot fields and asset metadata.
    - `dotnet test --filter "FullyQualifiedName~GetLoanStateTimelineHandlerTests|GetLatestLoanStateHandlerTests" -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~GetLoanStateTimelineHandlerTests|GetLatestLoanStateHandlerTests" -x</automated>
  </verify>
  <done>Both query test classes pass, covering QUERY-01 and QUERY-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query input -> Application handler | Caller-supplied AssetId crosses here; handler must scope results to that asset. |
| Handler -> Repository | Only the requested asset is loaded; no broad reads of other loans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | GetLoanStateTimelineHandler / GetLatestLoanStateHandler | mitigate | Load only the asset identified by query.AssetId; return empty/null if the asset is missing or not a BTC loan. |
| T-07-06 | Information Disclosure | DTO projection | mitigate | Project only snapshot fields into dedicated DTOs; do not expose internal setup-only data beyond what LoanStateSnapshot already contains. |
| T-07-07 | Tampering | Asset ID in query | mitigate | Treat AssetId as opaque; pass it through AssetId constructor and repository lookup; do not use it in dynamic SQL or file paths. |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~GetLoanStateTimelineHandlerTests|GetLatestLoanStateHandlerTests" -x` passes.
- `dotnet build Valt.sln` succeeds with 0 errors.
</verification>

<success_criteria>
- Timeline query returns snapshots in ascending EffectiveDate order for a BTC loan.
- Latest-state query returns the latest snapshot plus asset ID/name.
- Queries return empty/null for non-existent or non-BTC assets.
- All new query tests pass.
</success_criteria>

<output>
Create `.planning/phases/07-commands-queries/07-02-SUMMARY.md` when done.
</output>
