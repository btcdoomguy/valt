---
phase: 45-simulator-modal-ui-inputs-results-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - tests/Valt.Tests/UI/Screens/BtcLoanSimulatorViewModelTests.cs
autonomous: false
requirements:
  - SIM-08
  - SIM-09
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 11000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Unit tests assert the effective fee-inclusive APR (SIM-08) and distance to liquidation (SIM-09) are formatted correctly."
    - "Distance-to-liquidation color is red when current BTC price is at or below the liquidation price and green otherwise."
    - "Live recalculation continues to work across currency changes, interest-mode toggles, and invalid inputs."
    - "A human verifies the modal visually before the phase is considered complete."
  artifacts:
    - path: tests/Valt.Tests/UI/Screens/BtcLoanSimulatorViewModelTests.cs
      provides: Full SIM-08/SIM-09 and edge-case coverage.
  key_links:
    - from: BtcLoanSimulatorViewModel.Recalculate
      to: BtcLoanSimulationCalculator.Calculate
      via: the same input construction used in 45-01; tests now assert APR and distance outputs
    - from: BtcLoanSimulatorViewModel.DistanceToLiquidationColor
      to: StringToColorBrushConverter
      via: the Foreground binding on the distance TextBlock
---

<objective>
Close the Phase 45 requirement gap by adding automated coverage for effective APR and distance-to-liquidation, plus edge-case live-recalc behavior, then obtain human sign-off on the visual/interactive modal.

Purpose: Ensure the two remaining simulator result behaviors are correct and the UI matches the approved design contract.
Output: Passing tests for SIM-08 and SIM-09 and an approved human verification checkpoint.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-simulator-modal-ui-inputs-results-panel/45-UI-SPEC.md
@.planning/phases/45-simulator-modal-ui-inputs-results-panel/45-RESEARCH.md
@.planning/phases/45-simulator-modal-ui-inputs-results-panel/45-VALIDATION.md
@.planning/phases/45-simulator-modal-ui-inputs-results-panel/45-01-PLAN.md
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Converters/StringToColorBrushConverter.cs
@/home/vmabellini/RiderProjects/valt/src/Valt.Core/Modules/Assets/Simulation/BtcLoanSimulationResult.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tests: Effective APR, distance to liquidation, color, and edge cases</name>
  <files>tests/Valt.Tests/UI/Screens/BtcLoanSimulatorViewModelTests.cs</files>
  <behavior>
    - Effective APR: for a known simple loan with no fees and a 365-day term, the VM exposes "12.00%" when the input APR is 12%.
    - Distance: when the live BTC price is $100,000 and the liquidation price is $80,000, the distance string shows a positive percentage.
    - Distance color: when the current BTC price is below the liquidation price, DistanceToLiquidationColor is the red hex string; when above, it is green.
    - Currency change: changing SelectedCurrency triggers a fresh Recalculate and updates CurrencyCode/CurrencySymbol.
    - Interest-mode toggle: switching IsSimple from true to false changes the computed TotalRepayFiat (compound produces higher interest for multi-day loans per D-01).
    - Invalid inputs: clearing collateral, amount, or setting end date <= start date causes HasResults to become false.
  </behavior>
  <action>
    Extend BtcLoanSimulatorViewModelTests.cs with tests that lock SIM-08 and SIM-09 and harden the live-recalc behavior.

    1. Effective APR test (SIM-08): set a 365-day loan with principal 25,000, APR 12%, no fees, simple mode, and assert that EffectiveAprText equals "12.00%" (the fee-inclusive effective APR rounded to 2 dp per D-04/D-05, which equals the nominal APR when fees are zero).
    2. Distance test (SIM-09): set collateral to 1 BTC (100,000,000 sats), liquidation LTV to 80%, amount 25,000, and a live BTC price of 100,000 USD. Assert DistanceToLiquidation is a non-empty signed percentage matching ((100_000 - 31_250) / 31_250) formatted with the current culture.
    3. Distance color test (SIM-09): set live BTC price below the liquidation price and assert DistanceToLiquidationColor is the red hex string (#F44336); set it above and assert green (#4CAF50).
    4. Currency recalc test: change SelectedCurrency from USD to BRL while RatesState.FiatRates["BRL"] is set, and assert CurrencyCode and CurrencySymbol update and TotalRepayFiat is recomputed.
    5. Interest-mode toggle test: set a 90-day loan and toggle IsSimple to false; assert TotalRepayFiat changes and is different from the simple-mode value (compound must produce strictly higher interest when days > 1 per D-01).
    6. Invalid-input tests: set CollateralBtcValue to BtcValue.Empty, AmountTakenFiatValue to FiatValue.Empty, end date <= start date, and LTV outside 0-100; assert HasResults becomes false after each.

    Run the targeted test filter after each assertion group and confirm all new tests pass.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~BtcLoanSimulatorViewModelTests"</automated>
  </verify>
  <done>
    All SIM-08 and SIM-09 behaviors plus edge cases are covered by passing unit tests.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human visual sign-off on the BTC Loan Simulator modal</name>
  <what-built>The BTC Loan Simulator modal with the full inputs/results panel from 45-01, plus automated tests for effective APR and distance to liquidation.</what-built>
  <how-to-verify>
    1. Build and run the application: `dotnet run --project src/Valt.UI/Valt.UI.csproj`.
    2. Open any local database and wait for a live BTC price to load in the top bar.
    3. Click Tools → BTC Loan Simulator.
    4. In the modal, enter: Collateral = 1 BTC, Amount taken = $25,000, Liquidation LTV = 80%, Start date = today, Interest rate (APR) = 12%, Fees = $100, End date = today + 30 days.
    5. Confirm the right panel shows Total to repay, Principal, Interest, Fees, Liquidation BTC price, Effective APR, and Distance to liquidation.
    6. Switch Interest mode from Simple to Compound and confirm Total to repay increases (compound interest is higher for multi-day loans).
    7. Change the Currency dropdown and confirm all fiat values reformat and sats values update.
    8. Clear the Collateral or Amount taken fields and confirm the results panel resets to the empty state ("---").
    9. Resize the modal slightly and confirm the right panel scrolls if needed and no layout is clipped.
  </how-to-verify>
  <resume-signal>Type "approved" if the modal matches the 45-UI-SPEC, or describe any layout, recalc, or wording issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions → Production code | Tests validate VM formatting against Core calculator outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-04 | Information Disclosure | Test fixture exposes sample loan values | low | accept | Test data is synthetic and contains no secrets. |
| T-45-05 | Tampering | Test expectation drift if calculator rounding changes | low | mitigate | Tests assert formatted strings derived from the calculator's already parity-tested output; if they break, the discrepancy is surfaced immediately. |
| T-45-SC | Tampering | Package installs | n/a | n/a | No new packages; not applicable. |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~BtcLoanSimulatorViewModelTests"` passes all SIM-08/SIM-09 and edge-case tests.
- `dotnet build Valt.sln` succeeds.
- Human verification checkpoint is approved.
</verification>

<success_criteria>
- Effective APR and distance to liquidation are verified by automated tests.
- Distance-to-liquidation color switches between red and green correctly.
- Invalid inputs, currency changes, and interest-mode toggles clear or update results as expected.
- A human confirms the modal matches the approved 45-UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/45-simulator-modal-ui-inputs-results-panel/45-02-SUMMARY.md` when done.
</output>
