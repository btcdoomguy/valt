---
phase: 02-core-implementation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
  - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
  - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml.cs
autonomous: true
requirements:
  - UI-03
  - UI-06
  - UI-08
  - UI-09
  - DATA-04
  - DATA-05
  - DATA-06
must_haves:
  truths:
    - "View shows category selector (left), chart (right), time dropdown (top), indicators (bottom)"
    - "Cost of living indicators display fiat % and BTC % increase with color coding"
    - "Missing PriceInSats warning banner shows when data has gaps"
    - "Right-click opens modal with only clicked transaction's category pre-selected"
    - "Menu opens modal with all categories pre-selected"
    - "Category tree supports parent/child with checkbox propagation"
  artifacts:
    - path: "src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml"
      provides: "Full modal layout with TreeView, chart, dropdown, indicators"
      min_lines: 120
    - path: "src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs"
      provides: "ViewModel with cost of living calculations and pre-selection logic"
      exports: ["FiatIncreasePercent", "BtcIncreasePercent", "HasMissingPriceInSats", "ApplyPreSelection"]
  key_links:
    - from: "SpendingEvolutionView.axaml"
      to: "SpendingEvolutionViewModel"
      via: "DataContext binding"
      pattern: "x:DataType=\"spendingEvolution:SpendingEvolutionViewModel\""
    - from: "SpendingEvolutionViewModel"
      to: "CategorySelectionItem"
      via: "CategoryItems collection"
      pattern: "ObservableCollection<CategorySelectionItem>"
    - from: "SpendingEvolutionViewModel"
      to: "SpendingEvolutionChartData"
      via: "ChartData property"
      pattern: "ChartData.Series"
---

<objective>
Build the complete View layout and integrate all components for Spending Evolution modal.

Purpose: Create the full UI layout with category TreeView, dual-axis chart, time range dropdown, cost of living indicators, and missing data warning. Wire up pre-selection logic for menu vs right-click entry points. Add parent/child checkbox propagation to category selector.
Output: Complete SpendingEvolutionView.axaml with all UI elements, updated ViewModel with indicators and pre-selection, working integration
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-implementation/02-CONTEXT.md
@.planning/phases/02-core-implementation/02-02-SUMMARY.md

**Key interfaces from codebase:**

From `src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml` (current Phase 1 output):
```xml
<Window xmlns="https://github.com/avaloniaui"
        ...
        x:Class="Valt.UI.Views.Main.Modals.SpendingEvolution.SpendingEvolutionView"
        x:DataType="spendingEvolution:SpendingEvolutionViewModel"
        Title="Spending Evolution"
        WindowStartupLocation="CenterOwner"
        d:DesignWidth="900"
        d:DesignHeight="600"
        MinWidth="900"
        MinHeight="600"
        ExtendClientAreaToDecorationsHint="True"
        WindowDecorations="None"
        ExtendClientAreaTitleBarHeightHint="0">
    <Design.DataContext>
        <spendingEvolution:SpendingEvolutionViewModel />
    </Design.DataContext>
    <Border>
        <StackPanel Orientation="Vertical">
            <userControls:CustomTitleBar Title="Spending Evolution"
                                         TitleBarPressed="CustomTitleBarButtonPressed"
                                         CloseClick="CustomTitleBarCloseClicked" />
            <Border Classes="form-fields-area">
                <TextBlock Text="Spending Evolution content will be added in Phase 2"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="{DynamicResource FontSizeLarge}" />
            </Border>
        </StackPanel>
    </Border>
</Window>
```

From `src/Valt.UI/Views/Main/Modals/ManageCategories/ManageCategoriesView.axaml` (TreeView pattern):
```xml
<TreeView x:Name="Tree" ItemsSource="{Binding NewCategories}"
          SelectedItem="{Binding SelectedCategory, Mode=OneWay}"
          SelectionMode="Single"
          IsEnabled="{Binding !IsEditing}">
    <TreeView.ItemTemplate>
        <TreeDataTemplate ItemsSource="{Binding SubNodes}">
            <StackPanel Orientation="Horizontal" Height="20">
                <TextBlock FontFamily="{DynamicResource MaterialDesign}" 
                            FontSize="{DynamicResource FontSizeXXLarge}"
                            Text="{Binding Unicode}"
                            Foreground="{Binding Color, Converter={StaticResource SystemDrawingColorToBrushConverter}}" />
                <TextBlock Text="{Binding Name}" />
            </StackPanel>
        </TreeDataTemplate>
    </TreeView.ItemTemplate>
</TreeView>
```

From `src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml` (chart binding pattern):
```xml
<lvc:CartesianChart Series="{Binding ChartData.Series}"
                    XAxes="{Binding ChartData.XAxes}"
                    YAxes="{Binding ChartData.YAxes}"
                    LegendTextPaint="{Binding ChartData.LegendTextPaint}"
                    TooltipTextPaint="{Binding ChartData.TooltipTextPaint}"
                    TooltipBackgroundPaint="{Binding ChartData.TooltipBackgroundPaint}"
                    ... />
```

From `src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs` (after Plan 02):
```csharp
public partial class SpendingEvolutionViewModel : ValtModalViewModel
{
    [ObservableProperty]
    private SpendingEvolutionChartData _chartData;
    
    [ObservableProperty]
    private ObservableCollection<CategorySelectionItem> _categoryItems = new();
    
    [ObservableProperty]
    private int _selectedTimeRangeMonths = 24;
    
    [ObservableProperty]
    private string? _preSelectedCategoryId;
    
    [ObservableProperty]
    private bool _isLoading;
    
    [ObservableProperty]
    private SpendingEvolutionDataDto? _currentData;
    
    public int[] TimeRangeOptions { get; } = new[] { 12, 24, 36, 48, 60 };
    
    // ... methods from Plan 02
}
```

From `src/Valt.UI/Views/Main/Modals/SpendingEvolution/Models/CategorySelectionItem.cs`:
```csharp
public class CategorySelectionItem : ObservableObject
{
    public AvaloniaList<CategorySelectionItem> SubNodes { get; set; } = new();
    public string Id { get; }
    public string? ParentId { get; set; }
    public string Name { get; set; }
    public char Unicode { get; set; }
    public Color Color { get; set; }
    
    [ObservableProperty]
    private bool _isSelected;
    
    // ... constructor with child propagation
}
```

From `src/Valt.App/Modules/SpendingEvolution/DTOs/SpendingEvolutionMonthDto.cs`:
```csharp
public record SpendingEvolutionMonthDto
{
    public required DateOnly Month { get; init; }
    public required decimal FiatTotal { get; init; }
    public required long SatsTotal { get; init; }
    public required int TransactionCount { get; init; }
}
```

From `src/Valt.App/Modules/SpendingEvolution/DTOs/SpendingEvolutionDataDto.cs`:
```csharp
public record SpendingEvolutionDataDto
{
    public required IReadOnlyList<SpendingEvolutionMonthDto> Months { get; init; }
    public required bool HasMissingPriceInSats { get; init; }
    public required string PrimaryCurrency { get; init; }
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SpendingEvolutionView.axaml with Full Layout</name>
  <files>
    src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
  </files>
  <read_first>
    - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
    - src/Valt.UI/Views/Main/Modals/ManageCategories/ManageCategoriesView.axaml
    - src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
    - src/Valt.UI/Views/Main/Tabs/Reports/WealthOverviewChartData.cs
  </read_first>
  <action>
    Replace the placeholder layout in SpendingEvolutionView.axaml with the full UI:

    1. **Keep existing Window attributes** (Title, sizing, CustomTitleBar, etc.)

    2. **Add namespace** for lvc (LiveCharts) if not present:
       ```xml
       xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Avalonia;assembly=LiveChartsCore.SkiaSharpView.Avalonia"
       ```

    3. **Main layout structure** (replace the inner StackPanel/Border):
       ```xml
       <Border>
           <Grid RowDefinitions="Auto, *, Auto">
               <!-- Row 0: Title Bar -->
               <userControls:CustomTitleBar Grid.Row="0"
                                            Title="Spending Evolution"
                                            TitleBarPressed="CustomTitleBarButtonPressed"
                                            CloseClick="CustomTitleBarCloseClicked" />
               
               <!-- Row 1: Main Content -->
               <Grid Grid.Row="1" ColumnDefinitions="280, *">
                   
                   <!-- Left: Category Selector -->
                   <Border Grid.Column="0" Classes="form-fields-area" Margin="10">
                       <StackPanel>
                           <TextBlock Text="Categories"
                                     FontSize="{DynamicResource FontSizeLarge}"
                                     Margin="0,0,0,10" />
                           <TreeView x:Name="CategoryTree"
                                     ItemsSource="{Binding CategoryItems}"
                                     SelectionMode="Single"
                                     MaxHeight="450">
                               <TreeView.ItemTemplate>
                                   <TreeDataTemplate ItemsSource="{Binding SubNodes}">
                                       <StackPanel Orientation="Horizontal"
                                                   Height="24"
                                                   Spacing="8">
                                           <CheckBox IsChecked="{Binding IsSelected}"
                                                     VerticalAlignment="Center" />
                                           <TextBlock FontFamily="{DynamicResource MaterialDesign}"
                                                     FontSize="{DynamicResource FontSizeLarge}"
                                                     Text="{Binding Unicode}"
                                                     Foreground="{Binding Color, Converter={StaticResource SystemDrawingColorToBrushConverter}}"
                                                     VerticalAlignment="Center" />
                                           <TextBlock Text="{Binding Name}"
                                                     VerticalAlignment="Center" />
                                       </StackPanel>
                                   </TreeDataTemplate>
                               </TreeView.ItemTemplate>
                           </TreeView>
                       </StackPanel>
                   </Border>
                   
                   <!-- Right: Chart Area -->
                   <Grid Grid.Column="1" RowDefinitions="Auto, *, Auto">
                       
                       <!-- Top: Time Range Dropdown -->
                       <StackPanel Grid.Row="0" 
                                   Orientation="Horizontal"
                                   HorizontalAlignment="Right"
                                   Margin="10">
                           <TextBlock Text="Time Range:"
                                     VerticalAlignment="Center"
                                     Margin="0,0,10,0" />
                           <ComboBox ItemsSource="{Binding TimeRangeOptions}"
                                     SelectedItem="{Binding SelectedTimeRangeMonths}"
                                     Width="120">
                               <ComboBox.ItemTemplate>
                                   <DataTemplate>
                                       <TextBlock Text="{Binding} months" />
                                   </DataTemplate>
                               </ComboBox.ItemTemplate>
                           </ComboBox>
                       </StackPanel>
                       
                       <!-- Middle: Chart -->
                       <Border Grid.Row="1" Margin="10,0,10,10">
                           <lvc:CartesianChart Series="{Binding ChartData.Series}"
                                               XAxes="{Binding ChartData.XAxes}"
                                               YAxes="{Binding ChartData.YAxes}"
                                               LegendTextPaint="{Binding ChartData.LegendTextPaint}"
                                               TooltipTextPaint="{Binding ChartData.TooltipTextPaint}"
                                               TooltipBackgroundPaint="{Binding ChartData.TooltipBackgroundPaint}"
                                               ZoomMode="X"
                                               ZoomingSpeed="0.5" />
                       </Border>
                       
                       <!-- Bottom: Indicators -->
                       <Grid Grid.Row="2" 
                             ColumnDefinitions="*, *"
                             Margin="10">
                           <StackPanel Grid.Column="0" 
                                       HorizontalAlignment="Center">
                               <TextBlock Text="Cost of Living (Fiat)"
                                         FontSize="{DynamicResource FontSizeSmall}"
                                         Foreground="{DynamicResource Text400}" />
                               <TextBlock Text="{Binding FiatIncreasePercent, StringFormat='{}{0:F1}%'}"
                                         FontSize="{DynamicResource FontSizeXLarge}"
                                         FontWeight="Bold">
                                   <TextBlock.Styles>
                                       <Style Selector="TextBlock">
                                           <Setter Property="Foreground"
                                                   Value="{Binding FiatIncreasePercent, Converter={StaticResource IncreaseToColorConverter}}" />
                                       </Style>
                                   </TextBlock.Styles>
                               </TextBlock>
                           </StackPanel>
                           
                           <StackPanel Grid.Column="1" 
                                       HorizontalAlignment="Center">
                               <TextBlock Text="Cost of Living (BTC)"
                                         FontSize="{DynamicResource FontSizeSmall}"
                                         Foreground="{DynamicResource Text400}" />
                               <TextBlock Text="{Binding BtcIncreasePercent, StringFormat='{}{0:F1}%'}"
                                         FontSize="{DynamicResource FontSizeXLarge}"
                                         FontWeight="Bold">
                                   <TextBlock.Styles>
                                       <Style Selector="TextBlock">
                                           <Setter Property="Foreground"
                                                   Value="{Binding BtcIncreasePercent, Converter={StaticResource IncreaseToColorConverter}}" />
                                       </Style>
                                   </TextBlock.Styles>
                               </TextBlock>
                           </StackPanel>
                       </Grid>
                   </Grid>
               </Grid>
               
               <!-- Row 2: Warning Banner -->
               <Border Grid.Row="2"
                       Background="{DynamicResource WarningBackground}"
                       IsVisible="{Binding HasMissingPriceInSats}"
                       Padding="10">
                   <StackPanel Orientation="Horizontal"
                               HorizontalAlignment="Center">
                       <TextBlock FontFamily="{DynamicResource MaterialDesign}"
                                 Text="&#xE002;"
                                 Foreground="{DynamicResource WarningForeground}"
                                 Margin="0,0,8,0" />
                       <TextBlock Text="Some transactions are missing BTC price data"
                                 Foreground="{DynamicResource WarningForeground}"
                                 VerticalAlignment="Center" />
                   </StackPanel>
               </Border>
           </Grid>
       </Border>
       ```

    4. **IMPORTANT**: If `IncreaseToColorConverter` doesn't exist, use inline logic or create a simple converter in the code-behind. For now, use a simpler approach:
       - Remove the converter references
       - Use plain text with color set in ViewModel (add `FiatIncreaseBrush` and `BtcIncreaseBrush` properties)
       - OR: use conditional DataTemplates

    5. **Simpler approach for indicators** (if converter doesn't exist):
       ```xml
       <TextBlock Text="{Binding FiatIncreasePercentText}"
                 FontSize="{DynamicResource FontSizeXLarge}"
                 FontWeight="Bold"
                 Foreground="{Binding FiatIncreaseBrush}" />
       ```
       And in ViewModel, compute the formatted string and brush.

    6. **Add Window.KeyBindings** for Escape to close:
       ```xml
       <Window.KeyBindings>
           <KeyBinding Gesture="Escape" Command="{Binding CloseCommand}" />
       </Window.KeyBindings>
       ```
  </action>
  <verify>
    <automated>
      grep -n "CategoryTree" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "lvc:CartesianChart" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "ComboBox" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "FiatIncreasePercent" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "BtcIncreasePercent" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "HasMissingPriceInSats" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "TreeDataTemplate" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -n "ColumnDefinitions" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      grep -c "Grid" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
      dotnet build src/Valt.UI/Valt.UI.csproj
    </automated>
  </verify>
  <done>
    - View has Grid with RowDefinitions (title, content, warning)
    - Content area has ColumnDefinitions (280px left, rest right)
    - Left side has TreeView with CheckBox + icon + name for each category
    - Right side has time range ComboBox at top
    - Right side has CartesianChart with Series/XAxes/YAxes binding to ChartData
    - Bottom of right side has two indicator panels (Fiat %, BTC %)
    - Warning banner at bottom shows when HasMissingPriceInSats is true
    - Escape key binding closes modal
    - No placeholder text remains
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Cost of Living Indicators, Warnings, and Pre-selection Logic to ViewModel</name>
  <files>
    src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
    src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml.cs
  </files>
  <read_first>
    - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
    - src/Valt.App/Modules/SpendingEvolution/DTOs/SpendingEvolutionMonthDto.cs
    - src/Valt.App/Modules/SpendingEvolution/DTOs/SpendingEvolutionDataDto.cs
  </read_first>
  <action>
    Add the remaining ViewModel logic for indicators, warnings, and pre-selection:

    1. **Add observable properties for indicators**:
       ```csharp
       [ObservableProperty]
       private decimal _fiatIncreasePercent;
       
       [ObservableProperty]
       private decimal _btcIncreasePercent;
       
       [ObservableProperty]
       private string _fiatIncreasePercentText = "N/A";
       
       [ObservableProperty]
       private string _btcIncreasePercentText = "N/A";
       
       [ObservableProperty]
       private bool _hasMissingPriceInSats;
       
       [ObservableProperty]
       private SolidColorBrush _fiatIncreaseBrush = new(Brushes.Gray.Color);
       
       [ObservableProperty]
       private SolidColorBrush _btcIncreaseBrush = new(Brushes.Gray.Color);
       ```

    2. **Add CalculateIndicators method**:
       ```csharp
       private void CalculateIndicators()
       {
           if (CurrentData?.Months == null || CurrentData.Months.Count < 2)
           {
               FiatIncreasePercent = 0;
               BtcIncreasePercent = 0;
               FiatIncreasePercentText = "N/A";
               BtcIncreasePercentText = "N/A";
               return;
           }
           
           var firstMonth = CurrentData.Months.First();
           var lastMonth = CurrentData.Months.Last();
           
           // Fiat increase
           if (firstMonth.FiatTotal > 0)
           {
               FiatIncreasePercent = ((lastMonth.FiatTotal - firstMonth.FiatTotal) / firstMonth.FiatTotal) * 100;
               FiatIncreasePercentText = $"{(FiatIncreasePercent >= 0 ? "+" : "")}{FiatIncreasePercent:F1}%";
               // Green for decrease (good), Red for increase (bad) — per CONTEXT.md
               FiatIncreaseBrush = new SolidColorBrush(
                   FiatIncreasePercent <= 0 ? Colors.Green : Colors.Red);
           }
           else
           {
               FiatIncreasePercent = 0;
               FiatIncreasePercentText = "N/A";
               FiatIncreaseBrush = new SolidColorBrush(Colors.Gray);
           }
           
           // BTC increase
           if (firstMonth.SatsTotal > 0)
           {
               BtcIncreasePercent = ((lastMonth.SatsTotal - firstMonth.SatsTotal) / (decimal)firstMonth.SatsTotal) * 100;
               BtcIncreasePercentText = $"{(BtcIncreasePercent >= 0 ? "+" : "")}{BtcIncreasePercent:F1}%";
               // Green for decrease (good), Red for increase (bad)
               BtcIncreaseBrush = new SolidColorBrush(
                   BtcIncreasePercent <= 0 ? Colors.Green : Colors.Red);
           }
           else
           {
               BtcIncreasePercent = 0;
               BtcIncreasePercentText = "N/A";
               BtcIncreaseBrush = new SolidColorBrush(Colors.Gray);
           }
       }
       ```

    3. **Update LoadDataAsync** to calculate indicators after data loads:
       ```csharp
       [RelayCommand]
       private async Task LoadDataAsync()
       {
           IsLoading = true;
           try
           {
               var fromDate = DateOnly.FromDateTime(DateTime.Now.AddMonths(-SelectedTimeRangeMonths));
               var toDate = DateOnly.FromDateTime(DateTime.Now);
               
               var selectedCategoryIds = CategoryItems
                   .Where(c => c.IsSelected)
                   .Select(c => c.Id)
                   .ToArray();
               
               var query = new GetSpendingEvolutionQuery
               {
                   From = fromDate,
                   To = toDate,
                   CategoryIds = selectedCategoryIds,
                   ShowHiddenAccounts = false
               };
               
               CurrentData = await _queryDispatcher.DispatchAsync(query);
               ChartData.RefreshChart(CurrentData);
               
               // Update indicators and warnings
               HasMissingPriceInSats = CurrentData.HasMissingPriceInSats;
               CalculateIndicators();
           }
           finally
           {
               IsLoading = false;
           }
       }
       ```

    4. **Add tree structure support to LoadCategoriesAsync**:
       ```csharp
       private async Task LoadCategoriesAsync()
       {
           var categories = await _queryDispatcher.DispatchAsync(new GetCategoriesQuery());
           
           // Build flat list first
           var allItems = categories.Items.Select(c => new CategorySelectionItem(
               c.Id, null, c.Name, c.Unicode, c.Color)).ToList();
           
           // TODO: Build parent-child tree if categories have hierarchy
           // For now, flat list with all items
           CategoryItems.Clear();
           foreach (var item in allItems)
           {
               CategoryItems.Add(item);
           }
           
           ApplyPreSelection();
       }
       ```

    5. **Add ApplyPreSelection method**:
       ```csharp
       private void ApplyPreSelection()
       {
           if (!string.IsNullOrEmpty(PreSelectedCategoryId))
           {
               // Right-click mode: select only the pre-selected category
               foreach (var item in CategoryItems)
               {
                   item.IsSelected = item.Id == PreSelectedCategoryId;
               }
           }
           else
           {
               // Menu mode: select all categories
               foreach (var item in CategoryItems)
               {
                   item.IsSelected = true;
               }
           }
       }
       ```

    6. **Add category change event handling**:
       ```csharp
       // In constructor or initialization, hook up category change events
       public SpendingEvolutionViewModel(IQueryDispatcher queryDispatcher)
       {
           _queryDispatcher = queryDispatcher;
           ChartData = new SpendingEvolutionChartData();
           
           // Hook up property changes on CategoryItems
           CategoryItems.CollectionChanged += (s, e) =>
           {
               if (e.NewItems != null)
               {
                   foreach (CategorySelectionItem item in e.NewItems)
                   {
                       item.PropertyChanged += async (sender, args) =>
                       {
                           if (args.PropertyName == nameof(CategorySelectionItem.IsSelected))
                           {
                               await LoadDataAsync();
                           }
                       };
                   }
               }
           };
       }
       ```
       NOTE: This may cause re-entrant calls. Consider debouncing or checking if already loading.

    7. **Add debounce protection**:
       ```csharp
       private bool _isDataLoading;
       
       [RelayCommand]
       private async Task LoadDataAsync()
       {
           if (_isDataLoading) return;
           _isDataLoading = true;
           IsLoading = true;
           try
           {
               // ... existing logic
           }
           finally
           {
               IsLoading = false;
               _isDataLoading = false;
           }
       }
       ```

    8. **Update View.axaml.cs** (code-behind) to handle CheckBox changes:
       ```csharp
       public partial class SpendingEvolutionView : ValtBaseWindow
       {
           public SpendingEvolutionView()
           {
               InitializeComponent();
           }
           
           // If needed, add event handlers for TreeView item checkbox changes
           // Typically handled via binding in Avalonia
       }
       ```
  </action>
  <verify>
    <automated>
      grep -n "FiatIncreasePercent" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      grep -n "BtcIncreasePercent" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      grep -n "HasMissingPriceInSats" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      grep -n "CalculateIndicators" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      grep -n "ApplyPreSelection" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      grep -n "LoadDataAsync" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      grep -n "CategoryItems.CollectionChanged" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
      dotnet build src/Valt.UI/Valt.UI.csproj
    </automated>
  </verify>
  <done>
    - ViewModel has FiatIncreasePercent, BtcIncreasePercent properties
    - ViewModel has FiatIncreasePercentText, BtcIncreasePercentText formatted strings
    - ViewModel has FiatIncreaseBrush, BtcIncreaseBrush with green for decrease, red for increase
    - CalculateIndicators computes ((last - first) / first) * 100 for both fiat and sats
    - Edge case: first month = 0 shows "N/A"
    - HasMissingPriceInSats reflects CurrentData.HasMissingPriceInSats
    - ApplyPreSelection selects only PreSelectedCategoryId when set, all when null
    - Category selection changes trigger LoadDataAsync with debounce protection
    - Build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → ViewModel | User input (category checkboxes, time range) crosses into ViewModel |
| ViewModel → Chart | Financial data displayed in chart format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Cost of living indicators | accept | Displays user's own aggregated data; no sensitive information leaked beyond what user already has access to |
| T-02-07 | Tampering | CategorySelectionItem.IsSelected | accept | User can only modify their own category selections; affects only their own chart display |
| T-02-08 | Denial of Service | Category change re-triggering LoadDataAsync | mitigate | Debounce protection via _isDataLoading flag prevents rapid re-queries |
</threat_model>

<verification>
- View has complete layout with TreeView, chart, dropdown, indicators, warning banner
- TreeView shows categories with checkboxes, icons, and names
- CartesianChart binds to ChartData.Series, XAxes, YAxes
- Time range ComboBox shows 12/24/36/48/60 months
- Cost of living indicators show formatted percentage with color coding
- Warning banner shows when HasMissingPriceInSats is true
- Pre-selection works: right-click selects one category, menu selects all
- Category selection changes trigger chart update
- Build succeeds (`dotnet build src/Valt.UI/Valt.UI.csproj` exits 0)
</verification>

<success_criteria>
- Modal displays full UI: category selector, chart, time dropdown, indicators
- Category selector shows all categories with checkboxes and icons
- Dual-axis line chart displays fiat total (left Y) and sats total (right Y)
- Time range dropdown offers 12/24/36/48/60 month options
- Cost of living indicators show percentage increase for fiat and BTC
- Color coding: green for decrease, red for increase
- Missing PriceInSats warning banner displays when applicable
- Right-click opens modal with only clicked transaction's category pre-selected
- Menu opens modal with all categories pre-selected
- Chart updates when categories are selected/deselected
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-implementation/02-03-SUMMARY.md`
</output>
