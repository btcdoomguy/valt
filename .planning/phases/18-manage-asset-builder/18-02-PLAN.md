---
phase: 18-manage-asset-builder
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/Valt.UI/Extensions.cs
  - src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs
  - tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs
  - tests/Valt.Tests/UI/Services/AssetFormBuilderTests.cs
autonomous: true
requirements:
  - VM-SVC-02

must_haves:
  truths:
    - `IAssetFormBuilder` is registered as a singleton in the UI DI container.
    - `ManageAssetViewModel` delegates create, edit, and load construction to the builder.
    - `ManageAssetViewModel` remains functionally identical and its parameterless design-time constructor still works.
    - Unit tests cover the builder for all 5 command/DTO families with focused coverage for BTC loan, leveraged, and real estate.
  artifacts:
    - path: src/Valt.UI/Extensions.cs
      provides: DI registration for IAssetFormBuilder
      contains: "AddSingleton<IAssetFormBuilder, AssetFormBuilder>"
    - path: src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs
      provides: wiring to builder
      contains: "_assetFormBuilder"
    - path: tests/Valt.Tests/UI/Services/AssetFormBuilderTests.cs
      provides: builder unit tests
      contains: "[TestFixture]"
    - path: tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs
      provides: updated ViewModel test helper
      contains: "AssetFormBuilder"
  key_links:
    - from: ManageAssetViewModel.CreateNewAssetAsync
      to: IAssetFormBuilder.BuildCreateCommandAsync
      via: AssetFormSnapshot built from current observable properties
    - from: ManageAssetViewModel.EditExistingAssetAsync
      to: IAssetFormBuilder.BuildEditDetailsAsync
      via: AssetFormSnapshot built from current observable properties
    - from: ManageAssetViewModel.OnBindParameterAsync
      to: IAssetFormBuilder.LoadFromDto
      via: AssetFormValues; SelectedAssetType applied last
---

<objective>
Wire the new `IAssetFormBuilder` into the DI container and `ManageAssetViewModel`, then add/update unit tests so the refactor is fully covered.

Purpose: Complete the extraction and prove that `ManageAssetViewModel` remains functionally identical.
Output: Updated DI registration, refactored ViewModel, updated ViewModel tests, and new `AssetFormBuilderTests`.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-manage-asset-builder/18-RESEARCH.md
@.planning/phases/18-manage-asset-builder/18-PATTERNS.md
@src/Valt.UI/Services/IAssetFormBuilder.cs
@src/Valt.UI/Services/AssetFormBuilder.cs
@src/Valt.UI/Services/Exceptions/AssetFormBuildException.cs
@src/Valt.UI/Extensions.cs
@src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs
@tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs
@tests/Valt.Tests/UI/Services/TransactionDetailsBuilderTests.cs
@src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
@src/Valt.App/Modules/Assets/DTOs/AssetDetailsInputDTO.cs
@src/Valt.App/Modules/Assets/Commands/CreateBasicAsset/CreateBasicAssetCommand.cs
@src/Valt.App/Modules/Assets/Commands/CreateRealEstateAsset/CreateRealEstateAssetCommand.cs
@src/Valt.App/Modules/Assets/Commands/CreateLeveragedPosition/CreateLeveragedPositionCommand.cs
@src/Valt.App/Modules/Assets/Commands/CreateBtcLoan/CreateBtcLoanCommand.cs
@src/Valt.App/Modules/Assets/Commands/CreateBtcLending/CreateBtcLendingCommand.cs
@src/Valt.Infra/Modules/Assets/PriceProviders/AssetPriceProviderSelector.cs
@src/Valt.Infra/Modules/Assets/PriceProviders/IAssetPriceProvider.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register IAssetFormBuilder in DI</name>
  <files>src/Valt.UI/Extensions.cs</files>
  <read_first>
    src/Valt.UI/Extensions.cs
    src/Valt.UI/Services/IAssetFormBuilder.cs
    src/Valt.UI/Services/AssetFormBuilder.cs
  </read_first>
  <action>
    In src/Valt.UI/Extensions.cs, add `services.AddSingleton&lt;IAssetFormBuilder, AssetFormBuilder&gt;();` on a new line immediately after the existing `services.AddSingleton&lt;ITransactionDetailsBuilder, TransactionDetailsBuilder&gt;();` registration.
    Do not change any other registrations.
  </action>
  <verify>
    <automated>dotnet build src/Valt.UI/Valt.UI.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "services.AddSingleton<IAssetFormBuilder, AssetFormBuilder>" src/Valt.UI/Extensions.cs` returns 1.
    - The new registration appears after `services.AddSingleton<ITransactionDetailsBuilder, TransactionDetailsBuilder>`.
    - `dotnet build src/Valt.UI/Valt.UI.csproj` exits 0.
  </acceptance_criteria>
  <done>Builder service is registered in the UI DI container.</done>
</task>

<task type="auto">
  <name>Task 2: Wire builder into ManageAssetViewModel</name>
  <files>src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs</files>
  <read_first>
    src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs
    src/Valt.UI/Services/IAssetFormBuilder.cs
    src/Valt.UI/Services/AssetFormBuilder.cs
    src/Valt.UI/Services/Exceptions/AssetFormBuildException.cs
    src/Valt.App/Modules/Assets/Commands/CreateBasicAsset/CreateBasicAssetCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateRealEstateAsset/CreateRealEstateAssetCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateLeveragedPosition/CreateLeveragedPositionCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateBtcLoan/CreateBtcLoanCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateBtcLending/CreateBtcLendingCommand.cs
    src/Valt.App/Modules/Assets/Commands/EditAsset/EditAssetCommand.cs
    src/Valt.App/Modules/Assets/DTOs/AssetDetailsInputDTO.cs
  </read_first>
  <action>
    Refactor src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs to use IAssetFormBuilder.

    1. Add a private readonly field `private readonly IAssetFormBuilder? _assetFormBuilder;`.
    2. Add an `IAssetFormBuilder? assetFormBuilder` parameter to the production constructor (after `IModalFactory modalFactory`) and assign it to `_assetFormBuilder`.
    3. Leave the parameterless design-time constructor exactly as it is.

    4. In `OnBindParameterAsync`, after fetching `assetDto` and setting common fields (`Name`, `SelectedCurrency`, `IncludeInNetWorth`, `Visible`), replace the entire type-specific switch with:
       `var values = _assetFormBuilder!.LoadFromDto(assetDto);`
       then copy every field from `values` into the corresponding observable properties, and finally assign `SelectedAssetType = values.SelectedAssetType;` LAST.
       Keep the comment explaining that the type is set last so date pickers are populated first.

    5. Replace `CreateNewAssetAsync` body with:
       - Build an `AssetFormSnapshot` from the current observable properties (`Name`, `SelectedAssetType`, `SelectedCurrency`, `IncludeInNetWorth`, `Visible`, plus all type-specific fields).
       - Call `var envelope = await _assetFormBuilder!.BuildCreateCommandAsync(snapshot);`.
       - Switch on the envelope and dispatch the wrapped command using `_commandDispatcher!.DispatchAsync`:
         * `BasicAssetCommandEnvelope` → dispatch `basic.Command`
         * `RealEstateAssetCommandEnvelope` → dispatch `realEstate.Command`
         * `LeveragedPositionCommandEnvelope` → dispatch `leveraged.Command`
         * `BtcLoanCommandEnvelope` → dispatch `btcLoan.Command`
         * `BtcLendingCommandEnvelope` → dispatch `btcLending.Command`
       - Preserve the existing error handling (`result.IsFailure` → `MessageBoxHelper.ShowErrorAsync`) and `CloseDialog?.Invoke(new Response(true, result.Value!.AssetId))` behavior.
       - For the default branch throw `new AssetFormBuildException()`.

    6. Replace `EditExistingAssetAsync` body with:
       - Build an `AssetFormSnapshot` from the current observable properties.
       - Call `var details = await _assetFormBuilder!.BuildEditDetailsAsync(snapshot);`.
       - Dispatch `EditAssetCommand { AssetId = _assetId!, Name = Name, Details = details, IncludeInNetNetWorth = IncludeInNetWorth, Visible = Visible }`.
       - Preserve existing error handling and `CloseDialog?.Invoke(new Response(true, _assetId))`.

    7. Remove direct construction of `CreateBasicAssetCommand`, `CreateRealEstateAssetCommand`, `CreateLeveragedPositionCommand`, `CreateBtcLoanCommand`, `CreateBtcLendingCommand`, `BasicAssetDetailsInputDTO`, `RealEstateAssetDetailsInputDTO`, `LeveragedPositionDetailsInputDTO`, `BtcLoanDetailsInputDTO`, and `BtcLendingDetailsInputDTO` from the ViewModel.
       Keep the `using` imports for the envelope switch arms if still required.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_assetFormBuilder" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` is at least 3.
    - `grep -c "new CreateBasicAssetCommand" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new CreateRealEstateAssetCommand" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new CreateLeveragedPositionCommand" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new CreateBtcLoanCommand" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new CreateBtcLendingCommand" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new BasicAssetDetailsInputDTO" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new RealEstateAssetDetailsInputDTO" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new LeveragedPositionDetailsInputDTO" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new BtcLoanDetailsInputDTO" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - `grep -c "new BtcLendingDetailsInputDTO" src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs` returns 0.
    - The line `SelectedAssetType = ((AssetTypes)assetDto.AssetTypeId).ToString();` remains the LAST assignment inside the edit-mode branch of `OnBindParameterAsync`.
    - The parameterless constructor `public ManageAssetViewModel()` still exists and only sets `SelectedAssetType`.
    - `dotnet build Valt.sln` exits 0.
  </acceptance_criteria>
  <done>ManageAssetViewModel delegates all per-type construction to the builder and compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Update ViewModel tests and add AssetFormBuilderTests</name>
  <files>
    tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs
    tests/Valt.Tests/UI/Services/AssetFormBuilderTests.cs
  </files>
  <read_first>
    tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs
    tests/Valt.Tests/UI/Services/TransactionDetailsBuilderTests.cs
    src/Valt.UI/Services/IAssetFormBuilder.cs
    src/Valt.UI/Services/AssetFormBuilder.cs
    src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
    src/Valt.App/Modules/Assets/DTOs/AssetDetailsInputDTO.cs
    src/Valt.App/Modules/Assets/Commands/CreateBasicAsset/CreateBasicAssetCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateRealEstateAsset/CreateRealEstateAssetCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateLeveragedPosition/CreateLeveragedPositionCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateBtcLoan/CreateBtcLoanCommand.cs
    src/Valt.App/Modules/Assets/Commands/CreateBtcLending/CreateBtcLendingCommand.cs
    src/Valt.Infra/Modules/Assets/PriceProviders/AssetPriceProviderSelector.cs
    src/Valt.Infra/Modules/Assets/PriceProviders/IAssetPriceProvider.cs
  </read_first>
  <action>
    Update tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs:
    - Add `private IAssetFormBuilder _assetFormBuilder = null!;`.
    - In `SetUp`, instantiate `_assetFormBuilder = new AssetFormBuilder(_priceProviderSelector);` after `_priceProviderSelector` is created.
    - Update `CreateViewModel()` to pass `_assetFormBuilder` as the final argument to the `ManageAssetViewModel` constructor.
    - Keep all existing tests and helpers unchanged.

    Create tests/Valt.Tests/UI/Services/AssetFormBuilderTests.cs with an NUnit `[TestFixture]`:
    - `SetUp`: create `_priceProviderSelector = Substitute.For&lt;IAssetPriceProviderSelector&gt;();` and `_builder = new AssetFormBuilder(_priceProviderSelector);`.
    - Add helper methods that build `AssetFormSnapshot` instances for each asset family and `AssetDTO` instances for each asset type.
    - When price fetching is expected, configure `_priceProviderSelector.GetPriceAsync(...).Returns(new AssetPriceResult(...))`.

    Cover at least the following behaviors (test names may vary):
    - Create: basic Stock with manual price returns `BasicAssetCommandEnvelope` with correct `AssetType`, `CurrentPrice`, `PriceSource`, `AcquisitionDate`, `AcquisitionPrice`, `IncludeInNetWorth`, `Visible`.
    - Create: basic Crypto with YahooFinance fetches price and sets `CurrentPrice` to the fetched value when symbol is non-empty.
    - Create: RealEstate returns `RealEstateAssetCommandEnvelope` with `Address`, `MonthlyRentalIncome`, `AcquisitionDate`, `AcquisitionPrice` mapping.
    - Create: Leveraged collateral mode returns `LeveragedPositionCommandEnvelope` with `InputMode == 0` and `PositionSize == null`.
    - Create: Leveraged exact-position mode returns `LeveragedPositionCommandEnvelope` with `InputMode == 1` and `PositionSize == snapshot.PositionSize`.
    - Create: Leveraged with non-manual price fetches price.
    - Create: BTC loan with `UseFixedTotalDebt == true` returns `BtcLoanCommandEnvelope` with `Apr == 0m` and `FixedTotalDebt == snapshot.FixedTotalDebtFiat.Value`.
    - Create: BTC loan with `UseFixedTotalDebt == false` returns `BtcLoanCommandEnvelope` with `Apr == snapshot.AprPercentage / 100m`, fetches BTC LivePrice, and sets `CurrentBtcPrice`.
    - Create: BTC lending returns `BtcLendingCommandEnvelope` with `Apr == snapshot.LendingAprPercentage / 100m`.
    - Edit: basic returns `BasicAssetDetailsInputDTO` with fetched price.
    - Edit: RealEstate returns `RealEstateAssetDetailsInputDTO`.
    - Edit: Leveraged returns `LeveragedPositionDetailsInputDTO` with `InputMode` and `PositionSize` mapping.
    - Edit: BTC loan with fixed total debt returns `BtcLoanDetailsInputDTO` with `Apr == 0m` and `FixedTotalDebt` set.
    - Edit: BTC lending returns `BtcLendingDetailsInputDTO`.
    - Load: Stock returns `AssetFormValues` with `SelectedAssetType == "Stock"`, `AcquisitionDate` round-tripped, `AcquisitionPriceFiat` set, and `Quantity` set.
    - Load: RealEstate returns values with `Address`, `CurrentValueFiat`, `MonthlyRentalIncomeFiat`, and acquisition fields.
    - Load: Leveraged Bitcoin position sets `IsBitcoinUnderlyingAsset == true`, `UseExactPosition` from `InputModeId`, and `PositionSize`.
    - Load: BTC loan with no repayment date sets `IsIndefiniteLoan == true`; with fixed total debt sets `UseFixedTotalDebt == true` and `FixedTotalDebtFiat`.
    - Load: BTC lending with no repayment date sets `IsIndefiniteLending == true`.

    Ensure focused coverage on BTC loan, leveraged, and real estate edge cases.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~AssetFormBuilderTests|FullyQualifiedName~ManageAssetViewModelTests"</automated>
  </verify>
  <acceptance_criteria>
    - tests/Valt.Tests/UI/Services/AssetFormBuilderTests.cs exists and declares `[TestFixture]`.
    - The test file contains at least 15 `[Test]` methods.
    - `_assetFormBuilder` is instantiated in `ManageAssetViewModelTests.SetUp` and passed to `CreateViewModel()`.
    - `dotnet test --filter "FullyQualifiedName~AssetFormBuilderTests"` passes.
    - `dotnet test --filter "FullyQualifiedName~ManageAssetViewModelTests"` passes.
    - `dotnet test` exits 0.
  </acceptance_criteria>
  <done>Builder and ViewModel tests pass and provide focused coverage of complex asset types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Builder input | Form snapshot values already validated by `ManageAssetViewModel` and command validators; no untrusted input crosses into the builder. |
| Price provider call | `IAssetPriceProviderSelector` is an existing infrastructure abstraction; no new network surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | In-memory form snapshot | accept | Values are validated upstream by ViewModel `[Required]`/`[NotifyDataErrorInfo]` and App-layer command validators. |
| T-18-02 | Information Disclosure | Builder logging/error messages | accept | Builder throws only `AssetFormBuildException`; no PII or secrets are logged. |
| T-18-SC | Tampering | Package installs | mitigate | No packages installed; ASVS L1 high-severity blocking threshold is not crossed. |
</threat_model>

<verification>
- `dotnet build Valt.sln` passes.
- `dotnet test --filter "FullyQualifiedName~AssetFormBuilderTests"` passes.
- `dotnet test --filter "FullyQualifiedName~ManageAssetViewModelTests"` passes.
- `dotnet test` full suite passes.
- `LayerDependencyTests` architecture tests still pass.
- `ManageAssetViewModel` parameterless constructor remains unchanged.
</verification>

<success_criteria>
- `IAssetFormBuilder` is registered in DI and injected into `ManageAssetViewModel`.
- `ManageAssetViewModel` no longer constructs `Create*AssetCommand` or `AssetDetailsInputDTO` subtypes directly.
- `ManageAssetViewModel` behavior is functionally identical (all existing tests pass).
- `AssetFormBuilderTests` cover all 5 create/edit families with focused BTC loan, leveraged, and real estate cases.
- Full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/18-manage-asset-builder/18-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/Valt.UI/Services/IAssetFormBuilder.cs`
  - `AssetFormSnapshot` sealed record
  - `AssetFormValues` sealed record
  - `CreateAssetCommandEnvelope` abstract record
  - `BasicAssetCommandEnvelope` sealed record
  - `RealEstateAssetCommandEnvelope` sealed record
  - `LeveragedPositionCommandEnvelope` sealed record
  - `BtcLoanCommandEnvelope` sealed record
  - `BtcLendingCommandEnvelope` sealed record
  - `IAssetFormBuilder` interface
- `src/Valt.UI/Services/Exceptions/AssetFormBuildException.cs`
  - `AssetFormBuildException` class
- `src/Valt.UI/Services/AssetFormBuilder.cs`
  - `AssetFormBuilder` class
- `src/Valt.UI/Extensions.cs`
  - `services.AddSingleton&lt;IAssetFormBuilder, AssetFormBuilder&gt;();`
- `src/Valt.UI/Views/Main/Modals/ManageAsset/ManageAssetViewModel.cs`
  - `_assetFormBuilder` field
  - production constructor parameter
  - `BuildCreateCommandAsync` call site
  - `BuildEditDetailsAsync` call site
  - `LoadFromDto` call site
- `tests/Valt.Tests/UI/Services/AssetFormBuilderTests.cs`
  - `AssetFormBuilderTests` fixture and tests
- `tests/Valt.Tests/UI/Screens/ManageAssetViewModelTests.cs`
  - `_assetFormBuilder` field and updated `CreateViewModel()` helper
