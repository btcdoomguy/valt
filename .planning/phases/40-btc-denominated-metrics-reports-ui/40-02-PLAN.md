---
phase: 40-btc-denominated-metrics-reports-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - src/Valt.Infra/Modules/BtcDenominatedMetrics/Queries/BtcDenominatedMetricsQueries.cs
  - src/Valt.App/Modules/BtcDenominatedMetrics/DTOs/BtcDenominatedMetricsDataDto.cs
  - src/Valt.App/Modules/BtcDenominatedMetrics/DTOs/SatsSpentByCategoryDto.cs
  - src/Valt.UI/Views/Main/Tabs/Reports/BtcDenominatedMetricsChartData.cs
  - src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
  - src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
  - tests/Valt.Tests/Reports/BtcDenominatedMetricsQueriesTests.cs
autonomous: true
requirements:
  - BTC-02
  - BTC-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The 'Sats earned & spent' panel has a toggle that switches between monthly grouped bars and a per-category horizontal-bar breakdown (D-13, D-14)."
    - "The category breakdown is computed from selected categories for the same date range and excludes internal transfers and missing-rate transactions (D-01, D-04, D-06, D-07)."
    - "Categories with zero sats spent are omitted from the category chart while remaining categories keep their relative scale (UI-SPEC covered E4 partial)."
    - "Zero categories in the breakdown render the same empty-state copy as the monthly view (UI-SPEC covered E4)."
    - "The category chart is wrapped in a ScrollViewer with MaxHeight='600' and VerticalScrollBarVisibility='Auto' (UI-SPEC covered E4)."
    - "The query tests verify earned, spent, internal-transfer exclusion, missing-rate skip, native bitcoin income/expense sign handling, non-main-currency conversion, stack velocity including BTC purchases/sales, and category aggregation (D-01 through D-09)."
    - "The full test suite and the build remain green after the phase."
  artifacts:
    - BtcDenominatedMetricsQueries category aggregation
    - SatsSpentByCategoryDto populated list
    - BtcDenominatedMetricsChartData category-row mode
    - ReportsViewModel.IsBtcMetricsCategoryView observable
    - ReportsView.axaml toggle row
    - BtcDenominatedMetricsQueriesTests comprehensive suite
  key_links:
    - "BtcDenominatedMetricsQueries.SpentByCategory -> BtcDenominatedMetricsChartData category mode"
    - "ReportsViewModel.IsBtcMetricsCategoryView -> ReportsView.axaml toggle -> chart visibility"
---

<objective>
Complete the Phase 40 feature set by adding the sats-spent-by-category breakdown and comprehensive tests, then verify the entire phase is green.

Purpose: The category view is the remaining piece of BTC-02 and must share the same aggregation rules as the monthly view; tests lock down the scope decisions so Phase 43 can localize and document without regression risk.

Output: Populated `SpentByCategory` DTO, category chart mode, VM toggle, XAML toggle, and full `BtcDenominatedMetricsQueriesTests` suite.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-btc-denominated-metrics-reports-ui/40-CONTEXT.md
@.planning/phases/40-btc-denominated-metrics-reports-ui/40-PATTERNS.md
@.planning/phases/40-btc-denominated-metrics-reports-ui/40-UI-SPEC.md
@/home/vmabellini/RiderProjects/valt/AGENTS.md
@/home/vmabellini/RiderProjects/valt/.planning/phases/40-btc-denominated-metrics-reports-ui/40-01-PLAN.md
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ExpensesByCategoryChartData.cs
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
@/home/vmabellini/RiderProjects/valt/src/Valt.App/Modules/BtcDenominatedMetrics/DTOs/SatsSpentByCategoryDto.cs
@/home/vmabellini/RiderProjects/valt/src/Valt.Infra/Modules/BtcDenominatedMetrics/Queries/BtcDenominatedMetricsQueries.cs
@/home/vmabellini/RiderProjects/valt/tests/Valt.Tests/Reports/BtcDenominatedMetricsQueriesTests.cs
</context>

## Cross-cutting declarations

- API coverage: No new external API/SDK/webhook/plugin is introduced. No COVERAGE.md matrix is produced.
- Schema push: No LiteDB migration or ORM schema push is required. Only read-only DTOs are extended.
- Assumption-delta: None. The category list is a second projection of existing category entities, not a new domain identity transition.

<tasks>

<task type="auto">
  <name>Task 1: Sats spent per category breakdown</name>
  <files>
    src/Valt.Infra/Modules/BtcDenominatedMetrics/Queries/BtcDenominatedMetricsQueries.cs
    src/Valt.App/Modules/BtcDenominatedMetrics/DTOs/BtcDenominatedMetricsDataDto.cs
    src/Valt.App/Modules/BtcDenominatedMetrics/DTOs/SatsSpentByCategoryDto.cs
    src/Valt.UI/Views/Main/Tabs/Reports/BtcDenominatedMetricsChartData.cs
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
  </files>
  <read_first>
    @/home/vmabellini/RiderProjects/valt/src/Valt.Infra/Modules/BtcDenominatedMetrics/Queries/BtcDenominatedMetricsQueries.cs
    @/home/vmabellini/RiderProjects/valt/src/Valt.App/Modules/BtcDenominatedMetrics/DTOs/SatsSpentByCategoryDto.cs
    @/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ExpensesByCategoryChartData.cs
    @/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    @/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
  </read_first>
  <action>
    Extend the Plan 40-01 implementation to support the category-breakdown view.

    1. BtcDenominatedMetricsQueries: after computing monthly sats spent, also aggregate by category ID for the same filtered transaction set. For each expense transaction (fiat converted to sats at the transaction date or native bitcoin negative), add the sats amount to a dictionary keyed by `CategoryId`. Look up the category in `provider.Categories` to obtain `Name`, `Icon.Unicode`, and `Icon.Color`. Sort by `SatsTotal` descending (largest negative magnitude first) and return the list in `BtcDenominatedMetricsDataDto.SpentByCategory`. Only include categories with `SatsTotal > 0`; omit zero-spend categories. Keep the same exclusion rules (internal transfers, missing exact BTC rate, no BTC purchases/sales) so the category view is consistent with the monthly view (D-01, D-04, D-06, D-07).
    2. BtcDenominatedMetricsChartData: add a category mode. Add an `ObservableCollection<string> CategoryLabels`, an `Axis[]` for category labels, and a `RefreshCategoryChart(IReadOnlyList<SatsSpentByCategoryDto> items)` method that creates a `RowSeries<double>` with values as positive doubles (for bar length) and individual bar colors from `IconColor`. Use a dynamic `ChartHeight` similar to `ExpensesByCategoryChartData` (`Math.Max(300, count * 40)`). Wrap the row series in a `ScrollViewer` with `MaxHeight="600"` and `VerticalScrollBarVisibility="Auto"` in the XAML (not in the chart-data class). Dispose the row series and recreate it on each refresh.
    3. ReportsViewModel: add `[ObservableProperty] private bool _isBtcMetricsCategoryView;` (default false). In `FetchBtcDenominatedMetricsAsync`, after calling `BtcDenominatedMetricsChartData.RefreshChart(data)`, also call `BtcDenominatedMetricsChartData.RefreshCategoryChart(data.SpentByCategory)`. Add a `partial void OnIsBtcMetricsCategoryViewChanged(bool value)` handler that triggers a no-op property notification on `BtcDenominatedMetricsChartData` so the XAML visibility bindings re-evaluate; do NOT re-fetch data on toggle changes.
    4. ReportsView.axaml: inside the "Sats earned & spent" expander, above the chart container, add a centered `StackPanel` with `Orientation="Horizontal"` containing `TextBlock Text="{x:Static lang:language.Reports_BtcMetrics_MonthlyLabel}"`, a `ToggleSwitch IsChecked="{Binding IsBtcMetricsCategoryView, Mode=TwoWay}"`, and `TextBlock Text="{x:Static lang:language.Reports_BtcMetrics_CategoryLabel}"`. Then add two panels: one for the monthly CartesianChart (visible when `!IsBtcMetricsCategoryView`) and one containing the `ScrollViewer`/CartesianChart for the category view (visible when `IsBtcMetricsCategoryView`). Both share the same outer loading and empty states.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>
    - The query populates `SpentByCategory` with sorted, non-zero category rows.
    - `BtcDenominatedMetricsChartData` exposes a category row series mode.
    - The toggle switches the visible chart in the UI without re-fetching.
    - `dotnet build Valt.sln` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive query tests</name>
  <files>
    tests/Valt.Tests/Reports/BtcDenominatedMetricsQueriesTests.cs
  </files>
  <read_first>
    @/home/vmabellini/RiderProjects/valt/tests/Valt.Tests/Reports/BtcDenominatedMetricsQueriesTests.cs
    @/home/vmabellini/RiderProjects/valt/tests/Valt.Tests/Reports/SavingsRateQueriesTests.cs
    @/home/vmabellini/RiderProjects/valt/src/Valt.Infra/Modules/Reports/ReportDataProvider.cs
  </read_first>
  <action>
    Expand the tracer test file into a full suite covering every scope decision.

    Use the `DatabaseTest` base, `FakeClock`, `TransactionBuilder`, `FiatAccountBuilder`, `BtcAccountBuilder`, `CategoryBuilder`, and seed BTC and fiat rates for every day of the test range. Add the following tests (each name must be unique and descriptive):

    1. `Should_Convert_Fiat_Income_And_Expense_To_Sats_Per_Month` — verifies `SatsEarned` and `SatsSpent` for a single BRL transaction month using the seeded BTC/USD and BRL/USD rates (D-05, D-08).
    2. `Should_Include_Native_Bitcoin_Income_And_Expense_By_Sign` — add a positive native BTC transaction to earned and a negative native BTC transaction to spent (D-02).
    3. `Should_Exclude_Btc_Purchases_And_Sales_From_Earned_And_Spent` — add a `FiatToBitcoin` and a `BitcoinToFiat` transaction and assert they do not affect `SatsEarned`/`SatsSpent` (D-01).
    4. `Should_Include_Btc_Purchases_And_Sales_In_Stack_Velocity` — assert the same transactions from test 3 do affect `StackVelocity` with the correct sign (D-09).
    5. `Should_Exclude_Internal_Transfers` — add a transaction with a `ToAccountId` that exists in the seeded accounts and assert it is excluded from earned, spent, and velocity (D-04).
    6. `Should_Skip_Missing_Btc_Rate_Date` — remove the BTC rate for the transaction date and assert the transaction is silently skipped (D-06, D-07).
    7. `Should_Include_Current_Incomplete_Month` — set the clock to the middle of a month and assert that month appears in the result (D-03, D-12).
    8. `Should_Return_Zero_Velocity_Months_On_Baseline` — insert transactions in month 1 and month 3 but not month 2; assert month 2 has `StackVelocity == 0` and is present (D-11).
    9. `Should_Aggregate_Sats_Spent_By_Category` — insert two expense transactions in different categories and assert `SpentByCategory` has two rows sorted by magnitude with correct category names and icon data (BTC-02).
    10. `Should_Honor_Category_Exclusion` — pass a `CategoryIds` array containing only one of the two categories and assert the excluded category does not appear in either monthly spent or category breakdown (D-18).
    11. `Should_Honor_Account_Filter` — pass an `AccountIds` array containing only one account and assert transactions from other accounts are excluded.

    Use `ReportDataProvider` directly via the convenience constructor or `ReportDataProviderFactory` in the test helper, and construct `BtcDenominatedMetricsQueries` exactly as `SavingsRateQueriesTests` constructs `SavingsRateQueries`.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~BtcDenominatedMetricsQueriesTests"</automated>
  </verify>
  <done>
    - All 11 tests pass.
    - The suite runs in under 60 seconds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final verification and cleanup</name>
  <files>
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
    tests/Valt.Tests/Reports/BtcDenominatedMetricsQueriesTests.cs
  </files>
  <read_first>
    @/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    @/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
  </read_first>
  <action>
    Run the final quality gates and fix any immediate regressions.

    1. Build the solution: `dotnet build Valt.sln`.
    2. Run the full test suite: `dotnet test`.
    3. If any test fails due to the new code, fix it in this task (do not leave failing tests). If failures are unrelated, note them and stop.
    4. Verify that `ReportsViewModel` disposes `BtcDenominatedMetricsChartData` and `StackVelocityChartData` in its `Dispose` method and that the new observables are initialized.
    5. Grep the new `language.resx` keys and their `language.Designer.cs` accessors to confirm they exist.
    6. Confirm that the two new expanders are placed between the Fixed vs variable and Categories expanders.
  </action>
  <verify>
    <automated>
      dotnet build Valt.sln && dotnet test
    </automated>
  </verify>
  <done>
    - `dotnet build Valt.sln` passes.
    - `dotnet test` passes (or any failures are pre-existing and documented).
    - All localization keys and accessors are present.
    - New UI sections are in the correct order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Category toggle → ViewModel | Local boolean binding; no external input. |
| Category aggregation → query | Same read-only, already-filtered data path as Plan 40-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-02-01 | Tampering | `IsBtcMetricsCategoryView` toggle state | low | accept | Toggle only changes local UI visibility; no data or authorization decision depends on it. |
| T-40-02-02 | Denial of Service | Category breakdown on very large category sets | low | mitigate | Category aggregation is a single dictionary accumulation; the chart height is capped at `ScrollViewer.MaxHeight="600"`. |
| T-40-SC | Tampering | NuGet/package installs | n/a | n/a | No new packages are installed in this phase. |
</threat_model>

<verification>
- `dotnet build Valt.sln` passes.
- `dotnet test --filter "FullyQualifiedName~BtcDenominatedMetricsQueriesTests"` passes the full suite.
- `dotnet test` passes or documents pre-existing failures.
- The category toggle in the UI switches the chart view without re-fetching.
</verification>

<success_criteria>
- The sats spent per category breakdown is returned by the query, rendered as horizontal bars, and sorted by magnitude.
- The UI toggle correctly switches between monthly grouped bars and the category view.
- Comprehensive tests cover all scope decisions (D-01 through D-09) and the UI placement (D-13, D-14, D-16).
- The build and test suite are green.
</success_criteria>

## Artifacts this phase produces (Plan 40-02)

### Backend
- `BtcDenominatedMetricsQueries` category aggregation logic.
- Populated `SatsSpentByCategoryDto` list in `BtcDenominatedMetricsDataDto`.

### UI
- `BtcDenominatedMetricsChartData.RefreshCategoryChart` and category row-series mode.
- `ReportsViewModel.IsBtcMetricsCategoryView` observable and toggle handler.
- `ReportsView.axaml` toggle row (`Monthly` / `ToggleSwitch` / `By category`) and dual chart panels.

### Tests
- Expanded `BtcDenominatedMetricsQueriesTests` covering all 11 scenarios above.

<output>
Create `.planning/phases/40-btc-denominated-metrics-reports-ui/40-02-SUMMARY.md` when done.
</output>
