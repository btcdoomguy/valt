---
phase: 09-loan-state-history-screen
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/Valt.UI/Views/Main/Tabs/Assets/AssetsView.axaml
  - src/Valt.UI/Views/Main/Tabs/Assets/AssetsViewModel.cs
  - src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateView.axaml
  - src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateViewModel.cs
autonomous: true
requirements:
  - UI-07
user_setup: []

must_haves:
  truths:
    - User can open Loan State History from the Assets tab context menu on a BTC loan
    - User can open Loan State History from a "View History" link inside the Update Loan State modal
    - Both entry points pass the correct asset ID to the history modal
  artifacts:
    - path: src/Valt.UI/Views/Main/Tabs/Assets/AssetsView.axaml
      provides: "Context menu item for Loan State History"
      contains: "Assets_LoanStateHistory"
    - path: src/Valt.UI/Views/Main/Tabs/Assets/AssetsViewModel.cs
      provides: "OpenLoanStateHistoryCommand"
      exports:
        - OpenLoanStateHistoryCommand
    - path: src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateView.axaml
      provides: "View History link"
      contains: "UpdateLoanState_ViewHistory"
    - path: src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateViewModel.cs
      provides: "OpenHistoryCommand"
      exports:
        - OpenHistoryCommand
  key_links:
    - from: AssetsView.axaml MenuItem
      to: AssetsViewModel.OpenLoanStateHistoryCommand
      via: Command binding
      pattern: "OpenLoanStateHistoryCommand"
    - from: AssetsViewModel.OpenLoanStateHistoryCommand
      to: LoanStateHistoryViewModel.Request
      via: _modalFactory.CreateAsync
      pattern: "new LoanStateHistoryViewModel.Request { AssetId = asset.Id }"
    - from: UpdateLoanStateView.axaml Button
      to: UpdateLoanStateViewModel.OpenHistoryCommand
      via: Command binding
      pattern: "OpenHistoryCommand"
    - from: UpdateLoanStateViewModel.OpenHistoryCommand
      to: LoanStateHistoryViewModel.Request
      via: _modalFactory.CreateAsync
      pattern: "new LoanStateHistoryViewModel.Request { AssetId = AssetId }"
---

<objective>
Wire the two entry points that open the Loan State History modal: the Assets tab BTC-loan context menu and the "View History" link inside the Update Loan State modal.

Purpose: Fulfills UI-07 by making the history modal reachable from both specified locations.
Output: Modified AssetsView/AssetsViewModel and UpdateLoanStateView/UpdateLoanStateViewModel connected to the new LoanStateHistory modal.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-loan-state-history-screen/09-CONTEXT.md
@.planning/phases/09-loan-state-history-screen/09-PATTERNS.md
@.planning/phases/09-loan-state-history-screen/09-UI-SPEC.md

@src/Valt.UI/Views/Main/Tabs/Assets/AssetsView.axaml
@src/Valt.UI/Views/Main/Tabs/Assets/AssetsViewModel.cs
@src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateView.axaml
@src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateViewModel.cs
@src/Valt.UI/Views/Main/Modals/LoanStateHistory/LoanStateHistoryViewModel.cs
@src/Valt.UI/Views/ApplicationModalNames.cs
@src/Valt.UI/Lang/language.resx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Assets tab context menu and command</name>
  <files>src/Valt.UI/Views/Main/Tabs/Assets/AssetsView.axaml, src/Valt.UI/Views/Main/Tabs/Assets/AssetsViewModel.cs</files>
  <read_first>
    src/Valt.UI/Views/Main/Tabs/Assets/AssetsView.axaml
    src/Valt.UI/Views/Main/Tabs/Assets/AssetsViewModel.cs
    src/Valt.UI/Views/Main/Modals/LoanStateHistory/LoanStateHistoryViewModel.cs
    .planning/phases/09-loan-state-history-screen/09-PATTERNS.md
  </read_first>
  <action>
    In AssetsView.axaml, add a new MenuItem immediately after the existing "Assets_UpdateLoanState" MenuItem (before the "Assets_RepayLoan" item) with Header="{x:Static lang:language.Assets_LoanStateHistory}", Command="{Binding $parent[UserControl].((assets:AssetsViewModel)DataContext).OpenLoanStateHistoryCommand}", CommandParameter="{Binding}", IsVisible="{Binding IsBtcLoan}". In AssetsViewModel.cs add using Valt.UI.Views.Main.Modals.LoanStateHistory; add a new [RelayCommand] private async Task OpenLoanStateHistory(AssetViewModel? asset) method immediately after UpdateLoanStateCommand. The method must: return early if IsSecureModeEnabled is true; return early if asset is null or !asset.IsBtcLoan; get ownerWindow from GetUserControlOwnerWindow?.Invoke() and return if null; create the modal via (LoanStateHistoryView)await _modalFactory.CreateAsync(ApplicationModalNames.LoanStateHistory, ownerWindow, new LoanStateHistoryViewModel.Request { AssetId = asset.Id }); await modal.ShowDialogSafeAsync&lt;LoanStateHistoryViewModel.Response?&gt;(ownerWindow).
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <acceptance_criteria>
    - AssetsView.axaml contains a MenuItem with Header="{x:Static lang:language.Assets_LoanStateHistory}" bound to OpenLoanStateHistoryCommand
    - The new MenuItem has CommandParameter="{Binding}" and IsVisible="{Binding IsBtcLoan}"
    - AssetsViewModel.cs contains a method signature `[RelayCommand] private async Task OpenLoanStateHistory(AssetViewModel? asset)`
    - The command returns early when `IsSecureModeEnabled` is true
    - The command returns early when `asset is null || !asset.IsBtcLoan`
    - The command calls `_modalFactory.CreateAsync(ApplicationModalNames.LoanStateHistory, ownerWindow, new LoanStateHistoryViewModel.Request { AssetId = asset.Id })`
    - The command awaits `modal.ShowDialogSafeAsync<LoanStateHistoryViewModel.Response?>(ownerWindow)`
  </acceptance_criteria>
  <done>The Assets tab context menu exposes Loan State History only for BTC loans and opens the modal with the correct asset ID.</done>
</task>

<task type="auto">
  <name>Task 2: Add View History link to Update Loan State modal</name>
  <files>src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateView.axaml, src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateViewModel.cs</files>
  <read_first>
    src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateView.axaml
    src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateViewModel.cs
    src/Valt.UI/Views/Main/Modals/LoanStateHistory/LoanStateHistoryViewModel.cs
    .planning/phases/09-loan-state-history-screen/09-UI-SPEC.md
    .planning/phases/09-loan-state-history-screen/09-PATTERNS.md
  </read_first>
  <action>
    In UpdateLoanStateView.axaml, locate the "Current Loan Context" Border with the TextBlock using Classes="form-section-header" and Text="{x:Static lang:language.UpdateLoanState_ContextHeader}". Wrap that header and a new right-aligned Button inside a Grid with ColumnDefinitions="*, Auto". The TextBlock stays in Column="0". Add a Button in Column="1" with Content="{x:Static lang:language.UpdateLoanState_ViewHistory}", Command="{Binding OpenHistoryCommand}", Classes="transparent link", HorizontalAlignment="Right". In UpdateLoanStateViewModel.cs add a new [RelayCommand] private async Task OpenHistory() method after the existing Close command. The method must: return early if GetWindow is null; set var ownerWindow = GetWindow(); create the modal via (LoanStateHistoryView)await _modalFactory.CreateAsync(ApplicationModalNames.LoanStateHistory, ownerWindow, new LoanStateHistoryViewModel.Request { AssetId = AssetId }); await modal.ShowDialogSafeAsync&lt;LoanStateHistoryViewModel.Response?&gt;(ownerWindow). Do not close the UpdateLoanStateView when opening history.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <acceptance_criteria>
    - UpdateLoanStateView.axaml contains a Grid with ColumnDefinitions="*, Auto" inside the Current Loan Context section
    - The Grid contains a TextBlock with Text="{x:Static lang:language.UpdateLoanState_ContextHeader}" in Column="0"
    - The Grid contains a Button with Content="{x:Static lang:language.UpdateLoanState_ViewHistory}", Command="{Binding OpenHistoryCommand}", Classes="transparent link"
    - UpdateLoanStateViewModel.cs contains a method signature `[RelayCommand] private async Task OpenHistory()`
    - The command returns early when `GetWindow is null`
    - The command calls `_modalFactory.CreateAsync(ApplicationModalNames.LoanStateHistory, ownerWindow, new LoanStateHistoryViewModel.Request { AssetId = AssetId })`
    - The command awaits `modal.ShowDialogSafeAsync<LoanStateHistoryViewModel.Response?>(ownerWindow)`
    - The UpdateLoanStateView is not closed by the OpenHistory command
  </acceptance_criteria>
  <done>The Update Loan State modal has a right-aligned "View History" link that opens the Loan State History modal for the same asset without closing the update modal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Assets context menu | User action limited to selecting an existing asset |
| User → Update Loan State link | User action inside an already-opened authorized modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | OpenLoanStateHistoryCommand parameter | mitigate | Command validates `asset.IsBtcLoan` before opening the modal; only real BTC loan assets reach the history view |
| T-09-02-02 | Information Disclosure | OpenHistoryCommand | accept | History is opened for the same AssetId already visible in the Update Loan State modal |
| T-09-02-03 | Tampering | CommandParameter binding | accept | WPF/Avalonia binding passes the bound AssetViewModel; no user-controlled data parsed |
| T-09-SC | Tampering | npm/pip/cargo installs | not applicable | No new external packages installed in this phase |
</threat_model>

<verification>
- dotnet build Valt.sln passes after each task.
- The new context-menu item and View History button resolve their bindings at runtime.
</verification>

<success_criteria>
- Assets tab context menu shows "Loan State History" only for BTC loans.
- Selecting it opens the Loan State History modal with the asset ID of the selected card.
- The Update Loan State modal shows a right-aligned "View History" link next to the Current Loan Context header.
- Clicking the link opens the Loan State History modal for the same asset.
</success_criteria>

<output>
Create `.planning/phases/09-loan-state-history-screen/09-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Modified files**
- `src/Valt.UI/Views/Main/Tabs/Assets/AssetsView.axaml`
- `src/Valt.UI/Views/Main/Tabs/Assets/AssetsViewModel.cs`
- `src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateView.axaml`
- `src/Valt.UI/Views/Main/Modals/UpdateLoanState/UpdateLoanStateViewModel.cs`

**New commands**
- `AssetsViewModel.OpenLoanStateHistoryCommand` (method: `private async Task OpenLoanStateHistory(AssetViewModel? asset)`)
- `UpdateLoanStateViewModel.OpenHistoryCommand` (method: `private async Task OpenHistory()`)

**New UI elements**
- `MenuItem` in `AssetsView.axaml` with `Header="{x:Static lang:language.Assets_LoanStateHistory}"`, `Command="{Binding ...OpenLoanStateHistoryCommand}"`, `IsVisible="{Binding IsBtcLoan}"`
- `Button` in `UpdateLoanStateView.axaml` with `Content="{x:Static lang:language.UpdateLoanState_ViewHistory}"`, `Command="{Binding OpenHistoryCommand}"`, `Classes="transparent link"`

**New using directive**
- `using Valt.UI.Views.Main.Modals.LoanStateHistory;` in `AssetsViewModel.cs`
