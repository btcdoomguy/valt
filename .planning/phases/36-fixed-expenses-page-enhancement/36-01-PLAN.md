---
phase: 36-fixed-expenses-page-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../valt-docs/docs/funcionalidades/despesas-fixas.md
autonomous: true
requirements:
  - FXE-01
  - FXE-02
  - FXE-03
must_haves:
  truths:
    - "The Portuguese Fixed Expenses page contains the four record states inline in the Recording section."
    - "The record-states table has exactly State and Meaning columns; no third column explains how each state is reached."
    - "Portuguese state labels are the exact app strings: Pago, Pago Manualmente, Ignorado, Pendente."
    - "The Portuguese page contains a standalone Visão Geral Anual section between Managing and Fixed Expenses Report."
    - "The yearly overview is a brief paragraph (3-4 sentences) covering the calendar icon navigation path, 12-month grid, year selector, expected/actual columns, and totals."
    - "The yearly overview does not enumerate every column or every possible state."
    - "The Portuguese page contains an out-of-range sub-section inside the yearly overview."
    - "Out-of-range wording is conceptual only: values are flagged when the paid amount differs from expected or falls outside the expected range."
    - "The out-of-range sub-section does not include exact formulas or UI color/highlight details."
    - "The Portuguese page contains an info-note inside the Linking to an Account section clarifying account-vs-currency exclusivity."
    - "Portuguese account-vs-currency labels are the exact app strings: Da conta padrão and Definição direta."
    - "The info-note states that a fixed expense is bound to either an account or a currency, not both, and that selecting one clears the other."
    - "D-01: Record states documented inline in the existing Lançando uma Despesa Fixa section."
    - "D-02: States presented as a compact table with State and Meaning columns."
    - "D-03: Table kept to state and meaning only; no column explaining how each state is reached."
    - "D-04: Exact app UI labels used for Portuguese and English state names."
    - "D-05: Yearly overview documented as a brief paragraph (3-4 sentences)."
    - "D-06: Yearly overview added as a standalone Visão Geral Anual / Yearly Overview section, separate from the Fixed Expenses Report section."
    - "D-07: Navigation path included: opened from the calendar icon in the Fixed Expenses panel header on the Transactions tab."
    - "D-08: 12-month grid, year selector, and expected/actual columns mentioned at a high level; no exhaustive enumeration."
    - "D-09: Out-of-range detection documented as a sub-section inside the yearly overview."
    - "D-10: Out-of-range explanation kept conceptual only."
    - "D-11: No exact formulas or UI color/highlight details in the out-of-range explanation."
    - "D-12: Account-vs-currency exclusivity clarified as an info-note inside Vinculando a uma Conta / Linking to an Account."
    - "D-13: Exact app UI labels used for the two modes: Da conta padrão / From default account and Definição direta / Direct set."
    - "D-14: Rule stated as an info note: a fixed expense is bound to either an account or a currency, not both; selecting one clears the other."
  artifacts:
    - path: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      provides: "Updated Portuguese Fixed Expenses page with record states, yearly overview, and account-vs-currency note"
      contains: "## Visão Geral Anual"
    - path: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      provides: "Record-states table in the Recording section"
      contains: "| Estado | Significado |"
    - path: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      provides: "Account-vs-currency exclusivity info-note"
      contains: "!!! info \"Conta ou moeda, não ambos\""
  key_links:
    - from: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      to: src/Valt.UI/Lang/language.pt-BR.resx
      via: "Portuguese state labels and currency-definition labels copied from FixedExpenseOverview.Status.* and ManageFixedExpenses.CurrencyDefinition.* keys"
    - from: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      to: src/Valt.Core/Modules/Budget/FixedExpenses/FixedExpenseRecordState.cs
      via: "Domain enum values map to the Pending/Pago user-facing label for the Empty state"
    - from: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      to: src/Valt.Core/Modules/Budget/FixedExpenses/FixedExpense.cs
      via: "Mutual exclusivity of DefaultAccountId and Currency"
---

<objective>
Update the Portuguese Fixed Expenses public documentation page (`../valt-docs/docs/funcionalidades/despesas-fixas.md`) to document the four record states, the yearly overview modal, and the account-vs-currency binding exclusivity. The page must use the exact Valt app Portuguese terminology and preserve the existing section structure.

Purpose: Portuguese readers can understand the current Fixed Expenses feature, including record states, the yearly overview, and the mutual exclusivity of account and currency binding.
Output: An updated `../valt-docs/docs/funcionalidades/despesas-fixas.md` that satisfies FXE-01, FXE-02, and FXE-03.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-fixed-expenses-page-enhancement/36-CONTEXT.md
@.planning/phases/36-fixed-expenses-page-enhancement/36-RESEARCH.md
@.planning/phases/36-fixed-expenses-page-enhancement/36-PATTERNS.md
@.claude/docs/fixedexpenses.md
@../valt-docs/docs/funcionalidades/despesas-fixas.md
@src/Valt.UI/Lang/language.pt-BR.resx
</context>

<tasks>

<task>
  <id>36-01-01</id>
  <name>Add record-states table inside the Recording section</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/despesas-fixas.md</files>
  <read_first>
    - .planning/phases/36-fixed-expenses-page-enhancement/36-CONTEXT.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-RESEARCH.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/despesas-fixas.md
    - src/Valt.UI/Lang/language.pt-BR.resx
  </read_first>
  <action>
    Verify that ../valt-docs/mkdocs.yml is readable; abort and report if the sibling repository is missing. Edit ../valt-docs/docs/funcionalidades/despesas-fixas.md and insert a compact record-states table inside the existing `## Lançando uma Despesa Fixa` section, after the step-2 paragraph (`A tela de adicionar transação será aberta...`) and before the `DICAS` paragraph. The table must have exactly two columns: `Estado` and `Significado`. Use the exact Portuguese app labels from `language.pt-BR.resx` for `FixedExpenseOverview.Status.*`: **Pago**, **Pago Manualmente**, **Ignorado**, **Pendente**. The meanings must match the domain semantics from `FixedExpenseRecordState.cs` and `FixedExpenseOverviewViewModel.cs`: Paid = linked to a transaction for the reference date; Manually Paid = marked as paid without a linked transaction; Ignored = skipped for the reference date; Pending = no action taken yet. Do not add a third column explaining how each state is reached. Preserve the existing heading and surrounding prose; optionally tighten the `DICAS` paragraph to mention the right-click options `Ignorar para essa data` and `Marcar como pago` from `language.pt-BR.resx` if the existing text does not already cover them.
  </action>
  <acceptance_criteria>
    - ../valt-docs/docs/funcionalidades/despesas-fixas.md still contains the heading `## Lançando uma Despesa Fixa`.
    - A table with header `| Estado | Significado |` exists inside that section, after the step-2 paragraph and before the `DICAS` paragraph.
    - The table contains exactly the four Portuguese labels **Pago**, **Pago Manualmente**, **Ignorado**, **Pendente** in the Estado column.
    - The table does not contain a third column such as `Como é atingido`, `Ação`, or equivalent.
    - The `DICAS` paragraph mentions the right-click actions to ignore or mark as paid, or already covers them.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f ../valt-docs/mkdocs.yml && \
      grep -q "^## Lançando uma Despesa Fixa" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "| Estado | Significado |" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Pago" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Pago Manualmente" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Ignorado" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Pendente" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      ! grep -q "| Estado | Significado | Como" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      ! grep -q "| Ação |" ../valt-docs/docs/funcionalidades/despesas-fixas.md
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The Portuguese Fixed Expenses page contains the record-states table inline in the Recording section, using exact app labels and only State and Meaning columns.</done>
</task>

<task>
  <id>36-01-02</id>
  <name>Add Yearly Overview section with out-of-range sub-section</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/despesas-fixas.md</files>
  <read_first>
    - .planning/phases/36-fixed-expenses-page-enhancement/36-CONTEXT.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-RESEARCH.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/despesas-fixas.md
    - src/Valt.UI/Lang/language.pt-BR.resx
    - .claude/docs/fixedexpenses.md
  </read_first>
  <action>
    Insert a new standalone `## Visão Geral Anual` section in ../valt-docs/docs/funcionalidades/despesas-fixas.md between the `## Gerenciando Despesas Fixas` section and the `## Relatório de Despesas Fixas 📊` section. Keep the body to 3-4 sentences. Include the navigation path from `FixedExpenseOverviewViewModel.cs`: opened from the calendar icon in the Fixed Expenses panel header on the **Transações** tab. Mention the 12-month grid, the year selector, the expected/actual columns, and the **Total Pago** and **Despesas Futuras** totals from `FixedExpenseOverview.PaidTotal` and `FixedExpenseOverview.FutureExpensesTotal`. Do not enumerate every column or every possible state. Immediately after the overview paragraph, add a sub-section `### Detecção de valores fora da faixa` that explains the out-of-range detection conceptually: for a fixed amount the entry is flagged when the paid amount differs from the expected value; for a variable amount the entry is flagged when the paid amount falls outside the expected minimum-maximum range. Use neutral wording such as `sinalizada` or `marcada`; do not include exact formulas (`|Real| < Min`, `|Real| > Max`, etc.) and do not describe UI colors or highlights.
  </action>
  <acceptance_criteria>
    - The file contains the heading `## Visão Geral Anual` between `## Gerenciando Despesas Fixas` and `## Relatório de Despesas Fixas 📊`.
    - The section body is 3-4 sentences and mentions the calendar icon, the **Transações** tab, the 12-month grid, the year selector, the expected/actual columns, and the **Total Pago** / **Despesas Futuras** totals.
    - The section does not exhaustively list every column or every state.
    - The file contains the sub-section `### Detecção de valores fora da faixa` immediately after the overview paragraph.
    - The sub-section uses conceptual wording (`sinalizada` / `fora da faixa`) and does not contain formulas, color names, or highlight descriptions.
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "^## Visão Geral Anual" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "^### Detecção de valores fora da faixa" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "12 meses\|grade\|calendário\|ícone de calendário" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Total Pago" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Despesas Futuras" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      ! grep -q "|Real| < Min\||Real| > Max\|Math.Abs" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      ! grep -iE "cor |vermelho|amarelo|laranja|destaque" ../valt-docs/docs/funcionalidades/despesas-fixas.md
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The Portuguese page contains a brief Yearly Overview section with a conceptual out-of-range sub-section.</done>
</task>

<task>
  <id>36-01-03</id>
  <name>Add account-vs-currency exclusivity info-note</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/despesas-fixas.md</files>
  <read_first>
    - .planning/phases/36-fixed-expenses-page-enhancement/36-CONTEXT.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-RESEARCH.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/despesas-fixas.md
    - src/Valt.UI/Lang/language.pt-BR.resx
    - src/Valt.Core/Modules/Budget/FixedExpenses/FixedExpense.cs
  </read_first>
  <action>
    Insert an `!!! info` admonition inside the existing `## Vinculando a uma Conta` section in ../valt-docs/docs/funcionalidades/despesas-fixas.md, after the `### Sem Conta Vinculada` subsection and before the `## Histórico de Valores (Ranges) 📈` section. Use the admonition title `Conta ou moeda, não ambos`. The body must state that a fixed expense is bound to either an **account** or a **currency**, never both, and that in the editor the user chooses **Da conta padrão** to link a fiat account or **Definição direta** to choose a currency directly, and that selecting one clears the other. Use the exact Portuguese labels from `language.pt-BR.resx` for `ManageFixedExpenses.CurrencyDefinition.AttachedToDefaultAccount` and `ManageFixedExpenses.CurrencyDefinition.AttachedToCurrency`. Preserve the existing `!!! note` admonition style observed elsewhere on the page (lowercase type, title in double quotes, body indented four spaces).
  </action>
  <acceptance_criteria>
    - The file contains the admonition `!!! info "Conta ou moeda, não ambos"` inside the `## Vinculando a uma Conta` section.
    - The admonition appears after `### Sem Conta Vinculada` and before `## Histórico de Valores (Ranges) 📈`.
    - The admonition contains the exact labels **Da conta padrão** and **Definição direta**.
    - The admonition states that a fixed expense is bound to either an account or a currency, never both, and that selecting one clears the other.
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q '!!! info "Conta ou moeda, não ambos"' ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Da conta padrão" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "Definição direta" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "conta" ../valt-docs/docs/funcionalidades/despesas-fixas.md && \
      grep -q "moeda" ../valt-docs/docs/funcionalidades/despesas-fixas.md
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The Portuguese page contains the account-vs-currency exclusivity info-note in the Linking to an Account section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs ←→ Valt application code | Facts claimed in the public docs must be traceable to the application code or language resources. |
| Portuguese source ←→ English mirror | Every structural change in the Portuguese page must be reflected in the English `.en.md` mirror. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01-01 | Information Integrity | Public Fixed Expenses page | High | Mitigate | Source every factual claim from `src/Valt.Core/Modules/Budget/FixedExpenses/FixedExpenseRecordState.cs`, `FixedExpense.cs`, `FixedExpenseOverviewViewModel.cs`, and the language files. Use exact app labels. Add HTML source-evidence comments above factual claims. Verify with `mkdocs build --strict` and targeted grep checks. Block completion if any claim cannot be traced to a canonical source. |
| T-36-01-02 | Information Disclosure | Public docs pages | Low | Accept | Documentation is intentionally public; no PII, credentials, or secrets are introduced in this phase. |
| T-36-01-SC | Tampering | pip/mkdocs installs | Low | N/A | No new packages are installed. The build uses the existing `valt-docs/.venv` virtual environment. |
</threat_model>

<verification>
- The Portuguese Fixed Expenses page contains the record-states table with only State and Meaning columns, using exact app labels.
- The Portuguese page contains a standalone Yearly Overview section with a conceptual out-of-range sub-section.
- The Portuguese page contains the account-vs-currency exclusivity info-note using exact app mode labels.
- All three additions preserve the existing page structure and section ordering.
</verification>

<success_criteria>
- A Portuguese reader can understand the four record states (Paid, Manually Paid, Ignored, Pending) and their meanings.
- A Portuguese reader can understand the yearly overview, including how to open it, the 12-month grid, and the out-of-range detection concept.
- A Portuguese reader understands that a fixed expense is bound to either an account or a currency, not both, and that selecting one clears the other.
</success_criteria>

<output>
Create `.planning/phases/36-fixed-expenses-page-enhancement/36-01-SUMMARY.md` when done.
</output>

## Context Decisions Addressed

The following user decisions from `36-CONTEXT.md` are implemented in this plan:

- D-01: Document the four record states inline within the existing `Lançando uma Despesa Fixa` section.
- D-02: Present the states as a compact table with State and Meaning columns.
- D-03: Keep the table to state and meaning only; do not add a column explaining how each state is reached.
- D-04: Use the exact app UI labels for the states (`Pago`, `Pago Manualmente`, `Ignorado`, `Pendente` in Portuguese; `Paid`, `Manually Paid`, `Ignored`, `Pending` in English).
- D-05: Document the yearly overview as a brief paragraph (3-4 sentences).
- D-06: Add it as a standalone `Visão Geral Anual / Yearly Overview` section, separate from the Fixed Expenses Report section.
- D-07: Include the navigation path from the calendar icon in the Fixed Expenses panel header on the Transactions tab.
- D-08: Mention the 12-month grid, year selector, and expected/actual columns at a high level; do not enumerate every column or state.
- D-09: Document out-of-range detection as a sub-section inside the yearly overview.
- D-10: Keep the explanation conceptual only: values are flagged when the paid amount differs from expected or falls outside the expected range.
- D-11: Do not include exact formulas or UI color/highlight details; use neutral wording such as `flagged` or `sinalizada`.
- D-12: Clarify the account-vs-currency exclusivity as an info-note inside the `Vinculando a uma Conta / Linking to an Account` section.
- D-13: Use the exact app UI labels for the two modes (`Da conta padrão / From default account` and `Definição direta / Direct set`).
- D-14: State the rule as an info note: a fixed expense is bound to either an account or a currency, not both; selecting one clears the other.

## Artifacts this phase produces

**New files:**
- None.

**Modified files:**
- `../valt-docs/docs/funcionalidades/despesas-fixas.md` — Portuguese Fixed Expenses page updated with the record-states table, the yearly overview and out-of-range sub-section, and the account-vs-currency exclusivity info-note.

**Symbols created:**
- None.
