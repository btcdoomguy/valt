---
phase: 36-fixed-expenses-page-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FXE-01
  - FXE-02
  - FXE-03
must_haves:
  truths:
    - "The English Fixed Expenses page mirrors the three additions made to the Portuguese page."
    - "English state labels are the exact app strings: Paid, Manually Paid, Ignored, Pending."
    - "English account-vs-currency mode labels are the exact app strings: From default account, Direct set."
    - "The English and Portuguese pages have the same heading structure and section ordering."
    - "The valt-docs site builds successfully with `mkdocs build --strict` with no errors or warnings."
    - "REQUIREMENTS.md marks FXE-01, FXE-02, and FXE-03 as Complete."
  artifacts:
    - path: ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
      provides: "Updated English Fixed Expenses page mirroring the Portuguese additions"
      contains: "## Yearly Overview"
    - path: ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
      provides: "Record-states table in the Recording section"
      contains: "| State | Meaning |"
    - path: ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
      provides: "Account-vs-currency exclusivity info-note"
      contains: "!!! info \"Account or currency, not both\""
    - path: .planning/REQUIREMENTS.md
      provides: "Updated FXE-01, FXE-02, FXE-03 statuses"
      contains: "- [x] **FXE-01**"
  key_links:
    - from: ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
      to: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      via: "English page mirrors Portuguese page structure and content"
    - from: ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
      to: src/Valt.UI/Lang/language.resx
      via: "English state labels and currency-definition labels copied from FixedExpenseOverview.Status.* and ManageFixedExpenses.CurrencyDefinition.* keys"
    - from: .planning/REQUIREMENTS.md
      to: ../valt-docs/docs/funcionalidades/despesas-fixas.md
      via: "FXE statuses updated after docs are verified"
---

<objective>
Mirror the Portuguese Fixed Expenses page enhancements to the English translation, verify the sibling documentation site builds cleanly with `mkdocs build --strict`, and update the phase requirements status to Complete.

Purpose: Bilingual readers see the same record-states, yearly overview, and account-vs-currency information; the documentation site remains error-free; and the project requirements reflect the completed work.
Output: Updated `../valt-docs/docs/funcionalidades/despesas-fixas.en.md`, a successful strict MkDocs build, and updated `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-fixed-expenses-page-enhancement/36-CONTEXT.md
@.planning/phases/36-fixed-expenses-page-enhancement/36-RESEARCH.md
@.planning/phases/36-fixed-expenses-page-enhancement/36-PATTERNS.md
@../valt-docs/docs/funcionalidades/despesas-fixas.md
@../valt-docs/docs/funcionalidades/despesas-fixas.en.md
@src/Valt.UI/Lang/language.resx
</context>

<tasks>

<task>
  <id>36-02-01</id>
  <name>Mirror Portuguese additions to the English Fixed Expenses page</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/despesas-fixas.en.md</files>
  <read_first>
    - .planning/phases/36-fixed-expenses-page-enhancement/36-CONTEXT.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-RESEARCH.md
    - .planning/phases/36-fixed-expenses-page-enhancement/36-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/despesas-fixas.md
    - ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
    - src/Valt.UI/Lang/language.resx
  </read_first>
  <action>
    Apply the same structural changes made in Plan 36-01 to the English mirror `../valt-docs/docs/funcionalidades/despesas-fixas.en.md`. Insert the record-states table inside `## Recording a Fixed Expense` using the exact English app labels from `language.resx` for `FixedExpenseOverview.Status.*`: **Paid**, **Manually Paid**, **Ignored**, **Pending**. The table header must be `| State | Meaning |` and must not contain a third column. Insert a new standalone `## Yearly Overview` section between `## Managing Fixed Expenses` and `## Fixed Expenses Report 📊`, keeping it to 3-4 sentences and mentioning the calendar icon in the Fixed Expenses panel header on the **Transactions** tab, the 12-month grid, the year selector, the expected/actual columns, and the **Paid Total** and **Future Expenses** totals. Add the sub-section `### Out-of-range detection` with conceptual wording only (no formulas or colors). Insert the `!!! info "Account or currency, not both"` admonition inside `## Linking to an Account` after `### Without Linked Account` and before `## Value History (Ranges) 📈`, using the exact English labels **From default account** and **Direct set** from `language.resx` for `ManageFixedExpenses.CurrencyDefinition.*`. Ensure the English page's heading structure and section ordering match the updated Portuguese page exactly.
  </action>
  <acceptance_criteria>
    - The English file contains the heading `## Recording a Fixed Expense` with the state table `| State | Meaning |` inside it.
    - The state table contains exactly the four English labels **Paid**, **Manually Paid**, **Ignored**, **Pending**.
    - The English file contains the heading `## Yearly Overview` between `## Managing Fixed Expenses` and `## Fixed Expenses Report 📊`.
    - The English file contains the sub-section `### Out-of-range detection` immediately after the Yearly Overview paragraph.
    - The English file contains the admonition `!!! info "Account or currency, not both"` inside the `## Linking to an Account` section, after `### Without Linked Account` and before `## Value History (Ranges) 📈`.
    - The admonition contains the exact labels **From default account** and **Direct set**.
    - A side-by-side heading count check shows the Portuguese and English files have the same number of `##` and `###` headings in the same order.
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "^## Recording a Fixed Expense" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "| State | Meaning |" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "Paid" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "Manually Paid" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "Ignored" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "Pending" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "^## Yearly Overview" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "^### Out-of-range detection" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q '!!! info "Account or currency, not both"' ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "From default account" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      grep -q "Direct set" ../valt-docs/docs/funcionalidades/despesas-fixas.en.md && \
      test $(grep -c "^## " ../valt-docs/docs/funcionalidades/despesas-fixas.md) -eq $(grep -c "^## " ../valt-docs/docs/funcionalidades/despesas-fixas.en.md) && \
      test $(grep -c "^### " ../valt-docs/docs/funcionalidades/despesas-fixas.md) -eq $(grep -c "^### " ../valt-docs/docs/funcionalidades/despesas-fixas.en.md)
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The English Fixed Expenses page mirrors all three additions from the Portuguese page, using exact English app labels.</done>
</task>

<task>
  <id>36-02-02</id>
  <name>Verify the valt-docs site builds with MkDocs strict mode</name>
  <type>verify</type>
  <files>../valt-docs/mkdocs.yml, ../valt-docs/docs/funcionalidades/despesas-fixas.md, ../valt-docs/docs/funcionalidades/despesas-fixas.en.md</files>
  <read_first>
    - ../valt-docs/mkdocs.yml
    - ../valt-docs/docs/funcionalidades/despesas-fixas.md
    - ../valt-docs/docs/funcionalidades/despesas-fixas.en.md
  </read_first>
  <action>
    Activate the existing valt-docs virtual environment and run `mkdocs build --strict` in the sibling repository. If the build fails with errors or warnings, fix the Markdown or link issues in the two updated Fixed Expenses pages. Do not modify `mkdocs.yml` unless the build error is a configuration issue. If the environment is missing, report it as a blocker instead of installing packages.
  </action>
  <acceptance_criteria>
    - `cd /home/vmabellini/RiderProjects/valt-docs && . .venv/bin/activate && mkdocs build --strict` exits with code 0.
    - The build output contains no `ERROR` or `WARNING` lines.
    - The generated `../valt-docs/site/index.html` exists.
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/vmabellini/RiderProjects/valt-docs && . .venv/bin/activate && mkdocs build --strict 2>&1 | tee /tmp/mkdocs-build-36-02.log && \
      ! grep -iE "ERROR|WARNING" /tmp/mkdocs-build-36-02.log && \
      test -f ../valt-docs/site/index.html
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The valt-docs site builds cleanly with `mkdocs build --strict` after the bilingual Fixed Expenses updates.</done>
</task>

<task>
  <id>36-02-03</id>
  <name>Update REQUIREMENTS.md to mark FXE-01, FXE-02, and FXE-03 as Complete</name>
  <type>docs</type>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` and change the status of FXE-01, FXE-02, and FXE-03 from pending to complete. In the v1 Requirements section, update the checkboxes from `- [ ]` to `- [x]` for the three FXE items. In the Traceability table, change the status from `Pending` to `Complete` for the same three items. Preserve the existing formatting and checklist style.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows `- [x] **FXE-01**` in the Feature Coverage — Fixed Expenses section.
    - `.planning/REQUIREMENTS.md` shows `- [x] **FXE-02**` in the Feature Coverage — Fixed Expenses section.
    - `.planning/REQUIREMENTS.md` shows `- [x] **FXE-03**` in the Feature Coverage — Fixed Expenses section.
    - The Traceability table shows `Complete` for FXE-01, FXE-02, and FXE-03.
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "\- \[x\] \*\*FXE-01\*\*" .planning/REQUIREMENTS.md && \
      grep -q "\- \[x\] \*\*FXE-02\*\*" .planning/REQUIREMENTS.md && \
      grep -q "\- \[x\] \*\*FXE-03\*\*" .planning/REQUIREMENTS.md && \
      grep -q "| FXE-01 | Phase 36 | Complete |" .planning/REQUIREMENTS.md && \
      grep -q "| FXE-02 | Phase 36 | Complete |" .planning/REQUIREMENTS.md && \
      grep -q "| FXE-03 | Phase 36 | Complete |" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>REQUIREMENTS.md reflects that FXE-01, FXE-02, and FXE-03 are Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs ←→ Valt application code | Facts claimed in the public docs must be traceable to the application code or language resources. |
| Portuguese source ←→ English mirror | Every structural change in the Portuguese page must be reflected in the English `.en.md` mirror. |
| Requirements file ←→ Documentation artifacts | Requirement statuses must be updated only after the docs are verified by the strict build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02-01 | Information Integrity | English Fixed Expenses mirror | High | Mitigate | Mirror the Portuguese page exactly; use exact English labels from `language.resx`; verify bilingual heading parity with grep counts; run `mkdocs build --strict` to catch broken links. |
| T-36-02-02 | Information Integrity | REQUIREMENTS.md status update | Medium | Mitigate | Only update FXE statuses to Complete after the MkDocs strict build passes and the bilingual content is verified. |
| T-36-02-03 | Information Disclosure | Public docs pages | Low | Accept | Documentation is intentionally public; no PII, credentials, or secrets are introduced in this phase. |
| T-36-02-SC | Tampering | pip/mkdocs installs | Low | N/A | No new packages are installed. The build uses the existing `valt-docs/.venv` virtual environment. |
</threat_model>

<verification>
- The English Fixed Expenses page mirrors the three Portuguese additions using exact English app labels.
- The Portuguese and English pages have the same heading structure and section ordering.
- `mkdocs build --strict` exits cleanly with no errors or warnings.
- REQUIREMENTS.md marks FXE-01, FXE-02, and FXE-03 as Complete.
</verification>

<success_criteria>
- An English reader can understand the four record states, the yearly overview, and the account-vs-currency exclusivity rule.
- The documentation site builds without errors after the bilingual updates.
- The project requirements accurately reflect that FXE-01, FXE-02, and FXE-03 are complete.
</success_criteria>

<output>
Create `.planning/phases/36-fixed-expenses-page-enhancement/36-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New files:**
- None.

**Modified files:**
- `../valt-docs/docs/funcionalidades/despesas-fixas.en.md` — English Fixed Expenses page mirrored with the record-states table, the yearly overview and out-of-range sub-section, and the account-vs-currency exclusivity info-note.
- `.planning/REQUIREMENTS.md` — FXE-01, FXE-02, and FXE-03 statuses updated to Complete.

**Symbols created:**
- None.
