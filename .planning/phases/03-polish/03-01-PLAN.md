---
phase: 03-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Valt.UI/Lang/language.resx
  - src/Valt.UI/Lang/language.pt-BR.resx
  - src/Valt.UI/Lang/language.es.resx
  - src/Valt.UI/Lang/language.Designer.cs
  - src/Valt.UI/Views/Main/MainView.axaml
  - src/Valt.UI/Views/Main/Tabs/Transactions/TransactionListView.axaml
  - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
  - src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
autonomous: true
requirements:
  - INT-06
must_haves:
  truths:
    - All Spending Evolution UI strings are localized in en-US, pt-BR, and es
    - No hardcoded English strings remain in SpendingEvolution XAML or ViewModel
    - Menu items and context menu use localized strings
    - language.Designer.cs contains static properties for all new resource keys
  artifacts:
    - path: src/Valt.UI/Lang/language.resx
      provides: English localization strings for Spending Evolution
      contains: "SpendingEvolution.Title"
    - path: src/Valt.UI/Lang/language.pt-BR.resx
      provides: Portuguese localization strings
      contains: "Evolução de gastos"
    - path: src/Valt.UI/Lang/language.es.resx
      provides: Spanish localization strings
      contains: "Evolución de gastos"
    - path: src/Valt.UI/Lang/language.Designer.cs
      provides: Auto-generated static properties
      contains: "SpendingEvolution_Title"
  key_links:
    - from: "MainView.axaml"
      to: "language.resx"
      via: "x:Static local:language.Main_Menu_SpendingEvolution"
      pattern: "x:Static local:language"
    - from: "SpendingEvolutionView.axaml"
      to: "language.resx"
      via: "{x:Static local:language.SpendingEvolution_Title}"
      pattern: "x:Static local:language"
---

<objective>
Localize all Spending Evolution module UI strings in en-US, pt-BR, and es.

Purpose: INT-06 requires all three language files to contain localization strings for the Spending Evolution feature. Currently, menu items, modal titles, labels, and warning messages are hardcoded in English.
Output: Updated .resx files for all three languages, updated Designer.cs, and XAML files referencing localized strings instead of hardcoded text.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Valt.UI/Lang/language.resx
@src/Valt.UI/Lang/language.pt-BR.resx
@src/Valt.UI/Lang/language.es.resx
@src/Valt.UI/Lang/language.Designer.cs
@src/Valt.UI/Views/Main/MainView.axaml
@src/Valt.UI/Views/Main/Tabs/Transactions/TransactionListView.axaml
@src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
@src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localization keys to all .resx files</name>
  <files>
    src/Valt.UI/Lang/language.resx
    src/Valt.UI/Lang/language.pt-BR.resx
    src/Valt.UI/Lang/language.es.resx
    src/Valt.UI/Lang/language.Designer.cs
  </files>
  <action>
Add the following data entries to ALL THREE .resx files (language.resx, language.pt-BR.resx, language.es.resx) and then regenerate language.Designer.cs:

**English (language.resx):**
```xml
<data name="SpendingEvolution.Title" xml:space="preserve">
    <value>Spending Evolution</value>
</data>
<data name="SpendingEvolution.Menu" xml:space="preserve">
    <value>Spending Evolution</value>
</data>
<data name="SpendingEvolution.ContextMenu" xml:space="preserve">
    <value>Analyze Spending Evolution</value>
</data>
<data name="SpendingEvolution.Categories" xml:space="preserve">
    <value>Categories</value>
</data>
<data name="SpendingEvolution.TimeRange" xml:space="preserve">
    <value>Time Range:</value>
</data>
<data name="SpendingEvolution.Months" xml:space="preserve">
    <value>{0} months</value>
</data>
<data name="SpendingEvolution.CostOfLiving.Fiat" xml:space="preserve">
    <value>Cost of Living (Fiat)</value>
</data>
<data name="SpendingEvolution.CostOfLiving.Btc" xml:space="preserve">
    <value>Cost of Living (BTC)</value>
</data>
<data name="SpendingEvolution.Warning.MissingPriceData" xml:space="preserve">
    <value>Some transactions are missing BTC price data</value>
</data>
<data name="SpendingEvolution.NA" xml:space="preserve">
    <value>N/A</value>
</data>
```

**Portuguese (language.pt-BR.resx):**
```xml
<data name="SpendingEvolution.Title" xml:space="preserve">
    <value>Evolução de Gastos</value>
</data>
<data name="SpendingEvolution.Menu" xml:space="preserve">
    <value>Evolução de Gastos</value>
</data>
<data name="SpendingEvolution.ContextMenu" xml:space="preserve">
    <value>Analisar Evolução de Gastos</value>
</data>
<data name="SpendingEvolution.Categories" xml:space="preserve">
    <value>Categorias</value>
</data>
<data name="SpendingEvolution.TimeRange" xml:space="preserve">
    <value>Período:</value>
</data>
<data name="SpendingEvolution.Months" xml:space="preserve">
    <value>{0} meses</value>
</data>
<data name="SpendingEvolution.CostOfLiving.Fiat" xml:space="preserve">
    <value>Custo de Vida (Fiat)</value>
</data>
<data name="SpendingEvolution.CostOfLiving.Btc" xml:space="preserve">
    <value>Custo de Vida (BTC)</value>
</data>
<data name="SpendingEvolution.Warning.MissingPriceData" xml:space="preserve">
    <value>Algumas transações não possuem dados de preço em BTC</value>
</data>
<data name="SpendingEvolution.NA" xml:space="preserve">
    <value>N/A</value>
</data>
```

**Spanish (language.es.resx):**
```xml
<data name="SpendingEvolution.Title" xml:space="preserve">
    <value>Evolución de Gastos</value>
</data>
<data name="SpendingEvolution.Menu" xml:space="preserve">
    <value>Evolución de Gastos</value>
</data>
<data name="SpendingEvolution.ContextMenu" xml:space="preserve">
    <value>Analizar Evolución de Gastos</value>
</data>
<data name="SpendingEvolution.Categories" xml:space="preserve">
    <value>Categorías</value>
</data>
<data name="SpendingEvolution.TimeRange" xml:space="preserve">
    <value>Período:</value>
</data>
<data name="SpendingEvolution.Months" xml:space="preserve">
    <value>{0} meses</value>
</data>
<data name="SpendingEvolution.CostOfLiving.Fiat" xml:space="preserve">
    <value>Costo de Vida (Fiat)</value>
</data>
<data name="SpendingEvolution.CostOfLiving.Btc" xml:space="preserve">
    <value>Costo de Vida (BTC)</value>
</data>
<data name="SpendingEvolution.Warning.MissingPriceData" xml:space="preserve">
    <value>Algunas transaciones no tienen datos de precio en BTC</value>
</data>
<data name="SpendingEvolution.NA" xml:space="preserve">
    <value>N/A</value>
</data>
```

After updating all three .resx files, add corresponding static properties to language.Designer.cs following the existing naming convention (dots become underscores). Example:
```csharp
public static string SpendingEvolution_Title {
    get { return ResourceManager.GetString("SpendingEvolution.Title", resourceCulture); }
}
```
Add ALL 10 properties to Designer.cs.
  </action>
  <verify>
    <automated>grep -c "SpendingEvolution" src/Valt.UI/Lang/language.resx | grep -q "10"</automated>
  </verify>
  <done>
    - language.resx contains all 10 SpendingEvolution entries
    - language.pt-BR.resx contains all 10 entries with Portuguese translations
    - language.es.resx contains all 10 entries with Spanish translations
    - language.Designer.cs contains 10 new static properties for SpendingEvolution strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Update XAML and ViewModel to use localized strings</name>
  <files>
    src/Valt.UI/Views/Main/MainView.axaml
    src/Valt.UI/Views/Main/Tabs/Transactions/TransactionListView.axaml
    src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml
    src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs
  </files>
  <action>
Update all hardcoded English strings to use localized bindings:

1. **MainView.axaml** (line ~78):
   Change: `Header="Spending Evolution"`
   To: `Header="{x:Static local:language.SpendingEvolution_Menu}"`

2. **TransactionListView.axaml** (line ~211):
   Change: `Header="Analyze Spending Evolution"`
   To: `Header="{x:Static local:language.SpendingEvolution_ContextMenu}"`

3. **SpendingEvolutionView.axaml**:
   - Line 12 (Window Title): Change `Title="Spending Evolution"` to `Title="{x:Static local:language.SpendingEvolution_Title}"`
   - Line 31 (CustomTitleBar Title): Change `Title="Spending Evolution"` to `Title="{x:Static local:language.SpendingEvolution_Title}"`
   - Line 41 (Categories label): Change `Text="Categories"` to `Text="{x:Static local:language.SpendingEvolution_Categories}"`
   - Line 77 (Time Range label): Change `Text="Time Range:"` to `Text="{x:Static local:language.SpendingEvolution_TimeRange}"`
   - Line 85 (months suffix in ComboBox): This requires a different approach since it's inline binding. Change the ComboBox ItemTemplate to use a converter or binding. Simpler approach: keep the numeric value in the ComboBox and add a separate label, OR use a MultiBinding with StringFormat. Recommended: modify the ItemTemplate to reference a ViewModel property that formats the string using localization.
   - Line 109 (Cost of Living Fiat): Change `Text="Cost of Living (Fiat)"` to `Text="{x:Static local:language.SpendingEvolution_CostOfLiving_Fiat}"`
   - Line 120 (Cost of Living BTC): Change `Text="Cost of Living (BTC)"` to `Text="{x:Static local:language.SpendingEvolution_CostOfLiving_Btc}"`
   - Line 143 (Warning banner): Change `Text="Some transactions are missing BTC price data"` to `Text="{x:Static local:language.SpendingEvolution_Warning_MissingPriceData}"`

4. **SpendingEvolutionViewModel.cs**:
   - Line 59: Change `_fiatIncreasePercentText = "N/A"` to `_fiatIncreasePercentText = language.SpendingEvolution_NA`
   - Line 62: Change `_btcIncreasePercentText = "N/A"` to `_btcIncreasePercentText = language.SpendingEvolution_NA`
   - Line 238, 239, 260, 276: Change `"N/A"` to `language.SpendingEvolution_NA`
   - Add `using Valt.UI.Lang;` at the top if not present
   - For the months ComboBox display: Add a new read-only property `TimeRangeDisplayOptions` that returns formatted strings using `string.Format(language.SpendingEvolution_Months, months)` for each option, and bind the ComboBox to this instead of raw integers. Alternatively, create a `TimeRangeItem` class with `Months` and `DisplayText` properties.

   Recommended approach for months: Create a simple record/class inside ViewModel or as a nested type:
   ```csharp
   public record TimeRangeOption(int Months, string DisplayText);
   ```
   Replace `int[] TimeRangeOptions` with:
   ```csharp
   public TimeRangeOption[] TimeRangeOptions { get; } = new[]
   {
       new TimeRangeOption(12, string.Format(language.SpendingEvolution_Months, 12)),
       new TimeRangeOption(24, string.Format(language.SpendingEvolution_Months, 24)),
       new TimeRangeOption(36, string.Format(language.SpendingEvolution_Months, 36)),
       new TimeRangeOption(48, string.Format(language.SpendingEvolution_Months, 48)),
       new TimeRangeOption(60, string.Format(language.SpendingEvolution_Months, 60)),
   };
   ```
   Update `SelectedTimeRangeMonths` to bind to `TimeRangeOption.Months` via `SelectedValuePath="Months"` and display via `DisplayMemberPath="DisplayText"`.
  </action>
  <verify>
    <automated>
      # Verify no hardcoded English strings remain in SpendingEvolution files
      grep -n "Spending Evolution" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml || true
      grep -n "Categories" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml | grep -v "x:Static" || true
      grep -n "Time Range:" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml || true
      grep -n "Cost of Living" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml || true
      grep -n '"N/A"' src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionViewModel.cs || true
      grep -n "Some transactions are missing" src/Valt.UI/Views/Main/Modals/SpendingEvolution/SpendingEvolutionView.axaml || true
      grep -n "Analyze Spending Evolution" src/Valt.UI/Views/Main/Tabs/Transactions/TransactionListView.axaml || true
      grep -n "Spending Evolution" src/Valt.UI/Views/Main/MainView.axaml | grep -v "x:Static" || true
    </automated>
  </verify>
  <done>
    - MainView.axaml uses x:Static binding for Spending Evolution menu
    - TransactionListView.axaml uses x:Static binding for context menu
    - SpendingEvolutionView.axaml uses x:Static bindings for all labels, title, and warning
    - SpendingEvolutionViewModel.cs uses language.SpendingEvolution_NA instead of "N/A"
    - ComboBox displays localized month strings via TimeRangeOption
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Resource files | User-facing strings cross from code to external resource files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | .resx files | accept | Resource files contain only UI strings, no sensitive data |
| T-03-02 | Denial of Service | Missing resource key | mitigate | Verify all keys exist in all three .resx files before commit; runtime will throw MissingManifestResourceException if key missing |
</threat_model>

<verification>
1. Run `dotnet build Valt.sln` to confirm no compilation errors from localization changes
2. Verify all three .resx files contain identical keys
3. Verify language.Designer.cs has properties for all new keys
4. Verify no hardcoded English strings remain in SpendingEvolution XAML/ViewModel
</verification>

<success_criteria>
- All Spending Evolution UI strings are localized in en-US, pt-BR, and es
- Menu item, context menu, modal title, labels, and warning use x:Static bindings
- ViewModel uses localized "N/A" string
- Months ComboBox displays localized format
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-01-SUMMARY.md`
</output>
