---
phase: 41-wealth-performance-reports-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighReport.cs
  - src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighData.cs
  - src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
  - src/Valt.UI/Lang/language.resx
  - src/Valt.UI/Lang/language.Designer.cs
  - tests/Valt.Tests/Reports/AllTimeHighReportTests.cs
autonomous: true
requirements:
  - WLT-04
must_haves:
  truths:
    - The AllTimeHighReport computes gross wealth including active net-worth assets (IncludeInNetWorth = true, not sold).
    - AllTimeHighData exposes the number of days between the report end date and the all-time-high date.
    - The existing All Time High dashboard panel displays the new days-under-water row without a separate panel.
    - New user-facing strings are added in English only; pt-BR/es localization is deferred to Phase 43.
  artifacts:
    - src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighReport.cs
    - src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighData.cs
    - src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    - src/Valt.UI/Lang/language.resx
    - src/Valt.UI/Lang/language.Designer.cs
    - tests/Valt.Tests/Reports/AllTimeHighReportTests.cs
  key_links:
    - IAssetQueries -> AllTimeHighReport (constructor injection, per D-02/D-12).
    - AllTimeHighData.DaysUnderWater -> ReportsViewModel row formatting.
    - language.resx string -> ReportsViewModel row label.
---

<objective>
Deliver the Wave 1 vertical tracer for Phase 41: extend the existing All Time High report and panel so users can see how many days their gross wealth (accounts + active net-worth assets) has been under water. This plan implements WLT-04; the remaining WLT-01/02/03 requirements are planned for subsequent waves.

Purpose: Reuse the existing ATH infrastructure and asset definitions to surface a new long-term performance metric with minimal new UI surface.
Output: Asset-aware AllTimeHighReport, days-under-water data, and a new row in the existing ATH dashboard panel.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-wealth-performance-reports-ui/41-CONTEXT.md
@.planning/phases/40-btc-denominated-metrics-reports-ui/40-CONTEXT.md
@.planning/phases/39-spending-analytics-reports-ui/39-CONTEXT.md
@.claude/docs/reports.md
@AGENTS.md
@src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighReport.cs
@src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighData.cs
@src/Valt.Infra/Modules/Reports/AllTimeHigh/IAllTimeHighReport.cs
@src/Valt.App/Modules/Assets/Contracts/IAssetQueries.cs
@src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
@src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
@src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
@src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
@src/Valt.UI/Lang/language.resx
@src/Valt.UI/Lang/language.Designer.cs
@tests/Valt.Tests/Reports/AllTimeHighReportTests.cs
@tests/Valt.Tests/Builders/AssetBuilder.cs
@tests/Valt.Tests/Builders/FiatAccountBuilder.cs
@tests/Valt.Tests/Builders/BtcAccountBuilder.cs
@tests/Valt.Tests/Builders/TransactionBuilder.cs
</context>

<tasks>

<task type="tracer">
  <name>WLT-04 tracer: Make AllTimeHighReport asset-aware and expose days under water</name>
  <files>
    src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighReport.cs
    src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighData.cs
  </files>
  <read_first>
    src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighReport.cs
    src/Valt.Infra/Modules/Reports/AllTimeHigh/AllTimeHighData.cs
    src/Valt.Infra/Modules/Reports/AllTimeHigh/IAllTimeHighReport.cs
    src/Valt.App/Modules/Assets/Contracts/IAssetQueries.cs
    src/Valt.App/Modules/Assets/DTOs/AssetDTO.cs
    src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
  </read_first>
  <action>
    Add IAssetQueries to the AllTimeHighReport constructor (per the existing CQRS pattern, the Infra report can depend on an App-layer query contract). The DI container will resolve it automatically; no registration change is required because IAssetQueries is already registered.
    In the Calculator, fetch the active assets once per calculation via IAssetQueries.GetAllAsync, then filter Where(a => a.IncludeInNetWorth). For each scan date, add the value of every asset that is active on that date (CreatedAt date <= scan date and (DateSold is null or scan date <= DateSold)).
    Convert each asset's CurrentValue from its CurrencyCode to the report currency using the same source-currency -> USD -> target-currency path already used for account balances: BTC assets use provider.GetUsdBitcoinPriceAt; fiat assets use provider.GetFiatRateAt. This mirrors the dashboard's current-wealth definition and is the agreed baseline for this phase; document the approximation in a code comment.
    Add a DaysUnderWater property to AllTimeHighData computed as the number of whole days between the report's end date (yesterday) and the all-time-high date. Keep IAllTimeHighReport.GetAsync signature unchanged; only add new data properties.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
    <automated>dotnet test --filter "FullyQualifiedName~AllTimeHigh"</automated>
  </verify>
  <done>
    AllTimeHighReport includes active net-worth assets in the wealth calculation, AllTimeHighData carries DaysUnderWater, and the build plus the AllTimeHigh tests pass.
  </done>
  <acceptance_criteria>
    - AllTimeHighReport constructor accepts IAssetQueries and resolves through DI.
    - Daily gross wealth total includes active net-worth assets using the same currency conversion path as accounts.
    - DaysUnderWater is a non-negative integer representing days between the report end date and the ATH date.
    - Existing account-only behavior is preserved when no net-worth assets exist.
  </acceptance_criteria>
  <reversibility rating="costly" rationale="Including assets in the AllTimeHighReport changes the meaning of the ATH panel and all derived metrics (days under water, CAGR, best/worst months) for users with assets; reverting would alter reported values."/>
</task>

<task type="auto">
  <name>Wire days under water into the existing All Time High dashboard panel</name>
  <files>
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    src/Valt.UI/Lang/language.resx
    src/Valt.UI/Lang/language.Designer.cs
  </files>
  <read_first>
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsViewModel.cs
    src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml
    src/Valt.UI/Lang/language.resx
    src/Valt.UI/Lang/language.Designer.cs
  </read_first>
  <action>
    Add a new English-only string Reports_AllTimeHigh_DaysUnderWater to language.resx and regenerate language.Designer.cs so the static property is available (per D-14 English-only strings; pt-BR/es are deferred to Phase 43).
    In ReportsViewModel.FetchAllTimeHighDataAsync, add a new RowItem after the ATH date row that shows the days-under-water label and value from AllTimeHighData.DaysUnderWater. Keep all existing conditional rows (max drawdown, BTC price to hit ATH) unchanged and do not create a separate dashboard panel.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
    <automated>dotnet test --filter "FullyQualifiedName~ReportsViewModel"</automated>
  </verify>
  <done>
    The existing All Time High dashboard panel displays a new row showing the days under water.
  </done>
  <acceptance_criteria>
    - New language key exists in language.resx and language.Designer.cs.
    - ReportsViewModel.FetchAllTimeHighDataAsync adds a RowItem using the new key and AllTimeHighData.DaysUnderWater.
    - The row is rendered in the existing ATH panel with no separate panel.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify asset-aware ATH and days-under-water with tests</name>
  <files>
    tests/Valt.Tests/Reports/AllTimeHighReportTests.cs
  </files>
  <read_first>
    tests/Valt.Tests/Reports/AllTimeHighReportTests.cs
    tests/Valt.Tests/Builders/AssetBuilder.cs
    tests/Valt.Tests/Builders/FiatAccountBuilder.cs
    tests/Valt.Tests/Builders/BtcAccountBuilder.cs
    tests/Valt.Tests/Builders/TransactionBuilder.cs
  </read_first>
  <action>
    Update the existing AllTimeHighReportTests to pass a mock IAssetQueries (NSubstitute) to the new AllTimeHighReport constructor. Create tests that:
    (1) verify an active net-worth asset increases the reported ATH and changes the days-under-water calculation when current wealth is below the historical peak;
    (2) verify an asset with IncludeInNetWorth = false and a sold asset do not affect the ATH;
    (3) verify DaysUnderWater is zero when the peak occurs on the report end date and positive when the peak is earlier.
    Run the full test suite to confirm no regressions across the Reports module.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~AllTimeHigh"</automated>
    <automated>dotnet test</automated>
  </verify>
  <done>
    Tests cover active net-worth asset inclusion, exclusion of sold and non-net-worth assets, and correct days-under-water calculation.
  </done>
  <acceptance_criteria>
    - Existing AllTimeHighReportTests compile with the new constructor.
    - New tests verify asset inclusion/exclusion behavior.
    - New tests verify DaysUnderWater values for peak-on-end-date and peak-before-end-date scenarios.
    - Full test suite is green.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build Valt.sln` succeeds.
- `dotnet test` passes, including updated and new AllTimeHighReport tests.
- The Reports tab All Time High panel shows a new "Days under water" row that updates when the asset set changes.
</verification>

<success_criteria>
- The AllTimeHighReport calculates gross wealth using accounts plus active net-worth assets per D-02.
- The AllTimeHighData record exposes DaysUnderWater.
- The existing ATH dashboard panel renders the new metric without a separate panel per D-10.
- New strings are English-only per D-14.
- All tests pass and the build is green.
</success_criteria>

<output>
Create `.planning/phases/41-wealth-performance-reports-ui/41-01-SUMMARY.md` when done.
</output>
