---
phase: 37-mcp-server-page-update
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - ../valt-docs/docs/funcionalidades/mcp-server.en.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "The English page mirrors every structural addition made to the Portuguese page in plan 37-01: the AssetTools section (5 H4 subsections, 28 tools, 5 parameter tables, 1 no-parameters note), the IndicatorTools section, the 2 tool-count updates, and the Assets next-steps link."
    - "English headings are `### Assets (AssetTools)` with H4s `#### Asset Operations`, `#### BTC Loans`, `#### Asset Groups`, `#### Loan State Timeline`, `#### Sold Assets`, and `### Indicators (IndicatorTools)`."
    - "English table headers are `| Tool | Description |` and `| Parameter | Description |`; parameter labels are `**Parameters of \`ToolName\`:**`."
    - "English descriptions track the code `[Description]` attribute text closely (D-05; canonical list in 37-PATTERNS.md §Shared Patterns); tool and parameter names stay as English backticked identifiers in both languages."
    - "`ListSoldAssets` is documented with the note that it takes no parameters."
    - "The English page claims `over 80 tools` in exactly 2 spots (intro line 3, Available Tools intro line 133); the stale 45-tool claim is gone."
    - "The `## Next Steps` section includes a link to `ativos.md`."
    - "The `<!-- Source: ... -->` comments appear at identical positions in both files (comments are not translated)."
    - "Bilingual parity: heading counts (`grep -c '^#'`) and table-row counts (`grep -c '^|'`) are equal between `mcp-server.md` and `mcp-server.en.md` (QA-01)."
    - "`mkdocs build --strict` inside the valt-docs `.venv` exits 0 with no ERROR or WARNING lines (QA-02)."
    - "REQUIREMENTS.md marks MCP-01, MCP-02, and MCP-03 as `- [x]` and `Complete` in the traceability table, updated only after the build passes."
  artifacts:
    - path: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      provides: "English AssetTools category section mirroring the Portuguese one"
      contains: "### Assets (AssetTools)"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      provides: "English loan-state timeline subsection with parameter tables (MCP-02)"
      contains: "#### Loan State Timeline"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      provides: "English sold-assets subsection with parameter tables (MCP-03)"
      contains: "#### Sold Assets"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      provides: "English IndicatorTools section (D-06)"
      contains: "### Indicators (IndicatorTools)"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      provides: "Corrected tool-count claim (Q1)"
      contains: "over 80 tools"
    - path: .planning/REQUIREMENTS.md
      provides: "Updated MCP-01, MCP-02, MCP-03 statuses"
      contains: "- [x] **MCP-01**"
  key_links:
    - from: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      to: ../valt-docs/docs/funcionalidades/mcp-server.md
      via: "English page mirrors Portuguese page structure, row counts, and source comments; only prose is translated (QA-01)"
    - from: ../valt-docs/docs/funcionalidades/mcp-server.en.md
      to: src/Valt.Infra/Mcp/Tools/AssetTools.cs
      via: "English tool/parameter names and description semantics track the [Description] attributes per D-05"
      pattern: "totalBorrowed|interestAccruedUntilDate|collateralSats|dateSold"
    - from: .planning/REQUIREMENTS.md
      to: ../valt-docs/docs/funcionalidades/mcp-server.md
      via: "MCP statuses flipped to Complete only after the bilingual docs pass the strict build gate"
---

<objective>
Mirror the Portuguese MCP Server page updates to the English translation (`mcp-server.en.md`), verify bilingual parity, run the `mkdocs build --strict` gate inside the valt-docs `.venv`, and flip MCP-01/02/03 to Complete in REQUIREMENTS.md.

Purpose: English readers see the same complete toolset (AssetTools, loan-state tools with parameters, sold-asset tools with parameters, IndicatorTools); the documentation site remains error-free; project requirements reflect the completed work.
Output: Updated `../valt-docs/docs/funcionalidades/mcp-server.en.md`, a green strict MkDocs build, and updated `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-mcp-server-page-update/37-CONTEXT.md
@.planning/phases/37-mcp-server-page-update/37-RESEARCH.md
@.planning/phases/37-mcp-server-page-update/37-PATTERNS.md
@../valt-docs/docs/funcionalidades/mcp-server.md
@../valt-docs/docs/funcionalidades/mcp-server.en.md
@src/Valt.Infra/Mcp/Tools/AssetTools.cs
@src/Valt.Infra/Mcp/Tools/IndicatorTools.cs
</context>

<tasks>

<task>
  <id>37-02-01</id>
  <name>Mirror all Portuguese additions to the English MCP Server page</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/mcp-server.en.md</files>
  <read_first>
    - .planning/phases/37-mcp-server-page-update/37-CONTEXT.md
    - .planning/phases/37-mcp-server-page-update/37-RESEARCH.md
    - .planning/phases/37-mcp-server-page-update/37-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/mcp-server.md
    - ../valt-docs/docs/funcionalidades/mcp-server.en.md
    - src/Valt.Infra/Mcp/Tools/AssetTools.cs
    - src/Valt.Infra/Mcp/Tools/IndicatorTools.cs
  </read_first>
  <action>
    Apply to `../valt-docs/docs/funcionalidades/mcp-server.en.md` the exact mirror of the three additions plan 37-01 made to the Portuguese page (read the updated PT file first — it is the structural spec). Concretely:

    1. Insert `### Assets (AssetTools)` after the `### Currencies (CurrencyTools)` table and before `## Use Cases 💡`, with the same `<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->` comment directly under the heading (comments are not translated). Add five H4 subsections with these exact English headings: `#### Asset Operations` (12 tools), `#### BTC Loans` (3 tools), `#### Asset Groups` (6 tools), `#### Loan State Timeline` (4 tools + parameter tables), `#### Sold Assets` (3 tools + parameter tables). Use table header `| Tool | Description |`. The 28 backticked tool identifiers are identical to the PT page (GetAssets, GetVisibleAssets, GetAsset, GetAssetsSummary, CreateBasicAsset, CreateRealEstateAsset, CreateLeveragedPosition, UpdateAssetPrice, UpdateAssetQuantity, ToggleAssetVisibility, ToggleAssetNetWorthInclusion, DeleteAsset, CreateBtcLoan, CreateBtcLending, RepayLoan, GetAssetGroups, CreateAssetGroup, UpdateAssetGroup, DeleteAssetGroup, MoveAssetToGroup, RemoveAssetFromGroup, AddLoanStateUpdate, DeleteLoanStateUpdate, GetLoanStateTimeline, GetLatestLoanState, MarkAssetAsSold, UndoAssetSale, ListSoldAssets). English descriptions must track the canonical `[Description]` attribute text listed in 37-PATTERNS.md §Shared Patterns (e.g. `CreateBtcLoan` → "Create a BTC-collateralized loan (borrowing fiat against BTC collateral); tracks LTV, health status, and accrued interest; supports daily APR or a fixed total debt (HodlHodl-style)"); RESEARCH §Code Example 5 gives the conventions. Include the same two 1-sentence subsection intros translated to English (loan snapshot timeline; sold-asset hide-and-preserve-visibility behavior).
    2. Below the Loan State Timeline table add three labeled parameter tables with header `| Parameter | Description |`: `**Parameters of \`AddLoanStateUpdate\`:**` with rows `assetId` (The asset ID), `effectiveDate` (Effective date, yyyy-MM-dd), `totalBorrowed` (Borrowed principal still owed at the snapshot date), `interestAccruedUntilDate` (Interest accrued up to the snapshot date), `collateralSats` (BTC collateral in satoshis), `apr` (APR as decimal, e.g., 0.12 for 12%), `fees` (Fees paid), `note` (Optional note); `**Parameters of \`DeleteLoanStateUpdate\`:**` with rows `assetId`, `effectiveDate`; and one shared table `**Parameters of \`GetLoanStateTimeline\` and \`GetLatestLoanState\`:**` with the single row `assetId`.
    3. Below the Sold Assets table add `**Parameters of \`MarkAssetAsSold\`:**` with rows `assetId` (The ID of the asset to mark as sold) and `dateSold` (Sale date in yyyy-MM-dd format, optional, defaults to today); `**Parameters of \`UndoAssetSale\`:**` with row `assetId` (The ID of the sold asset to restore); and the one-line note `` `ListSoldAssets` takes no parameters. `` instead of an empty table.
    4. Insert `### Indicators (IndicatorTools)` after the Sold Assets subsection and before `## Use Cases 💡`, with the same `<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->` comment, a 1-line intro (Bitcoin macro indicators refreshed in the background and served from cache), and a single-row table: `GetBitcoinIndicators` → "Gets current Bitcoin macro indicators: Mayer Multiple, Rainbow Chart, Fear & Greed Index, and Bitcoin Dominance" (RESEARCH §Code Example 4 has the ready EN block).
    5. Update the two stale tool-count claims (Q1): in the intro paragraph (line 3) change the count phrase to `over 80 tools` preserving the rest of the sentence; in the `## Available Tools 🛠️` intro (line 133) change the sentence to `Valt's MCP server exposes over 80 tools organized by category.`
    6. In `## Next Steps` (Q2), append one bullet after the `Average Price` line: `- [Assets](ativos.md) - External investments, BTC loans, and sold asset history` (wording is the agent's discretion; link target must be `ativos.md`).

    Do NOT touch the 8 pre-existing English category sections (D-04 — drifted names stay as-is for Phase 38 QA). Ensure the English page's heading structure, table row counts, and source-comment positions match the updated Portuguese page exactly (QA-01; Phase 36 mirror discipline — every prose change is mirrored, not just structural additions).
  </action>
  <acceptance_criteria>
    - The English file contains `### Assets (AssetTools)` with the five H4 headings `#### Asset Operations`, `#### BTC Loans`, `#### Asset Groups`, `#### Loan State Timeline`, `#### Sold Assets`, in that order, positioned after `### Currencies (CurrencyTools)` and before `## Use Cases 💡`.
    - All 28 AssetTools identifiers plus `GetBitcoinIndicators` appear backticked in the English file.
    - Exactly 5 bold parameter-table labels exist (lines starting `**Parameters of`), covering the same 7 tools as the PT page; the parameter names `assetId`, `effectiveDate`, `totalBorrowed`, `interestAccruedUntilDate`, `collateralSats`, `apr`, `fees`, `note`, `dateSold` all appear backticked.
    - The note stating `ListSoldAssets` takes no parameters is present.
    - The English file contains `### Indicators (IndicatorTools)` mentioning Mayer Multiple, Rainbow Chart, and Fear & Greed Index.
    - Both `<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->` and `<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->` appear in the English file.
    - The phrase `over 80 tools` appears exactly twice and the previous "over 45 ..." count phrasing no longer appears.
    - The `## Next Steps` list contains a bullet linking to `ativos.md`.
    - Parity: `grep -c '^#'` and `grep -c '^|'` return equal numbers for `mcp-server.md` and `mcp-server.en.md`.
  </acceptance_criteria>
  <verify>
    <automated>
      PT=../valt-docs/docs/funcionalidades/mcp-server.md && EN=../valt-docs/docs/funcionalidades/mcp-server.en.md && \
      grep -q '^### Assets (AssetTools)' "$EN" && \
      grep -q '^#### Asset Operations' "$EN" && \
      grep -q '^#### BTC Loans' "$EN" && \
      grep -q '^#### Asset Groups' "$EN" && \
      grep -q '^#### Loan State Timeline' "$EN" && \
      grep -q '^#### Sold Assets' "$EN" && \
      grep -q '^### Indicators (IndicatorTools)' "$EN" && \
      for t in GetAssets GetVisibleAssets GetAsset GetAssetsSummary CreateBasicAsset CreateRealEstateAsset CreateLeveragedPosition UpdateAssetPrice UpdateAssetQuantity ToggleAssetVisibility ToggleAssetNetWorthInclusion DeleteAsset CreateBtcLoan CreateBtcLending RepayLoan GetAssetGroups CreateAssetGroup UpdateAssetGroup DeleteAssetGroup MoveAssetToGroup RemoveAssetFromGroup AddLoanStateUpdate DeleteLoanStateUpdate GetLoanStateTimeline GetLatestLoanState MarkAssetAsSold UndoAssetSale ListSoldAssets GetBitcoinIndicators; do grep -q "\`$t\`" "$EN" || { echo "MISSING TOOL: $t"; exit 1; }; done && \
      for p in assetId effectiveDate totalBorrowed interestAccruedUntilDate collateralSats apr fees note dateSold; do grep -q "\`$p\`" "$EN" || { echo "MISSING PARAM: $p"; exit 1; }; done && \
      test "$(grep -c '^\*\*Parameters of' "$EN")" -eq 5 && \
      grep -q 'takes no parameters' "$EN" && \
      grep -qF '<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->' "$EN" && \
      grep -qF '<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->' "$EN" && \
      test "$(grep -c 'over 80 tools' "$EN")" -eq 2 && \
      ! grep -q 'over 45 tools' "$EN" && \
      grep -q '](ativos.md)' "$EN" && \
      test "$(grep -c '^#' "$PT")" -eq "$(grep -c '^#' "$EN")" && \
      test "$(grep -c '^|' "$PT")" -eq "$(grep -c '^|' "$EN")"
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The English MCP Server page mirrors all Portuguese additions — AssetTools (28 tools, 5 subsections, parameter tables), IndicatorTools, the 80+ count fix, and the Assets next-steps link — with full bilingual parity (MCP-01/02/03, QA-01).</done>
</task>

<task>
  <id>37-02-02</id>
  <name>Verify the valt-docs site builds with MkDocs strict mode</name>
  <type>verify</type>
  <files>../valt-docs/mkdocs.yml, ../valt-docs/docs/funcionalidades/mcp-server.md, ../valt-docs/docs/funcionalidades/mcp-server.en.md</files>
  <read_first>
    - ../valt-docs/mkdocs.yml
    - ../valt-docs/docs/funcionalidades/mcp-server.md
    - ../valt-docs/docs/funcionalidades/mcp-server.en.md
  </read_first>
  <action>
    Activate the existing valt-docs virtual environment and run `mkdocs build --strict` in the sibling repository: `cd /home/vmabellini/RiderProjects/valt-docs && . .venv/bin/activate && mkdocs build --strict`. The `.venv` activation is MANDATORY — the pipx mkdocs on PATH lacks the Material theme and the system `python3 -m mkdocs` lacks the i18n plugin (RESEARCH Pitfall 1); do not attempt either fallback and do not install any packages. If the build fails with errors or warnings, fix the Markdown in the two updated MCP Server pages (most likely causes: malformed tables, broken admonition indentation, bad internal links). Do not modify `mkdocs.yml` unless the build error is genuinely a configuration issue. If the `.venv` is missing, report it as a blocker instead of installing packages.
  </action>
  <acceptance_criteria>
    - `cd /home/vmabellini/RiderProjects/valt-docs && . .venv/bin/activate && mkdocs build --strict` exits with code 0.
    - The build output contains no `ERROR` or `WARNING` lines.
    - The generated `../valt-docs/site/index.html` exists.
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/vmabellini/RiderProjects/valt-docs && . .venv/bin/activate && mkdocs build --strict 2>&1 | tee /tmp/mkdocs-build-37-02.log && \
      ! grep -iE "ERROR|WARNING" /tmp/mkdocs-build-37-02.log && \
      test -f ../valt-docs/site/index.html
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The valt-docs site builds cleanly with `mkdocs build --strict` after the bilingual MCP Server page updates (QA-02).</done>
</task>

<task>
  <id>37-02-03</id>
  <name>Update REQUIREMENTS.md to mark MCP-01, MCP-02, and MCP-03 as Complete</name>
  <type>docs</type>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Run this task only after task 37-02-02's build gate is green. Edit `.planning/REQUIREMENTS.md` in the main valt repository and flip the three MCP requirements to complete, copying the exact style used by the completed FXE rows. In the `### Feature Coverage — MCP Server` section, change the three checkboxes from `- [ ]` to `- [x]` for MCP-01, MCP-02, and MCP-03 (currently lines 42-44). In the Traceability table, change the status column from `Pending` to `Complete` for the rows `| MCP-01 | Phase 37 | Pending |`, `| MCP-02 | Phase 37 | Pending |`, `| MCP-03 | Phase 37 | Pending |` (currently lines 96-98). Leave NAV-01/02/03 as `Pending` (Phase 38 scope) and preserve all other formatting.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows `- [x] **MCP-01**`, `- [x] **MCP-02**`, and `- [x] **MCP-03**` in the Feature Coverage — MCP Server section.
    - The Traceability table shows `Complete` for MCP-01, MCP-02, and MCP-03 in Phase 37 rows.
    - NAV-01, NAV-02, and NAV-03 remain `- [ ]` and `Pending`.
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q '\- \[x\] \*\*MCP-01\*\*' .planning/REQUIREMENTS.md && \
      grep -q '\- \[x\] \*\*MCP-02\*\*' .planning/REQUIREMENTS.md && \
      grep -q '\- \[x\] \*\*MCP-03\*\*' .planning/REQUIREMENTS.md && \
      grep -q '| MCP-01 | Phase 37 | Complete |' .planning/REQUIREMENTS.md && \
      grep -q '| MCP-02 | Phase 37 | Complete |' .planning/REQUIREMENTS.md && \
      grep -q '| MCP-03 | Phase 37 | Complete |' .planning/REQUIREMENTS.md && \
      grep -q '| NAV-01 | Phase 38 | Pending |' .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>REQUIREMENTS.md reflects that MCP-01, MCP-02, and MCP-03 are Complete, with NAV items untouched for Phase 38.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs ←→ Valt application code | English descriptions must track the same code `[Description]` attributes as the Portuguese page; no invented semantics. |
| Portuguese source ←→ English mirror | Every structural change from plan 37-01 must appear in the `.en.md` file with identical heading/table skeleton (QA-01). |
| Requirements file ←→ Documentation artifacts | MCP statuses flip to Complete only after the strict build passes. |
| Build environment | Only the `valt-docs/.venv` mkdocs is trusted; the two broken system installs must not be used or "fixed" with installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02-01 | Tampering (integrity of guidance) | English MCP Server mirror | High | Mitigate | Mirror the finalized PT structure exactly; EN descriptions track the canonical `[Description]` list in 37-PATTERNS.md; automated parity checks (heading counts, table-row counts, 29 tool greps, 9 parameter greps) plus the strict build gate. |
| T-37-02-02 | Tampering | REQUIREMENTS.md status update | Medium | Mitigate | Task 37-02-03 is explicitly sequenced after the green build gate; acceptance criteria verify NAV rows remain Pending so scope cannot bleed into Phase 38. |
| T-37-02-03 | Information Disclosure | Public docs pages | Low | Accept | All content is already public in the open-source repo; no secrets or PII. The pre-existing localhost-only security admonition is preserved. |
| T-37-02-04 | Tampering | Build environment substitution | Medium | Mitigate | The verify task forbids pipx/system mkdocs fallbacks and forbids package installs; a missing `.venv` is a reported blocker, not an install opportunity. |
| T-37-02-SC | Tampering | pip/mkdocs installs | Low | N/A | No new packages are installed. The build uses the existing `valt-docs/.venv` virtual environment (Package Legitimacy Audit: not applicable — zero installs this phase). |
</threat_model>

<verification>
- The English page mirrors all three Portuguese additions with identical heading/table structure (QA-01 parity greps pass).
- `mkdocs build --strict` inside `valt-docs/.venv` exits 0 with no ERROR/WARNING lines (QA-02).
- REQUIREMENTS.md shows MCP-01/02/03 as `[x]` and `Complete`; NAV items remain Pending.
</verification>

<success_criteria>
- An English reader can see the AssetTools category, the 4 loan-state tools with parameters, and the 3 sold-asset tools with parameters (MCP-01, MCP-02, MCP-03 in EN).
- The documentation site builds without errors or warnings after the bilingual updates.
- The project requirements accurately reflect phase completion.
</success_criteria>

<output>
Create `.planning/phases/37-mcp-server-page-update/37-02-SUMMARY.md` when done.
</output>

## Context Decisions Addressed

- **D-01 / D-02 / D-03** (Task 37-02-01): English mirror preserves the AssetTools placement, the 5-subsection grouping, and the labeled parameter tables for exactly the 7 required tools.
- **D-04** (Task 37-02-01): The 8 pre-existing English category sections are not modified.
- **D-05** (Task 37-02-01): English parameter names and description semantics track `AssetTools.cs` `[Description]` attributes (canonical list in 37-PATTERNS.md).
- **D-06** (Task 37-02-01): `### Indicators (IndicatorTools)` mirrors the PT IndicatorTools section.
- **Q1 / Q2 resolutions** (Task 37-02-01): `over 80 tools` in both spots; `Assets` link in Next Steps.
- **QA-01** (Task 37-02-01): Full bilingual mirror with automated parity checks.
- **QA-02** (Task 37-02-02): Strict build gate in the mandatory `.venv`.

## Deferred / Out of Scope (for Phase 38 QA)

- **Q3 / RESEARCH Open Question 3:** Pre-existing category name drift is NOT fixed in this phase per D-04. Documented names that differ from code: GoalTools (docs list 8 incl. `CreateDCAGoal`; code has 13 incl. `CreateDcaGoal`), AvgPriceTools (docs 11 incl. `GetAvgPriceProfiles`; code 12 incl. `GetProfile`), ReportTools (docs 6 incl. `GetWealthHistory`; code 7 incl. `GetAllTimeHigh`/`GetMaxBtcStack`/`GetStatistics`), AccountTools (docs `CreateAccount`; code `CreateFiatAccount`/`CreateBtcAccount`), TransactionTools (docs list `AddBitcoinToBitcoinTransfer`; code has `DeleteTransaction`). Phase 38 (QA) should decide whether to correct these.
- Retrofitting parameter tables onto the 8 pre-existing categories (deferred from CONTEXT.md).
- Screenshots; translation to languages beyond pt-BR/en-US.

## Two-Repository Protocol

- Doc changes live in the sibling `valt-docs` repo: commit there with `git -C /home/vmabellini/RiderProjects/valt-docs` using the prefix `docs(37-02): ...` (matching `docs(36-02):` history).
- Planning artifacts (`.planning/REQUIREMENTS.md`, SUMMARY) live in the main `valt` repo — REQUIREMENTS.md is already tracked; any new `.planning/` files require `git add -f` (the path is gitignored).
- Never mix both repos' files in one commit.
- The final docs verification gate is `cd ../valt-docs && . .venv/bin/activate && mkdocs build --strict` (the `.venv` activation is MANDATORY — pipx/system mkdocs fail per RESEARCH Pitfall 1).

## Artifacts this phase produces

**New files:**
- None.

**Modified files:**
- `../valt-docs/docs/funcionalidades/mcp-server.en.md` — English MCP Server page: new `### Assets (AssetTools)` section (5 H4 subsections, 28 tool rows, 5 labeled parameter tables, 1 no-parameters note, source-evidence comment), new `### Indicators (IndicatorTools)` section, tool-count claim updated to `over 80 tools` in 2 spots, new `Assets` bullet in `## Next Steps`.
- `.planning/REQUIREMENTS.md` — MCP-01, MCP-02, MCP-03 checkboxes flipped to `[x]` and traceability rows flipped to `Complete`.

**Symbols created (doc anchors / headings):**
- `### Assets (AssetTools)` with `#### Asset Operations`, `#### BTC Loans`, `#### Asset Groups`, `#### Loan State Timeline`, `#### Sold Assets` (in `mcp-server.en.md`)
- `### Indicators (IndicatorTools)` (in `mcp-server.en.md`)
- 5 parameter-table labels: `**Parameters of \`AddLoanStateUpdate\`:**`, `**Parameters of \`DeleteLoanStateUpdate\`:**`, `**Parameters of \`GetLoanStateTimeline\` and \`GetLatestLoanState\`:**`, `**Parameters of \`MarkAssetAsSold\`:**`, `**Parameters of \`UndoAssetSale\`:**`
