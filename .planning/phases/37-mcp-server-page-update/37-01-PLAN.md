---
phase: 37-mcp-server-page-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../valt-docs/docs/funcionalidades/mcp-server.md
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "D-01: The Portuguese page contains an `### Ativos (AssetTools)` section inside `## Ferramentas Disponíveis 🛠️`, positioned after `### Moedas (CurrencyTools)` and before `## Casos de Uso 💡`."
    - "D-02: The AssetTools section is split into five H4 grouped subsections (Operações de Ativos, Empréstimos BTC, Grupos de Ativos, Linha do Tempo do Estado do Empréstimo, Ativos Vendidos), each keeping the page's 2-column `| Ferramenta | Descrição |` table style."
    - "All 28 AssetTools names appear as backticked identifiers in the Portuguese page, including `GetAsset` (corrected 28-tool inventory per RESEARCH Pitfall 2; CONTEXT's 26 figure is stale)."
    - "D-03: Exactly the 7 required tools have parameter tables — `AddLoanStateUpdate`, `DeleteLoanStateUpdate`, `GetLoanStateTimeline`, `GetLatestLoanState`, `MarkAssetAsSold`, `UndoAssetSale`, `ListSoldAssets` — as labeled `| Parâmetro | Descrição |` tables below their group tables, never nested inside table rows."
    - "D-05: Parameter names and optionality match `src/Valt.Infra/Mcp/Tools/AssetTools.cs` signatures verbatim: `assetId`, `effectiveDate`, `totalBorrowed`, `interestAccruedUntilDate`, `collateralSats`, `apr`, `fees`, `note` (optional), `dateSold` (optional, defaults to today)."
    - "`ListSoldAssets` is documented as taking no parameters with a one-line note instead of an empty table."
    - "D-04: The 8 pre-existing categories and the 21 non-required AssetTools keep the plain 2-column format with no parameter tables; no drifted names in pre-existing categories are touched."
    - "D-06: The Portuguese page contains a `### Indicadores (IndicatorTools)` section documenting `GetBitcoinIndicators` (Mayer Multiple, Rainbow Chart, Fear & Greed Index, Bitcoin Dominance)."
    - "Q1: The Portuguese page claims `mais de 80 ferramentas` in both the intro (line 3) and the Ferramentas Disponíveis intro (line 133); the stale 45-tool claim is gone."
    - "Q2: The `## Próximos Passos` section includes a link to `ativos.md`."
    - "Source-evidence comments `<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->` and `<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->` sit directly under the two new H3 headings (Phases 32-34 convention)."
  artifacts:
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "AssetTools category section with 5 grouped subsections and 28 tools"
      contains: "### Ativos (AssetTools)"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "Loan-state timeline subsection with parameter tables (MCP-02)"
      contains: "#### Linha do Tempo do Estado do Empréstimo"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "Sold-assets subsection with parameter tables (MCP-03)"
      contains: "#### Ativos Vendidos"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "Loan-state parameter documentation copied from code"
      contains: "`totalBorrowed`"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "IndicatorTools category section (D-06)"
      contains: "### Indicadores (IndicatorTools)"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "Corrected tool-count claim (Q1)"
      contains: "mais de 80 ferramentas"
    - path: ../valt-docs/docs/funcionalidades/mcp-server.md
      provides: "Ativos cross-link in Próximos Passos (Q2)"
      contains: "](ativos.md)"
  key_links:
    - from: ../valt-docs/docs/funcionalidades/mcp-server.md
      to: src/Valt.Infra/Mcp/Tools/AssetTools.cs
      via: "Tool names, parameter names, optionality, and description semantics copied verbatim from [Description] attributes per D-05"
      pattern: "totalBorrowed|interestAccruedUntilDate|collateralSats|dateSold"
    - from: ../valt-docs/docs/funcionalidades/mcp-server.md
      to: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs
      via: "GetBitcoinIndicators description mirrors the tool's [Description] attribute"
      pattern: "GetBitcoinIndicators"
    - from: ../valt-docs/docs/funcionalidades/mcp-server.md
      to: ../valt-docs/docs/funcionalidades/ativos.md
      via: "Próximos Passos cross-link to the Assets feature page (Q2)"
      pattern: "\\]\\(ativos\\.md\\)"
---

<objective>
Update the Portuguese MCP Server public documentation page (`../valt-docs/docs/funcionalidades/mcp-server.md`) so it lists the complete current toolset: add the missing AssetTools category (28 tools in 5 grouped subsections, with parameter tables for the 4 loan-state tools and 3 sold-asset tools), add the IndicatorTools category, correct the stale tool-count claim, and add an Ativos cross-link to Próximos Passos.

Purpose: Portuguese readers (and the AI agents they configure) can see the complete current MCP toolset, including AssetTools, the loan-state timeline tools, and the sold-asset tools with their exact parameters (MCP-01, MCP-02, MCP-03).
Output: An updated `../valt-docs/docs/funcionalidades/mcp-server.md` ready to be mirrored to English in plan 37-02.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vmabellini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-mcp-server-page-update/37-CONTEXT.md
@.planning/phases/37-mcp-server-page-update/37-RESEARCH.md
@.planning/phases/37-mcp-server-page-update/37-PATTERNS.md
@.claude/docs/assets.md
@../valt-docs/docs/funcionalidades/mcp-server.md
@src/Valt.Infra/Mcp/Tools/AssetTools.cs
@src/Valt.Infra/Mcp/Tools/IndicatorTools.cs
</context>

<tasks>

<task>
  <id>37-01-01</id>
  <name>Add AssetTools section with 5 grouped subsections and parameter tables for the 7 required tools</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/mcp-server.md</files>
  <read_first>
    - .planning/phases/37-mcp-server-page-update/37-CONTEXT.md
    - .planning/phases/37-mcp-server-page-update/37-RESEARCH.md
    - .planning/phases/37-mcp-server-page-update/37-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/mcp-server.md
    - src/Valt.Infra/Mcp/Tools/AssetTools.cs
    - .claude/docs/assets.md
  </read_first>
  <action>
    Verify that ../valt-docs/mkdocs.yml is readable; abort and report if the sibling repository is missing. Edit ../valt-docs/docs/funcionalidades/mcp-server.md and insert a new H3 section `### Ativos (AssetTools)` immediately after the end of the `### Moedas (CurrencyTools)` table (currently ends at line 225) and before `## Casos de Uso 💡` (currently line 227). Directly under the heading place the source-evidence comment `<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->` and a 1-line purpose paragraph (RESEARCH.md Code Example 1 has the ready wording). Then add five H4 subsections, each with the page's standard 2-column table header `| Ferramenta | Descrição |`, in this order, using the copy-paste-grade content blocks in 37-RESEARCH.md §Code Examples 1-3:

    1. `#### Operações de Ativos` — exactly these 12 tools as backticked rows: `GetAssets`, `GetVisibleAssets`, `GetAsset`, `GetAssetsSummary`, `CreateBasicAsset`, `CreateRealEstateAsset`, `CreateLeveragedPosition`, `UpdateAssetPrice`, `UpdateAssetQuantity`, `ToggleAssetVisibility`, `ToggleAssetNetWorthInclusion`, `DeleteAsset`.
    2. `#### Empréstimos BTC` — `CreateBtcLoan`, `CreateBtcLending`, `RepayLoan`.
    3. `#### Grupos de Ativos` — `GetAssetGroups`, `CreateAssetGroup`, `UpdateAssetGroup`, `DeleteAssetGroup`, `MoveAssetToGroup`, `RemoveAssetFromGroup`.
    4. `#### Linha do Tempo do Estado do Empréstimo` — `AddLoanStateUpdate`, `DeleteLoanStateUpdate`, `GetLoanStateTimeline`, `GetLatestLoanState`, preceded by a 1-sentence intro explaining that each BTC-backed loan keeps a timeline of state snapshots and current calculations always use the latest snapshot (wording in RESEARCH Example 2). Below this group's table add three labeled parameter tables with header `| Parâmetro | Descrição |`, each introduced by a bold label line: `**Parâmetros de \`AddLoanStateUpdate\`:**` with rows `assetId` (O ID do ativo), `effectiveDate` (Data efetiva yyyy-MM-dd), `totalBorrowed` (principal tomado ainda devido na data do snapshot), `interestAccruedUntilDate` (juros acumulados até a data do snapshot), `collateralSats` (colateral em BTC, em satoshis), `apr` (APR como decimal, ex.: 0,12 para 12%), `fees` (taxas pagas), `note` (observação, opcional); `**Parâmetros de \`DeleteLoanStateUpdate\`:**` with rows `assetId`, `effectiveDate`; and one shared table `**Parâmetros de \`GetLoanStateTimeline\` e \`GetLatestLoanState\`:**` with the single row `assetId`.
    5. `#### Ativos Vendidos` — `MarkAssetAsSold`, `UndoAssetSale`, `ListSoldAssets`, preceded by a 1-sentence intro explaining that marking an asset as sold hides it from the active list and moves it to the history, preserving its previous visibility state for restoration (wording in RESEARCH Example 3). Below the group table add `**Parâmetros de \`MarkAssetAsSold\`:**` with rows `assetId` (ativo a marcar como vendido) and `dateSold` (data da venda em yyyy-MM-dd, opcional, padrão: hoje); `**Parâmetros de \`UndoAssetSale\`:**` with row `assetId` (ativo vendido a restaurar); and the one-line note `` `ListSoldAssets` não recebe parâmetros. `` in place of an empty table.

    Parameter names, optionality, and semantics MUST come from the C# signatures and `[Description]` attributes in `src/Valt.Infra/Mcp/Tools/AssetTools.cs` lines 626-794 (D-05) — RESEARCH §Code Examples already extracted them; translate prose to Portuguese but never invent semantics. Parameter tables must sit BELOW the group tables with bold labels — never nested inside a `| Ferramenta | Descrição |` row (markdown cannot nest block tables; RESEARCH Pitfall 3). Do not add parameter tables to any of the other 21 AssetTools rows or to any of the 8 pre-existing categories (D-04). Do not rename or otherwise touch any pre-existing category row (e.g. `CreateDCAGoal`, `GetAvgPriceProfiles`) — that drift is logged for Phase 38 QA, out of scope here.
  </action>
  <acceptance_criteria>
    - The file contains the heading `### Ativos (AssetTools)` positioned after `### Moedas (CurrencyTools)` and before `## Casos de Uso 💡`.
    - The file contains the five H4 headings `#### Operações de Ativos`, `#### Empréstimos BTC`, `#### Grupos de Ativos`, `#### Linha do Tempo do Estado do Empréstimo`, `#### Ativos Vendidos`, in that order.
    - All 28 tool names appear as backticked identifiers: GetAssets, GetVisibleAssets, GetAsset, GetAssetsSummary, CreateBasicAsset, CreateRealEstateAsset, CreateLeveragedPosition, UpdateAssetPrice, UpdateAssetQuantity, ToggleAssetVisibility, ToggleAssetNetWorthInclusion, DeleteAsset, CreateBtcLoan, CreateBtcLending, RepayLoan, GetAssetGroups, CreateAssetGroup, UpdateAssetGroup, DeleteAssetGroup, MoveAssetToGroup, RemoveAssetFromGroup, AddLoanStateUpdate, DeleteLoanStateUpdate, GetLoanStateTimeline, GetLatestLoanState, MarkAssetAsSold, UndoAssetSale, ListSoldAssets.
    - Exactly 5 bold parameter-table labels exist (lines starting `**Parâmetros de`), covering AddLoanStateUpdate (8 params), DeleteLoanStateUpdate (2 params), the shared GetLoanStateTimeline/GetLatestLoanState table (1 param), MarkAssetAsSold (2 params), UndoAssetSale (1 param).
    - The parameter names `assetId`, `effectiveDate`, `totalBorrowed`, `interestAccruedUntilDate`, `collateralSats`, `apr`, `fees`, `note`, `dateSold` all appear backticked.
    - The note stating `ListSoldAssets` receives no parameters is present.
    - The comment `<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->` appears directly under the new H3 heading.
    - No `**Parâmetros de` label references any tool outside the 7 required ones, and the 8 pre-existing category sections are byte-identical to before the edit.
  </acceptance_criteria>
  <verify>
    <automated>
      F=../valt-docs/docs/funcionalidades/mcp-server.md && \
      grep -q '^### Ativos (AssetTools)' "$F" && \
      grep -q '^#### Operações de Ativos' "$F" && \
      grep -q '^#### Empréstimos BTC' "$F" && \
      grep -q '^#### Grupos de Ativos' "$F" && \
      grep -q '^#### Linha do Tempo do Estado do Empréstimo' "$F" && \
      grep -q '^#### Ativos Vendidos' "$F" && \
      for t in GetAssets GetVisibleAssets GetAsset GetAssetsSummary CreateBasicAsset CreateRealEstateAsset CreateLeveragedPosition UpdateAssetPrice UpdateAssetQuantity ToggleAssetVisibility ToggleAssetNetWorthInclusion DeleteAsset CreateBtcLoan CreateBtcLending RepayLoan GetAssetGroups CreateAssetGroup UpdateAssetGroup DeleteAssetGroup MoveAssetToGroup RemoveAssetFromGroup AddLoanStateUpdate DeleteLoanStateUpdate GetLoanStateTimeline GetLatestLoanState MarkAssetAsSold UndoAssetSale ListSoldAssets; do grep -q "\`$t\`" "$F" || { echo "MISSING TOOL: $t"; exit 1; }; done && \
      for p in assetId effectiveDate totalBorrowed interestAccruedUntilDate collateralSats apr fees note dateSold; do grep -q "\`$p\`" "$F" || { echo "MISSING PARAM: $p"; exit 1; }; done && \
      test "$(grep -c '^\*\*Parâmetros de' "$F")" -eq 5 && \
      grep -q 'não recebe parâmetros' "$F" && \
      grep -qF '<!-- Source: src/Valt.Infra/Mcp/Tools/AssetTools.cs -->' "$F" && \
      test "$(grep -n '^### Moedas (CurrencyTools)' "$F" | cut -d: -f1)" -lt "$(grep -n '^### Ativos (AssetTools)' "$F" | cut -d: -f1)" && \
      test "$(grep -n '^### Ativos (AssetTools)' "$F" | cut -d: -f1)" -lt "$(grep -n '^## Casos de Uso' "$F" | cut -d: -f1)"
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The Portuguese page documents the AssetTools category with all 28 tools in 5 grouped subsections, and exactly the 7 required tools have verbatim-from-code parameter tables (MCP-01, MCP-02, MCP-03 content in PT).</done>
</task>

<task>
  <id>37-01-02</id>
  <name>Add IndicatorTools section</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/mcp-server.md</files>
  <read_first>
    - .planning/phases/37-mcp-server-page-update/37-CONTEXT.md
    - .planning/phases/37-mcp-server-page-update/37-RESEARCH.md
    - .planning/phases/37-mcp-server-page-update/37-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/mcp-server.md
    - src/Valt.Infra/Mcp/Tools/IndicatorTools.cs
  </read_first>
  <action>
    In ../valt-docs/docs/funcionalidades/mcp-server.md, insert a new H3 section `### Indicadores (IndicatorTools)` immediately after the end of the `#### Ativos Vendidos` subsection added in task 37-01-01 (i.e., at the end of the AssetTools section) and before `## Casos de Uso 💡`. Directly under the heading place the source-evidence comment `<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->`, then a 1-line intro stating these are Bitcoin macro indicators refreshed in the background and served from cache, then a single-row 2-column table with header `| Ferramenta | Descrição |` whose only row is `GetBitcoinIndicators` with the description from RESEARCH §Code Example 4: it returns the current Bitcoin macro indicators — Mayer Multiple, Rainbow Chart, Fear & Greed Index, and BTC dominance. Use the exact indicator names Mayer Multiple, Rainbow Chart, Fear & Greed Index (matching the Reports UI panel labels per RESEARCH). The section is brief by design (D-06): one intro line, one table row, no parameter tables (the tool takes no user-facing parameters).
  </action>
  <acceptance_criteria>
    - The file contains the heading `### Indicadores (IndicatorTools)` positioned after `### Ativos (AssetTools)` and before `## Casos de Uso 💡`.
    - The section contains a table row with backticked `GetBitcoinIndicators`.
    - The description mentions Mayer Multiple, Rainbow Chart, and Fear & Greed Index.
    - The comment `<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->` appears directly under the new heading.
    - The section contains no `**Parâmetros de` labels.
  </acceptance_criteria>
  <verify>
    <automated>
      F=../valt-docs/docs/funcionalidades/mcp-server.md && \
      grep -q '^### Indicadores (IndicatorTools)' "$F" && \
      grep -q '`GetBitcoinIndicators`' "$F" && \
      grep -q 'Mayer Multiple' "$F" && \
      grep -q 'Rainbow Chart' "$F" && \
      grep -q 'Fear & Greed' "$F" && \
      grep -qF '<!-- Source: src/Valt.Infra/Mcp/Tools/IndicatorTools.cs -->' "$F" && \
      test "$(grep -n '^### Ativos (AssetTools)' "$F" | cut -d: -f1)" -lt "$(grep -n '^### Indicadores (IndicatorTools)' "$F" | cut -d: -f1)" && \
      test "$(grep -n '^### Indicadores (IndicatorTools)' "$F" | cut -d: -f1)" -lt "$(grep -n '^## Casos de Uso' "$F" | cut -d: -f1)"
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The Portuguese page documents the IndicatorTools category with its single Bitcoin macro-indicators tool (D-06), completing the current toolset listing.</done>
</task>

<task>
  <id>37-01-03</id>
  <name>Correct the tool-count claim and add the Ativos link to Próximos Passos</name>
  <type>docs</type>
  <files>../valt-docs/docs/funcionalidades/mcp-server.md</files>
  <read_first>
    - .planning/phases/37-mcp-server-page-update/37-RESEARCH.md
    - .planning/phases/37-mcp-server-page-update/37-PATTERNS.md
    - ../valt-docs/docs/funcionalidades/mcp-server.md
  </read_first>
  <action>
    In ../valt-docs/docs/funcionalidades/mcp-server.md, update the two occurrences of the stale tool-count claim (Q1; the page currently says it has "mais de 45" tools, which is self-contradictory once the page documents 81 tools). In the intro paragraph (currently line 3), change the count phrase to `mais de 80 ferramentas`, preserving the rest of the sentence. In the `## Ferramentas Disponíveis 🛠️` intro line (currently line 133), change the sentence to read `O servidor MCP do Valt expõe mais de 80 ferramentas organizadas por categoria.` Then, in the `## Próximos Passos` section at the end of the file (Q2), append one bullet after the `Preço Médio` line: `- [Ativos](ativos.md) - Investimentos externos, empréstimos BTC e histórico de ativos vendidos` (wording is the agent's discretion but the link target must be `ativos.md` and the description must mention assets/investments). Make no other edits in this task — do not touch the 8 pre-existing category tables, the Casos de Uso section, or any other count-like numbers elsewhere on the page (e.g. monetary amounts inside example conversations).
  </action>
  <acceptance_criteria>
    - The phrase `mais de 80 ferramentas` appears exactly twice in the file (intro paragraph and Ferramentas Disponíveis intro).
    - The previous "mais de 45 ..." count phrasing no longer appears anywhere in the file.
    - The `## Próximos Passos` list contains a bullet linking to `ativos.md`.
    - No other lines were modified (monetary amounts and example conversations remain intact).
  </acceptance_criteria>
  <verify>
    <automated>
      F=../valt-docs/docs/funcionalidades/mcp-server.md && \
      test "$(grep -c 'mais de 80 ferramentas' "$F")" -eq 2 && \
      ! grep -q 'mais de 45 ferramentas' "$F" && \
      grep -q '](ativos.md)' "$F" && \
      grep -q '^## Próximos Passos' "$F" && \
      grep -q 'R\$ 4.523,45' "$F"
    </automated>
  </verify>
  <autonomous>true</autonomous>
  <done>The Portuguese page's tool-count claim matches the documented toolset (80+) and Próximos Passos cross-links to the Ativos page (Q1, Q2 resolved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs ←→ Valt application code | Tool names, parameters, and behavior claims in the public docs must be traceable to `src/Valt.Infra/Mcp/Tools/*.cs` signatures and `[Description]` attributes. |
| Portuguese source ←→ English mirror (plan 37-02) | Every structural change made here must be mirrorable 1:1 in the `.en.md` file in the next plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01-01 | Tampering (integrity of guidance) | Portuguese MCP Server page | High | Mitigate | D-05 verbatim-from-code rule: every tool name, parameter name, and optionality flag is copied from `AssetTools.cs` / `IndicatorTools.cs` `[Description]` attributes; source-evidence HTML comments placed under each new heading; automated grep checks enumerate all 28 tool names and 9 parameter names. |
| T-37-01-02 | Information Disclosure | Public docs pages | Low | Accept | All documented material is already public in the open-source repository; no secrets, PII, or credentials exist in tool signatures. The pre-existing `!!! warning "Segurança"` localhost-only admonition is preserved untouched; no tunneling instructions are added. |
| T-37-01-03 | Tampering | Scope creep into pre-existing categories | Medium | Mitigate | D-04 enforced by acceptance criteria: 8 pre-existing category sections must remain byte-identical; drifted names (e.g. `CreateDCAGoal`) are explicitly logged for Phase 38 QA instead of being fixed here. |
| T-37-01-SC | Tampering | pip/mkdocs installs | Low | N/A | No new packages are installed. The build gate in plan 37-02 uses the existing `valt-docs/.venv` virtual environment. |
</threat_model>

<verification>
- `### Ativos (AssetTools)` exists with 5 H4 subsections, 28 backticked tool names, and parameter tables for exactly the 7 required tools.
- `### Indicadores (IndicatorTools)` exists with `GetBitcoinIndicators`.
- The page claims `mais de 80 ferramentas` in exactly 2 spots and links to `ativos.md` in Próximos Passos.
- The 8 pre-existing category sections are untouched (D-04).
- Ordering: CurrencyTools → Ativos (AssetTools) → Indicadores (IndicatorTools) → Casos de Uso.
</verification>

<success_criteria>
- A Portuguese reader can see the `AssetTools` category and its purpose on the MCP Server page (MCP-01).
- A Portuguese reader can see the loan-state tools `AddLoanStateUpdate`, `DeleteLoanStateUpdate`, `GetLoanStateTimeline`, `GetLatestLoanState` and their parameters (MCP-02).
- A Portuguese reader can see the sold-asset tools `MarkAssetAsSold`, `UndoAssetSale`, `ListSoldAssets` and their parameters (MCP-03).
- The page no longer contradicts itself about how many tools exist.
</success_criteria>

<output>
Create `.planning/phases/37-mcp-server-page-update/37-01-SUMMARY.md` when done.
</output>

## Context Decisions Addressed

- **D-01** (Task 37-01-01): AssetTools added as `### Ativos (AssetTools)` inside `## Ferramentas Disponíveis 🛠️`, appended after `### Moedas (CurrencyTools)` — the agent's-discretion placement from RESEARCH §Architecture Patterns (zero disruption to existing sections; newest categories last).
- **D-02** (Task 37-01-01): 28 tools split into 5 grouped H4 subsections, each keeping the 2-column `| Ferramenta | Descrição |` style.
- **D-03** (Task 37-01-01): Parameter tables for exactly the 7 required tools, placed as labeled tables below the group tables (RESEARCH Pattern 2 — markdown cannot nest tables in rows).
- **D-04** (Tasks 37-01-01, 37-01-03): No parameter retrofit and no name fixes in the 8 pre-existing categories.
- **D-05** (Task 37-01-01): All parameter names/optionality copied from `AssetTools.cs` signatures and `[Description]` attributes.
- **D-06** (Task 37-01-02): Brief `### Indicadores (IndicatorTools)` section added; requirements traceability stays on MCP-01/02/03.
- **Q1 resolution** (Task 37-01-03): Tool-count claim updated to `mais de 80 ferramentas` in both spots.
- **Q2 resolution** (Task 37-01-03): `Ativos` link added to Próximos Passos.

## Deferred / Out of Scope (for Phase 38 QA)

- **Q3 / RESEARCH Open Question 3:** Pre-existing category name drift is NOT fixed in this phase per D-04. Documented names that differ from code: GoalTools (docs list 8 incl. `CreateDCAGoal`; code has 13 incl. `CreateDcaGoal`), AvgPriceTools (docs 11 incl. `GetAvgPriceProfiles`; code 12 incl. `GetProfile`), ReportTools (docs 6 incl. `GetWealthHistory`; code 7 incl. `GetAllTimeHigh`/`GetMaxBtcStack`/`GetStatistics`), AccountTools (docs `CreateAccount`; code `CreateFiatAccount`/`CreateBtcAccount`), TransactionTools (docs list `AddBitcoinToBitcoinTransfer`; code has `DeleteTransaction`). Phase 38 (QA) should decide whether to correct these.
- Retrofitting parameter tables onto the 8 pre-existing categories (deferred from CONTEXT.md).
- Screenshots; translation to languages beyond pt-BR/en-US.

## Two-Repository Protocol

- Doc changes live in the sibling `valt-docs` repo: commit there with `git -C /home/vmabellini/RiderProjects/valt-docs` using the prefix `docs(37-01): ...` (matching `docs(36-01):` history).
- Planning artifacts (SUMMARY etc.) live in the main `valt` repo under `.planning/` — new files require `git add -f` (the path is gitignored).
- Never mix both repos' files in one commit.
- The final docs verification gate runs in plan 37-02: `cd ../valt-docs && . .venv/bin/activate && mkdocs build --strict` (the `.venv` activation is MANDATORY — pipx/system mkdocs fail per RESEARCH Pitfall 1).

## Artifacts this phase produces

**New files:**
- None.

**Modified files:**
- `../valt-docs/docs/funcionalidades/mcp-server.md` — Portuguese MCP Server page: new `### Ativos (AssetTools)` section (5 H4 subsections, 28 tool rows, 5 labeled parameter tables, 1 no-parameters note, 2 subsection intros, source-evidence comment), new `### Indicadores (IndicatorTools)` section (intro + 1-row table + source-evidence comment), tool-count claim updated to `mais de 80 ferramentas` in 2 spots, new `Ativos` bullet in `## Próximos Passos`.

**Symbols created (doc anchors / headings):**
- `### Ativos (AssetTools)` with `#### Operações de Ativos`, `#### Empréstimos BTC`, `#### Grupos de Ativos`, `#### Linha do Tempo do Estado do Empréstimo`, `#### Ativos Vendidos` (in `mcp-server.md`)
- `### Indicadores (IndicatorTools)` (in `mcp-server.md`)
- 5 parameter-table labels: `**Parâmetros de \`AddLoanStateUpdate\`:**`, `**Parâmetros de \`DeleteLoanStateUpdate\`:**`, `**Parâmetros de \`GetLoanStateTimeline\` e \`GetLatestLoanState\`:**`, `**Parâmetros de \`MarkAssetAsSold\`:**`, `**Parâmetros de \`UndoAssetSale\`:**`
