---
phase: quick
task_id: 260630-e2j
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
  - tests/Valt.Tests/Application/Assets/Queries/GetAssetSummaryHandlerTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - LONG leveraged positions with BTC collateral contribute only their PnL (current result) to asset summary totals, not collateral plus PnL.
    - The Assets tab summary and the Transactions tab totals summary both derive from the same fixed AssetQueries.GetSummaryAsync calculation.
    - Existing SHORT BTC-collateral and fiat-collateral leveraged position summary behavior is preserved.
  artifacts:
    - path: src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
      provides: Correct GetValueForSummary logic for LONG BTC-collateral leveraged positions
      contains: LeveragedPositionDetails switch with IsLong check
    - path: tests/Valt.Tests/Application/Assets/Queries/GetAssetSummaryHandlerTests.cs
      provides: Unit tests verifying LONG BTC-collateral summary uses PnL only
  key_links:
    - from: AssetQueries.GetValueForSummary
      to: LeveragedPositionDetails.CalculatePnL
      via: Returns CalculatePnL for LONG BTC-collateral positions
      pattern: leveraged.IsLong \? leveraged.CalculatePnL
    - from: AccountsTotalState.CurrentWealth
      to: AssetQueries.GetSummaryAsync
      via: AssetsWealthInMainFiatCurrency = AssetSummary.TotalValueInMainCurrency
      pattern: AssetSummary\?\.TotalValueInMainCurrency
---

<objective>
Fix the asset summary calculation so that LONG leveraged positions with BTC collateral contribute only their current PnL (result) to the totals shown in the Assets tab and the Transactions tab wealth summary, instead of the full current value which includes collateral.

Purpose: Prevents LONG BTC-collateral positions from inflating net worth by double-counting collateral that is already tracked as BTC holdings.
Output: Updated query logic in AssetQueries.cs plus unit tests for GetAssetSummaryHandler.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./AGENTS.md
@src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs
@src/Valt.Core/Modules/Assets/Details/LeveragedPositionDetails.cs
@src/Valt.UI/State/AccountsTotalState.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LONG BTC-collateral leveraged position summary value</name>
  <files>src/Valt.Infra/Modules/Assets/Queries/AssetQueries.cs</files>
  <action>
    In AssetQueries.GetValueForSummary, change the LeveragedPositionDetails branch so that LONG positions always return CalculatePnL(leveraged.CurrentPrice), even when CollateralAssetType is Btc. Keep SHORT BTC-collateral positions on CalculateCurrentValue to preserve their existing behavior, and keep fiat-collateral positions on CalculatePnL as before. Update the XML comment above GetValueForSummary to state that LONG BTC-collateral positions use only PnL so collateral is not counted twice, while SHORT BTC-collateral positions still use the full position value.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>
    LONG BTC-collateral leveraged positions in GetValueForSummary use CalculatePnL; build succeeds; comment accurately describes the new behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for asset summary leveraged position totals</name>
  <files>tests/Valt.Tests/Application/Assets/Queries/GetAssetSummaryHandlerTests.cs</files>
  <action>
    Create a new GetAssetSummaryHandlerTests class inheriting from DatabaseTest. Seed the in-memory database with a LONG BTC-collateral leveraged position created via AssetBuilder.WithDetails(new LeveragedPositionDetails(...)) using Btc collateral asset type, a positive contract count and contract size. Dispatch GetAssetSummaryQuery with MainCurrencyCode "USD", BtcPriceUsd matching the position's current price, and FiatRates {"USD": 1}. Assert that TotalValueInMainCurrency equals the position's PnL (not the full current value), TotalValueInSats converts that PnL at the BTC price, and TotalAssetsValueInMainCurrency matches the same PnL. Add a second assertion for a fiat-collateral LONG position verifying it continues to use PnL only. Run the new tests.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~Valt.Tests.Application.Assets.Queries.GetAssetSummaryHandlerTests"</automated>
  </verify>
  <done>
    New tests pass and prove LONG BTC-collateral summary uses PnL only while fiat-collateral LONG behavior is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User data → Asset summary query | Untrusted persisted asset data is converted to summary totals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260630-e2j-01 | Information Disclosure | Asset summary totals | accept | Values are user's own data displayed in the UI |
| T-260630-e2j-02 | Tampering | AssetQueries calculation logic | mitigate | Unit tests assert correct PnL-only behavior for LONG BTC-collateral positions |
</threat_model>

<verification>
- `dotnet build Valt.sln` succeeds with no errors.
- `dotnet test --filter "FullyQualifiedName~Valt.Tests.Application.Assets.Queries.GetAssetSummaryHandlerTests"` passes.
- Manual spot-check (optional): the Assets tab Total Value and the Transactions tab Assets wealth for a LONG BTC-collateral position now show the PnL amount only, not collateral plus PnL.
</verification>

<success_criteria>
- AssetQueries.GetValueForSummary returns CalculatePnL for LONG leveraged positions with BTC collateral.
- New GetAssetSummaryHandlerTests verify the corrected totals and pass.
- Solution builds and existing tests are not broken.
</success_criteria>

<output>
Create `.planning/quick/260630-e2j-fix-assets-tab-long-position-total-in-tr/260630-e2j-SUMMARY.md` when done.
</output>
