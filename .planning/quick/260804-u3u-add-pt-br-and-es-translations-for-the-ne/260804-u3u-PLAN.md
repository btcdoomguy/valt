---
quick_id: 260804-u3u
slug: add-pt-br-and-es-translations-for-the-ne
mode: quick
status: planned
---

# Quick Task: Add pt-BR and es translations for Phase 39 Reports strings

## Task Boundary

The new Phase 39 Reports panels (Burn Rate, Savings Rate, Fixed vs Variable) only have English strings in `language.resx`. Add the missing Portuguese (pt-BR) and Spanish (es) translations to `language.pt-BR.resx` and `language.es.resx`.

## Implementation Plan

- Identify all new `Reports_BurnRate_*`, `Reports_SavingsRate_*`, and `Reports_FixedVariable_*` keys in `language.resx`.
- Insert corresponding `<data>` entries into `language.pt-BR.resx` and `language.es.resx` before the closing `</root>` tag.
- Build `Valt.UI` and run `ReportsViewModelTests` to confirm no resource issues.

## Verification

- `dotnet build Valt.sln` succeeds with 0 errors.
- `dotnet test --filter "FullyQualifiedName~ReportsViewModelTests"` passes.
