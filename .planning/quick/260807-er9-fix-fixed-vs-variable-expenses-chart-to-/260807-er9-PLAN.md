# Quick Task 260807-er9: Fix Fixed vs Variable Expenses chart to show all 12 months including current and future months, matching Sats earned & spent chart behavior

**Date:** 2026-08-07
**Mode:** quick

## Goal

The Reports tab's "Fixed vs Variable Expenses" panel currently filters out the current month and future months from its X-axis. The adjacent "Sats earned & spent" panel already shows every month in the selected date range, including empty/current/future months. Apply the same behavior to Fixed vs Variable Expenses.

## Tasks

### 1. Update the Fixed vs Variable query to emit all months in the date range

- File: `src/Valt.Infra/Modules/SpendingAnalytics/Queries/FixedVsVariableQueries.cs`
- Replace the current filter (`Where(... < firstOfCurrentMonth)`) with a month-by-month walk from `query.From` to `query.To`, like `BtcDenominatedMetricsQueries` does.
- For each month in the range, return a `FixedVsVariableMonthDto` with `FixedTotal` and `VariableTotal` (defaulting to `0m` when no transactions exist for that month).
- The `firstOfCurrentMonth` variable can be removed unless still needed elsewhere.

### 2. Update existing tests to match the new behavior

- File: `tests/Valt.Tests/Reports/FixedVsVariableQueriesTests.cs`
- Rename/adjust `Should_Exclude_Current_Incomplete_Month` to assert the current month is now included.
- Adjust `Should_Return_Empty_Months_When_No_Expense_Transactions_In_Range` so it expects a month entry for every month in the queried range (with zero totals) instead of an empty list.

### 3. Build and test

- Run `dotnet build Valt.sln`.
- Run the affected test class: `dotnet test --filter "FullyQualifiedName~FixedVsVariableQueriesTests"`.

## Verification

- Build succeeds.
- All `FixedVsVariableQueriesTests` pass.
- The `FixedVsVariableChartData.RefreshChart` method already consumes `data.Months` in order, so no UI change is required beyond the query change.
