# Quick Task 260807-ff0: Add explanatory labels to Reports tab panels with full translations

**Date:** 2026-08-07
**Mode:** quick

## Goal

Add a simple explanatory label under the header of each non-summary panel on the Reports tab, describing what the panel represents and how it is calculated. Provide full translations for English (en-US), Portuguese (pt-BR), and Spanish (es).

## Affected panels

1. Wealth Overview
2. Monthly totals
3. Fixed vs variable expenses
4. Sats earned & spent
5. Stack velocity
6. Categories

## Tasks

### 1. Add localized strings to all three language files

- Files:
  - `src/Valt.UI/Lang/language.resx`
  - `src/Valt.UI/Lang/language.pt-BR.resx`
  - `src/Valt.UI/Lang/language.es.resx`
- Keys:
  - `Reports_WealthOverview_Description`
  - `Reports_MonthlyTotals_Description`
  - `Reports_FixedVariable_Description`
  - `Reports_BtcMetrics_Description`
  - `Reports_StackVelocity_Description`
  - `Reports_ByCategories_Description`
- Insert new entries before the closing `</root>` tag, following the existing format.

### 2. Update the language designer file

- File: `src/Valt.UI/Lang/language.Designer.cs`
- Add one public static string property per new key, mirroring the existing generated pattern.

### 3. Add explanatory TextBlocks to the Reports view

- File: `src/Valt.UI/Views/Main/Tabs/Reports/ReportsView.axaml`
- Add a styled `TextBlock` under each panel header `StackPanel`, right after the header `StackPanel`, with:
  - Text bound to the new language property
  - Small font size, muted color (`Text500Brush` or `Text400Brush`)
  - Wrapping enabled and a max width so long texts remain readable
  - Top/bottom margin to separate from header and content

### 4. Build and test

- Run `dotnet build Valt.sln`.
- Run `dotnet test --filter "FullyQualifiedName~ReportsViewModelTests"`.

### 5. Update quick-task artifacts

- Create `260807-ff0-SUMMARY.md`.
- Update `.planning/STATE.md` Quick Tasks Completed table.
- Commit code changes and docs artifacts separately.
