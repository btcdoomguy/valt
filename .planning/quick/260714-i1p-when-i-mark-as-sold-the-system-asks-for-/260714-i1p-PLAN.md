---
phase: quick-260714-i1p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml
  - src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptViewModel.cs
  - src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml.cs
autonomous: true
requirements:
  - QUICK-260714-i1p
must_haves:
  truths:
    - User can mark an asset as sold and pick a specific day from a calendar popup
    - DateSold prompt uses the same CalendarDatePicker pattern as the transaction editor
    - PreviousDay / NextDay buttons and SelectToday context menu work in the sold-date prompt
  artifacts:
    - path: src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml
      provides: CalendarDatePicker layout matching transaction editor
    - path: src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptViewModel.cs
      provides: DateSold as nullable DateTime, PreviousDay/NextDay/SelectToday commands
    - path: src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml.cs
      provides: Keyboard shortcuts for date navigation (Ctrl+Left/Right/R)
  key_links:
    - from: DateSoldPromptView.axaml
      to: DateSoldPromptViewModel.cs
      via: SelectedDate TwoWay bound to DateSold property
    - from: DateSoldPromptViewModel.cs
      to: AssetsViewModel.MarkAssetAsSold
      via: Response.DateSold is consumed by MarkAssetAsSoldCommand.DateSold
---

<objective>
Replace the month/year-only DateCalendarSelector in the Mark as Sold prompt with a CalendarDatePicker that lets the user pick the exact day, matching the transaction editor date picker pattern.

Purpose: The current sold-date selector only exposes month/year, forcing users to the first day of the month. The transaction editor already has a day-accurate CalendarDatePicker with PreviousDay/NextDay buttons and a SelectToday context menu; reusing that pattern fixes the UX and keeps the UI consistent.
Output: Updated DateSold prompt view, view model, and keyboard shortcuts.
</objective>

<execution_context>
@/home/vmabellini/.config/opencode/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@/home/vmabellini/RiderProjects/valt/.planning/STATE.md
@/home/vmabellini/RiderProjects/valt/AGENTS.md
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptViewModel.cs
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Modals/TransactionEditor/TransactionEditorView.axaml
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Modals/TransactionEditor/TransactionEditorView.axaml.cs
@/home/vmabellini/RiderProjects/valt/src/Valt.UI/Views/Main/Modals/TransactionEditor/TransactionEditorViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DateSoldPromptViewModel for CalendarDatePicker</name>
  <files>src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptViewModel.cs</files>
  <action>
    Change the DateSold property from DateTime to DateTime? (nullable) so it can bind to CalendarDatePicker.SelectedDate without coercion issues. Initialize it to DateTime.Now.Date. Add [RelayCommand] methods PreviousDay(), NextDay(), and SelectToday() that mirror the transaction editor commands: PreviousDay sets DateSold to DateSold.Value.AddDays(-1) (guard null), NextDay sets DateSold to DateSold.Value.AddDays(1) (guard null), and SelectToday sets DateSold to DateTime.Now.Date. Update the Ok() method to pass the nullable DateSold value directly into the Response record (the Response type already accepts DateTime?). Keep the WindowTitle and CancelCommand unchanged. Do not add validation attributes beyond the existing nullable type.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>DateSold is DateTime?, defaulting to today, with working PreviousDay, NextDay, and SelectToday commands; Ok returns the selected nullable date.</done>
</task>

<task type="auto">
  <name>Task 2: Replace DateCalendarSelector with CalendarDatePicker in DateSoldPromptView</name>
  <files>src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml</files>
  <action>
    Replace the DateCalendarSelector block inside the StackPanel with a horizontal StackPanel (Orientation="Horizontal", HorizontalAlignment="Center", Spacing="0") containing: a MaterialDesign Button with Content="&#xE5DE;" bound to {Binding PreviousDayCommand}, a CalendarDatePicker with Width="140", SelectedDateFormat="Short", IsTodayHighlighted="True", SelectedDate="{Binding DateSold, Mode=TwoWay}", and a ContextMenu containing a MenuItem Header="{x:Static lang:language.ManageTransactions_SelectToday}" Command="{Binding SelectTodayCommand}", and a MaterialDesign Button with Content="&#xE5DF;" bound to {Binding NextDayCommand}. Remove the now-unused DateCalendarSelector userControls xmlns import if no other userControls in this view use it (CustomTitleBar is also a userControl, so keep the xmlns). Reuse the existing lang namespace. Ensure the prompt text remains above the new date picker. Do not change the window dimensions or button layout at the bottom of the modal.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>DateSoldPromptView renders a CalendarDatePicker with PreviousDay/NextDay buttons and SelectToday context menu, bound to DateSold.</done>
</task>

<task type="auto">
  <name>Task 3: Add keyboard shortcuts for date navigation</name>
  <files>src/Valt.UI/Views/Main/Modals/DateSoldPrompt/DateSoldPromptView.axaml.cs</files>
  <action>
    Add a KeyDown handler in the DateSoldPromptView constructor using this.AddHandler(KeyDownEvent, OnKeyDown, RoutingStrategies.Bubble, handledEventsToo: true). Add the required using directives for Avalonia.Input and Avalonia.Interactivity. Implement OnKeyDown to invoke the view model commands when the DataContext is DateSoldPromptViewModel: Ctrl+Left -> PreviousDayCommand, Ctrl+Right -> NextDayCommand, Ctrl+R -> SelectTodayCommand, Enter -> OkCommand, Escape already handled by the existing KeyBinding. Ensure e.Handled is set for the processed shortcuts. Do not add focus-management logic; the existing modal behavior is sufficient.
  </action>
  <verify>
    <automated>dotnet build Valt.sln</automated>
  </verify>
  <done>DateSoldPromptView.axaml.cs compiles with keyboard shortcuts for Ctrl+Left/Right/R and Enter, invoking the matching view model commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → ViewModel | User date input is converted to a nullable DateTime by the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QUICK-01 | Denial of Service | DateSoldPromptViewModel.PreviousDay/NextDay | mitigate | Guard against null DateSold before calling AddDays to avoid NullReferenceException |
| T-QUICK-02 | Information Disclosure | DateSold prompt | accept | No sensitive data beyond the already-exposed asset sale date |
</threat_model>

<verification>
- `dotnet build Valt.sln` passes with no errors or warnings introduced by the changed files.
- The DateSold prompt can be opened from the Assets tab, shows a calendar popup, and allows selecting a day.
- PreviousDay/NextDay buttons and SelectToday context menu change the displayed date.
- The selected date is passed through to MarkAssetAsSoldCommand.DateSold unchanged.
</verification>

<success_criteria>
- DateSold prompt uses CalendarDatePicker with day-level selection.
- UI matches the transaction editor date picker pattern (PreviousDay/NextDay buttons, SelectToday context menu).
- Keyboard shortcuts Ctrl+Left, Ctrl+Right, Ctrl+R, and Enter work in the prompt.
- Build remains green and the Mark as Sold flow continues to pass the selected date to the command handler.
</success_criteria>

<output>
Create `.planning/quick/260714-i1p-when-i-mark-as-sold-the-system-asks-for-/260714-i1p-SUMMARY.md` when done.
</output>
